{
  "samples": [
    {
      "token_name": "ZephyrLend",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract ZephyrLend is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"ZephyrLend\", \"ZEPH\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for ZephyrLend:\n- Top wallet holds 88% of total supply\n- Top 5 wallets hold 90% of supply\n- Only 251 total holders\n- Gini coefficient: 0.91 (extremely concentrated)\n- Top wallet funded via Tornado Cash 5 days before deployment\n- No distribution event or airdrop detected",
        "contract_functions": "Contract Function Analysis for ZephyrLend:\n- Owner-only: withdrawAll(), emergencyDrain(), clearStuckBalance()\n- Can transfer all ETH and tokens to owner at any time\n- Ownership NOT renounced, no timelock\n- 4 functions move funds to owner address\n- Contract is not verified on block explorer",
        "deployer_history": "Deployer History for ZephyrLend:\n- Wallet age: 7 days\n- 7 previous contracts, 3 flagged as scam\n- Funded via Railgun\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for ZephyrLend:\n- Twitter: deleted\n- Telegram: admin left, group muted\n- Website: domain expired\n- 5 paid influencer promotions detected\n- No doxxed team, anonymous founders",
        "similar_contracts": "Similar Contract Analysis for ZephyrLend:\n- Bytecode: 92% match with 18 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $10M in losses\n- TokenSniffer: 7/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for ZephyrLend:\n- Launch: $0.0459\n- ATH: $8.44 (day 1)\n- Crash: -97% in 20 minutes\n- Current: $0.000394\n- Classic pump-and-dump chart pattern"
      }
    },
    {
      "token_name": "AuraFinance",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract AuraFinance is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"AuraFinance\", \"AUR\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for AuraFinance:\n- Only 8 total holders (all active traders)\n- All 8 wallets created within 3 hours of each other\n- All funded from single source address\n- No wallets hold any other tokens\n- Average wallet age: 3 days",
        "contract_functions": "Contract Function Analysis for AuraFinance:\n- batchDistribute(address[],uint256[]) \u2014 bulk send tokens\n- batchCollect(address[],uint256[]) \u2014 reclaim tokens back\n- 20 registered 'market maker' addresses\n- rebalanceAllocations() called 206 times in 24h\n- Circular transfer infrastructure is explicit in code",
        "deployer_history": "Deployer History for AuraFinance:\n- Created 12 contracts in past 19 days\n- All contracts have batch transfer functions\n- Also operates 7 market maker wallets\n- No public identity\n- All previous tokens show volume anomalies",
        "social_signals": "Social Analysis for AuraFinance:\n- Marketing: entirely focused on 'TOP VOLUME ON BSC' claims\n- Twitter: 984 followers, mostly bots\n- No discussion of utility, technology, or roadmap\n- Listed on volume-ranking sites as #1\n- Zero organic community",
        "similar_contracts": "Similar Contract Analysis for AuraFinance:\n- 11 contracts by same deployer with identical batch transfer logic\n- All similar tokens show volume anomalies\n- TokenSniffer: 21/100\n- Pattern: deploy \u2192 inflate volume \u2192 get listed \u2192 abandon",
        "price_history": "Price History for AuraFinance:\n- Tight range: $0.68 - $1.24\n- Volatility: 0.12% daily (impossibly stable)\n- Price moves in exact $0.008 increments\n- Volume bars: uniform height (algorithmic)\n- No organic market dynamics visible"
      }
    },
    {
      "token_name": "AtlasProtocol",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/security/Pausable.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract AtlasProtocol is ERC20, Pausable, Ownable {\n    uint256 public constant INITIAL_SUPPLY = 50_000_000e18;\n    address public treasuryWallet;\n    uint256 public withdrawalTimelock = 48 hours;\n    mapping(address => uint256) public lastWithdrawal;\n    uint256 public totalStaked;\n    mapping(address => uint256) public stakedBalance;\n\n    constructor(address _treasury) ERC20(\"AtlasProtocol\", \"ATLA\") Ownable(msg.sender) {\n        treasuryWallet = _treasury;\n        _mint(address(this), INITIAL_SUPPLY);\n    }\n\n    function stake(uint256 amount) external whenNotPaused {\n        _transfer(msg.sender, address(this), amount);\n        stakedBalance[msg.sender] += amount;\n        totalStaked += amount;\n    }\n\n    function unstake(uint256 amount) external whenNotPaused {\n        require(stakedBalance[msg.sender] >= amount, \"Insufficient stake\");\n        require(block.timestamp >= lastWithdrawal[msg.sender] + withdrawalTimelock, \"Timelock\");\n        stakedBalance[msg.sender] -= amount;\n        totalStaked -= amount;\n        lastWithdrawal[msg.sender] = block.timestamp;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function setWithdrawalTimelock(uint256 _timelock) external onlyOwner {\n        withdrawalTimelock = _timelock;\n    }\n\n    /// @dev Admin function to rebalance treasury allocation\n    function rebalanceTreasury(uint256 amount) external onlyOwner {\n        _transfer(address(this), treasuryWallet, amount);\n    }\n\n    function pause() external onlyOwner { _pause(); }\n    function unpause() external onlyOwner { _unpause(); }\n\n    receive() external payable {}\n\n    function withdrawETH() external onlyOwner {\n        payable(treasuryWallet).transfer(address(this).balance);\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for AtlasProtocol:\n- Top wallet holds 81% of total supply\n- Top 5 wallets hold 85% of supply\n- Only 289 total holders\n- Gini coefficient: 0.92 (extremely concentrated)\n- Top wallet funded via Tornado Cash 3 days before deployment\n- No distribution event or airdrop detected",
        "contract_functions": "Contract Function Analysis for AtlasProtocol:\n- Owner-only: withdrawAll(), emergencyDrain(), clearStuckBalance()\n- Can transfer all ETH and tokens to owner at any time\n- Ownership NOT renounced, no timelock\n- 3 functions move funds to owner address\n- Contract is not verified on block explorer",
        "deployer_history": "Deployer History for AtlasProtocol:\n- Wallet age: 1 days\n- 6 previous contracts, 2 flagged as scam\n- Funded via Tornado Cash\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for AtlasProtocol:\n- Twitter: deleted\n- Telegram: admin left, group muted\n- Website: domain expired\n- 6 paid influencer promotions detected\n- No doxxed team, anonymous founders",
        "similar_contracts": "Similar Contract Analysis for AtlasProtocol:\n- Bytecode: 90% match with 16 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $2M in losses\n- TokenSniffer: 15/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for AtlasProtocol:\n- Launch: $0.0477\n- ATH: $10.14 (day 3)\n- Crash: -96% in 14 minutes\n- Current: $0.000400\n- Classic pump-and-dump chart pattern"
      }
    },
    {
      "token_name": "JunctionFi",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/security/Pausable.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract JunctionFi is ERC20, Pausable, Ownable {\n    uint256 public constant INITIAL_SUPPLY = 50_000_000e18;\n    address public treasuryWallet;\n    uint256 public withdrawalTimelock = 48 hours;\n    mapping(address => uint256) public lastWithdrawal;\n    uint256 public totalStaked;\n    mapping(address => uint256) public stakedBalance;\n\n    constructor(address _treasury) ERC20(\"JunctionFi\", \"JUNC\") Ownable(msg.sender) {\n        treasuryWallet = _treasury;\n        _mint(address(this), INITIAL_SUPPLY);\n    }\n\n    function stake(uint256 amount) external whenNotPaused {\n        _transfer(msg.sender, address(this), amount);\n        stakedBalance[msg.sender] += amount;\n        totalStaked += amount;\n    }\n\n    function unstake(uint256 amount) external whenNotPaused {\n        require(stakedBalance[msg.sender] >= amount, \"Insufficient stake\");\n        require(block.timestamp >= lastWithdrawal[msg.sender] + withdrawalTimelock, \"Timelock\");\n        stakedBalance[msg.sender] -= amount;\n        totalStaked -= amount;\n        lastWithdrawal[msg.sender] = block.timestamp;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function setWithdrawalTimelock(uint256 _timelock) external onlyOwner {\n        withdrawalTimelock = _timelock;\n    }\n\n    /// @dev Admin function to rebalance treasury allocation\n    function rebalanceTreasury(uint256 amount) external onlyOwner {\n        _transfer(address(this), treasuryWallet, amount);\n    }\n\n    function pause() external onlyOwner { _pause(); }\n    function unpause() external onlyOwner { _unpause(); }\n\n    receive() external payable {}\n\n    function withdrawETH() external onlyOwner {\n        payable(treasuryWallet).transfer(address(this).balance);\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for JunctionFi:\n- Top wallet holds 89% of total supply\n- Top 5 wallets hold 93% of supply\n- Only 60 total holders\n- Gini coefficient: 0.90 (extremely concentrated)\n- Top wallet funded via Tornado Cash 5 days before deployment\n- No distribution event or airdrop detected",
        "contract_functions": "Contract Function Analysis for JunctionFi:\n- Owner-only: withdrawAll(), emergencyDrain(), clearStuckBalance()\n- Can transfer all ETH and tokens to owner at any time\n- Ownership NOT renounced, no timelock\n- 6 functions move funds to owner address\n- Contract is not verified on block explorer",
        "deployer_history": "Deployer History for JunctionFi:\n- Wallet age: 1 days\n- 4 previous contracts, 2 flagged as scam\n- Funded via Railgun\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for JunctionFi:\n- Twitter: deleted\n- Telegram: admin left, group muted\n- Website: domain expired\n- 10 paid influencer promotions detected\n- No doxxed team, anonymous founders",
        "similar_contracts": "Similar Contract Analysis for JunctionFi:\n- Bytecode: 93% match with 16 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $1M in losses\n- TokenSniffer: 16/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for JunctionFi:\n- Launch: $0.0173\n- ATH: $12.15 (day 5)\n- Crash: -98% in 16 minutes\n- Current: $0.000438\n- Classic pump-and-dump chart pattern"
      }
    },
    {
      "token_name": "FrostSwap",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface IRouter {\n    function swapExactTokensForETH(uint256, uint256, address[] calldata, address, uint256) external;\n    function WETH() external view returns (address);\n}\n\ncontract FrostSwap is ERC20, Ownable {\n    IRouter public router;\n    address public pair;\n    uint256 public maxWalletSize = 2_000_000e18;\n    mapping(address => bool) private _isExcluded;\n    bool private _swapping;\n\n    constructor(address _router) ERC20(\"FrostSwap\", \"FROT\") Ownable(msg.sender) {\n        router = IRouter(_router);\n        _mint(msg.sender, 100_000_000e18);\n        _isExcluded[msg.sender] = true;\n        _isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function setMaxWallet(uint256 _max) external onlyOwner {\n        require(_max >= 1_000_000e18, \"Too low\");\n        maxWalletSize = _max;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!_isExcluded[to] && to != pair) {\n            require(balanceOf(to) + amount <= maxWalletSize, \"Max wallet\");\n        }\n        super._update(from, to, amount);\n    }\n\n    function clearStuckBalance() external onlyOwner {\n        payable(owner()).transfer(address(this).balance);\n    }\n\n    function clearStuckTokens(address token) external onlyOwner {\n        uint256 bal = IERC20(token).balanceOf(address(this));\n        IERC20(token).transfer(owner(), bal);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for FrostSwap:\n- Top wallet holds 88% of total supply\n- Top 5 wallets hold 92% of supply\n- Only 259 total holders\n- Gini coefficient: 0.95 (extremely concentrated)\n- Top wallet funded via Tornado Cash 5 days before deployment\n- No distribution event or airdrop detected",
        "contract_functions": "Contract Function Analysis for FrostSwap:\n- Owner-only: withdrawAll(), emergencyDrain(), clearStuckBalance()\n- Can transfer all ETH and tokens to owner at any time\n- Ownership NOT renounced, no timelock\n- 6 functions move funds to owner address\n- Contract is not verified on block explorer",
        "deployer_history": "Deployer History for FrostSwap:\n- Wallet age: 6 days\n- 8 previous contracts, 4 flagged as scam\n- Funded via Tornado Cash\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for FrostSwap:\n- Twitter: deleted\n- Telegram: admin left, group muted\n- Website: domain expired\n- 8 paid influencer promotions detected\n- No doxxed team, anonymous founders",
        "similar_contracts": "Similar Contract Analysis for FrostSwap:\n- Bytecode: 96% match with 18 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $4M in losses\n- TokenSniffer: 20/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for FrostSwap:\n- Launch: $0.0191\n- ATH: $2.15 (day 4)\n- Crash: -99% in 16 minutes\n- Current: $0.000347\n- Classic pump-and-dump chart pattern"
      }
    },
    {
      "token_name": "WaveDAO",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract WaveDAO is ERC20, Ownable {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public immutable vestingStart;\n    uint256 public constant VESTING_DURATION = 365 days;\n    uint256 public teamAllocation;\n    uint256 public teamClaimed;\n\n    constructor() ERC20(\"WaveDAO\", \"WAVE\") Ownable(msg.sender) {\n        vestingStart = block.timestamp;\n        teamAllocation = MAX_SUPPLY * 15 / 100; // 15% team\n        _mint(address(this), teamAllocation);\n        _mint(msg.sender, MAX_SUPPLY - teamAllocation); // 85% to LP/community\n    }\n\n    function claimVested() external onlyOwner {\n        uint256 elapsed = block.timestamp - vestingStart;\n        if (elapsed > VESTING_DURATION) elapsed = VESTING_DURATION;\n        uint256 totalVested = (teamAllocation * elapsed) / VESTING_DURATION;\n        uint256 claimable = totalVested - teamClaimed;\n        require(claimable > 0, \"Nothing to claim\");\n        teamClaimed += claimable;\n        _transfer(address(this), owner(), claimable);\n    }\n\n    function renounceOwnership() public override onlyOwner {\n        super.renounceOwnership();\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for WaveDAO:\n- 89,772 holders, growing organically +2.9%/month\n- Top wallet: 1.8% (protocol treasury, multisig 3/5)\n- Top 10: 14% (includes DEX pairs)\n- Gini: 0.51 (well distributed)\n- Ownership renounced, no admin keys",
        "contract_functions": "Contract Function Analysis for WaveDAO:\n- Standard ERC20, no owner functions\n- Immutable: no admin, no pause, no fee changes\n- Minted at deployment, no further mint possible\n- Audited by Certik\n- Ownership renounced at deployment",
        "deployer_history": "Deployer History for WaveDAO:\n- Wallet age: 1509 days\n- Known entity: verified team with public identities\n- 11 previous contracts, all active, zero scam flags\n- Funded via Coinbase\n- ENS: wavedao.eth (registered 316 days ago)",
        "social_signals": "Social Analysis for WaveDAO:\n- Twitter: 180077 followers, high organic engagement\n- Discord: 35493 members, active dev discussion\n- GitHub: 329 stars, 25 contributors\n- Team: fully doxxed, regular AMAs\n- Media: featured in Bankless",
        "similar_contracts": "Similar Contract Analysis for WaveDAO:\n- Standard OpenZeppelin patterns, no custom backdoors\n- No similarity to known scam templates\n- TokenSniffer: 93/100\n- GoPlus: no issues detected\n- Architecture similar to Aave contracts",
        "price_history": "Price History for WaveDAO:\n- Token age: 398 days\n- ATH: $38.71 | ATL: $3.02\n- 30d: +10.6% (tracks market conditions)\n- Volatility: 7% daily (normal for tier)\n- Healthy corrections and recoveries throughout history"
      }
    },
    {
      "token_name": "UltraSwap",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface IRouter {\n    function swapExactTokensForETH(uint256, uint256, address[] calldata, address, uint256) external;\n    function WETH() external view returns (address);\n}\n\ncontract UltraSwap is ERC20, Ownable {\n    IRouter public router;\n    address public pair;\n    uint256 public maxWalletSize = 2_000_000e18;\n    mapping(address => bool) private _isExcluded;\n    bool private _swapping;\n\n    constructor(address _router) ERC20(\"UltraSwap\", \"ULTT\") Ownable(msg.sender) {\n        router = IRouter(_router);\n        _mint(msg.sender, 100_000_000e18);\n        _isExcluded[msg.sender] = true;\n        _isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function setMaxWallet(uint256 _max) external onlyOwner {\n        require(_max >= 1_000_000e18, \"Too low\");\n        maxWalletSize = _max;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!_isExcluded[to] && to != pair) {\n            require(balanceOf(to) + amount <= maxWalletSize, \"Max wallet\");\n        }\n        super._update(from, to, amount);\n    }\n\n    function clearStuckBalance() external onlyOwner {\n        payable(owner()).transfer(address(this).balance);\n    }\n\n    function clearStuckTokens(address token) external onlyOwner {\n        uint256 bal = IERC20(token).balanceOf(address(this));\n        IERC20(token).transfer(owner(), bal);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for UltraSwap:\n- Top wallet holds 84% of total supply\n- Top 5 wallets hold 88% of supply\n- Only 92 total holders\n- Gini coefficient: 0.95 (extremely concentrated)\n- Top wallet funded via Tornado Cash 3 days before deployment\n- No distribution event or airdrop detected",
        "contract_functions": "Contract Function Analysis for UltraSwap:\n- Owner-only: withdrawAll(), emergencyDrain(), clearStuckBalance()\n- Can transfer all ETH and tokens to owner at any time\n- Ownership NOT renounced, no timelock\n- 6 functions move funds to owner address\n- Contract is not verified on block explorer",
        "deployer_history": "Deployer History for UltraSwap:\n- Wallet age: 3 days\n- 8 previous contracts, 2 flagged as scam\n- Funded via Railgun\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for UltraSwap:\n- Twitter: deleted\n- Telegram: admin left, group muted\n- Website: domain expired\n- 7 paid influencer promotions detected\n- No doxxed team, anonymous founders",
        "similar_contracts": "Similar Contract Analysis for UltraSwap:\n- Bytecode: 92% match with 16 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $13M in losses\n- TokenSniffer: 13/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for UltraSwap:\n- Launch: $0.0169\n- ATH: $16.01 (day 4)\n- Crash: -97% in 14 minutes\n- Current: $0.000214\n- Classic pump-and-dump chart pattern"
      }
    },
    {
      "token_name": "ShardProtocol",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract ShardProtocol is ERC20, Ownable {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public immutable vestingStart;\n    uint256 public constant VESTING_DURATION = 365 days;\n    uint256 public teamAllocation;\n    uint256 public teamClaimed;\n\n    constructor() ERC20(\"ShardProtocol\", \"SHAR\") Ownable(msg.sender) {\n        vestingStart = block.timestamp;\n        teamAllocation = MAX_SUPPLY * 15 / 100; // 15% team\n        _mint(address(this), teamAllocation);\n        _mint(msg.sender, MAX_SUPPLY - teamAllocation); // 85% to LP/community\n    }\n\n    function claimVested() external onlyOwner {\n        uint256 elapsed = block.timestamp - vestingStart;\n        if (elapsed > VESTING_DURATION) elapsed = VESTING_DURATION;\n        uint256 totalVested = (teamAllocation * elapsed) / VESTING_DURATION;\n        uint256 claimable = totalVested - teamClaimed;\n        require(claimable > 0, \"Nothing to claim\");\n        teamClaimed += claimable;\n        _transfer(address(this), owner(), claimable);\n    }\n\n    function renounceOwnership() public override onlyOwner {\n        super.renounceOwnership();\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for ShardProtocol:\n- 113,976 holders, growing organically +2.3%/month\n- Top wallet: 3.1% (protocol treasury, multisig 3/5)\n- Top 10: 18% (includes DEX pairs)\n- Gini: 0.42 (well distributed)\n- Ownership renounced, no admin keys",
        "contract_functions": "Contract Function Analysis for ShardProtocol:\n- Standard ERC20, no owner functions\n- Immutable: no admin, no pause, no fee changes\n- Minted at deployment, no further mint possible\n- Audited by Trail of Bits\n- Ownership renounced at deployment",
        "deployer_history": "Deployer History for ShardProtocol:\n- Wallet age: 577 days\n- Known entity: verified team with public identities\n- 11 previous contracts, all active, zero scam flags\n- Funded via known VC\n- ENS: shardprotocol.eth (registered 353 days ago)",
        "social_signals": "Social Analysis for ShardProtocol:\n- Twitter: 158310 followers, high organic engagement\n- Discord: 35822 members, active dev discussion\n- GitHub: 353 stars, 29 contributors\n- Team: fully doxxed, regular AMAs\n- Media: featured in Bankless",
        "similar_contracts": "Similar Contract Analysis for ShardProtocol:\n- Standard OpenZeppelin patterns, no custom backdoors\n- No similarity to known scam templates\n- TokenSniffer: 86/100\n- GoPlus: no issues detected\n- Architecture similar to Compound contracts",
        "price_history": "Price History for ShardProtocol:\n- Token age: 200 days\n- ATH: $79.51 | ATL: $3.02\n- 30d: +0.2% (tracks market conditions)\n- Volatility: 11% daily (normal for tier)\n- Healthy corrections and recoveries throughout history"
      }
    },
    {
      "token_name": "IndexVault",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract IndexVault is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"IndexVault\", \"IND\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for IndexVault:\n- Only 7 total holders (all active traders)\n- All 7 wallets created within 5 hours of each other\n- All funded from single source address\n- No wallets hold any other tokens\n- Average wallet age: 4 days",
        "contract_functions": "Contract Function Analysis for IndexVault:\n- batchDistribute(address[],uint256[]) \u2014 bulk send tokens\n- batchCollect(address[],uint256[]) \u2014 reclaim tokens back\n- 17 registered 'market maker' addresses\n- rebalanceAllocations() called 614 times in 24h\n- Circular transfer infrastructure is explicit in code",
        "deployer_history": "Deployer History for IndexVault:\n- Created 10 contracts in past 23 days\n- All contracts have batch transfer functions\n- Also operates 11 market maker wallets\n- No public identity\n- All previous tokens show volume anomalies",
        "social_signals": "Social Analysis for IndexVault:\n- Marketing: entirely focused on 'TOP VOLUME ON BSC' claims\n- Twitter: 1295 followers, mostly bots\n- No discussion of utility, technology, or roadmap\n- Listed on volume-ranking sites as #1\n- Zero organic community",
        "similar_contracts": "Similar Contract Analysis for IndexVault:\n- 15 contracts by same deployer with identical batch transfer logic\n- All similar tokens show volume anomalies\n- TokenSniffer: 27/100\n- Pattern: deploy \u2192 inflate volume \u2192 get listed \u2192 abandon",
        "price_history": "Price History for IndexVault:\n- Tight range: $0.97 - $1.10\n- Volatility: 0.32% daily (impossibly stable)\n- Price moves in exact $0.008 increments\n- Volume bars: uniform height (algorithmic)\n- No organic market dynamics visible"
      }
    },
    {
      "token_name": "EclipseSwap",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract EclipseSwap is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"EclipseSwap\", \"ECLI\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for EclipseSwap:\n- Only 11 total holders (all active traders)\n- All 11 wallets created within 5 hours of each other\n- All funded from single source address\n- No wallets hold any other tokens\n- Average wallet age: 1 days",
        "contract_functions": "Contract Function Analysis for EclipseSwap:\n- batchDistribute(address[],uint256[]) \u2014 bulk send tokens\n- batchCollect(address[],uint256[]) \u2014 reclaim tokens back\n- 13 registered 'market maker' addresses\n- rebalanceAllocations() called 547 times in 24h\n- Circular transfer infrastructure is explicit in code",
        "deployer_history": "Deployer History for EclipseSwap:\n- Created 6 contracts in past 24 days\n- All contracts have batch transfer functions\n- Also operates 5 market maker wallets\n- No public identity\n- All previous tokens show volume anomalies",
        "social_signals": "Social Analysis for EclipseSwap:\n- Marketing: entirely focused on 'TOP VOLUME ON BSC' claims\n- Twitter: 533 followers, mostly bots\n- No discussion of utility, technology, or roadmap\n- Listed on volume-ranking sites as #1\n- Zero organic community",
        "similar_contracts": "Similar Contract Analysis for EclipseSwap:\n- 15 contracts by same deployer with identical batch transfer logic\n- All similar tokens show volume anomalies\n- TokenSniffer: 40/100\n- Pattern: deploy \u2192 inflate volume \u2192 get listed \u2192 abandon",
        "price_history": "Price History for EclipseSwap:\n- Tight range: $0.89 - $1.43\n- Volatility: 0.05% daily (impossibly stable)\n- Price moves in exact $0.001 increments\n- Volume bars: uniform height (algorithmic)\n- No organic market dynamics visible"
      }
    },
    {
      "token_name": "UltraSwap",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Burnable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract UltraSwap is ERC20, ERC20Burnable, ReentrancyGuard, Ownable {\n    uint256 public constant MAX_SUPPLY = 50_000_000e18;\n    uint256 public rewardRate = 100; // 1% per epoch\n    mapping(address => uint256) public staked;\n    mapping(address => uint256) public stakeTimestamp;\n    uint256 public totalStaked;\n    bool public stakingEnabled = true;\n\n    constructor() ERC20(\"UltraSwap\", \"ULTR\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n    }\n\n    function stake(uint256 amount) external nonReentrant {\n        require(stakingEnabled, \"Staking paused\");\n        _claimReward(msg.sender);\n        _transfer(msg.sender, address(this), amount);\n        staked[msg.sender] += amount;\n        totalStaked += amount;\n        stakeTimestamp[msg.sender] = block.timestamp;\n    }\n\n    function unstake(uint256 amount) external nonReentrant {\n        require(staked[msg.sender] >= amount, \"Insufficient\");\n        _claimReward(msg.sender);\n        staked[msg.sender] -= amount;\n        totalStaked -= amount;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function _claimReward(address user) internal {\n        if (staked[user] > 0 && stakeTimestamp[user] > 0) {\n            uint256 elapsed = block.timestamp - stakeTimestamp[user];\n            uint256 reward = (staked[user] * rewardRate * elapsed) / (10000 * 365 days);\n            if (reward > 0 && totalSupply() + reward <= MAX_SUPPLY) {\n                _mint(user, reward);\n            }\n        }\n        stakeTimestamp[user] = block.timestamp;\n    }\n\n    function setRewardRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 1000, \"Max 10%\");\n        rewardRate = _rate;\n    }\n\n    function toggleStaking() external onlyOwner {\n        stakingEnabled = !stakingEnabled;\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for UltraSwap:\n- 41,624 holders, growing organically +1.6%/month\n- Top wallet: 2.7% (protocol treasury, multisig 3/5)\n- Top 10: 15% (includes DEX pairs)\n- Gini: 0.52 (well distributed)\n- Ownership renounced, no admin keys",
        "contract_functions": "Contract Function Analysis for UltraSwap:\n- Standard ERC20, no owner functions\n- Immutable: no admin, no pause, no fee changes\n- Minted at deployment, no further mint possible\n- Audited by Trail of Bits\n- Ownership renounced at deployment",
        "deployer_history": "Deployer History for UltraSwap:\n- Wallet age: 654 days\n- Known entity: verified team with public identities\n- 6 previous contracts, all active, zero scam flags\n- Funded via protocol revenue\n- ENS: ultraswap.eth (registered 395 days ago)",
        "social_signals": "Social Analysis for UltraSwap:\n- Twitter: 88754 followers, high organic engagement\n- Discord: 41558 members, active dev discussion\n- GitHub: 416 stars, 29 contributors\n- Team: fully doxxed, regular AMAs\n- Media: featured in The Block",
        "similar_contracts": "Similar Contract Analysis for UltraSwap:\n- Standard OpenZeppelin patterns, no custom backdoors\n- No similarity to known scam templates\n- TokenSniffer: 89/100\n- GoPlus: no issues detected\n- Architecture similar to Compound contracts",
        "price_history": "Price History for UltraSwap:\n- Token age: 512 days\n- ATH: $90.55 | ATL: $1.72\n- 30d: -4.4% (tracks market conditions)\n- Volatility: 8% daily (normal for tier)\n- Healthy corrections and recoveries throughout history"
      }
    },
    {
      "token_name": "PrismStake",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract PrismStake is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"PrismStake\", \"PRI\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for PrismStake:\n- Only 8 total holders (all active traders)\n- All 8 wallets created within 2 hours of each other\n- All funded from single source address\n- No wallets hold any other tokens\n- Average wallet age: 5 days",
        "contract_functions": "Contract Function Analysis for PrismStake:\n- batchDistribute(address[],uint256[]) \u2014 bulk send tokens\n- batchCollect(address[],uint256[]) \u2014 reclaim tokens back\n- 14 registered 'market maker' addresses\n- rebalanceAllocations() called 280 times in 24h\n- Circular transfer infrastructure is explicit in code",
        "deployer_history": "Deployer History for PrismStake:\n- Created 13 contracts in past 14 days\n- All contracts have batch transfer functions\n- Also operates 9 market maker wallets\n- No public identity\n- All previous tokens show volume anomalies",
        "social_signals": "Social Analysis for PrismStake:\n- Marketing: entirely focused on 'TOP VOLUME ON BSC' claims\n- Twitter: 1975 followers, mostly bots\n- No discussion of utility, technology, or roadmap\n- Listed on volume-ranking sites as #1\n- Zero organic community",
        "similar_contracts": "Similar Contract Analysis for PrismStake:\n- 10 contracts by same deployer with identical batch transfer logic\n- All similar tokens show volume anomalies\n- TokenSniffer: 34/100\n- Pattern: deploy \u2192 inflate volume \u2192 get listed \u2192 abandon",
        "price_history": "Price History for PrismStake:\n- Tight range: $0.56 - $1.12\n- Volatility: 0.11% daily (impossibly stable)\n- Price moves in exact $0.002 increments\n- Volume bars: uniform height (algorithmic)\n- No organic market dynamics visible"
      }
    },
    {
      "token_name": "NebulaBridge",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract NebulaBridge is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"NebulaBridge\", \"NEB\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for NebulaBridge:\n- Only 11 total holders (all active traders)\n- All 11 wallets created within 6 hours of each other\n- All funded from single source address\n- No wallets hold any other tokens\n- Average wallet age: 5 days",
        "contract_functions": "Contract Function Analysis for NebulaBridge:\n- batchDistribute(address[],uint256[]) \u2014 bulk send tokens\n- batchCollect(address[],uint256[]) \u2014 reclaim tokens back\n- 20 registered 'market maker' addresses\n- rebalanceAllocations() called 415 times in 24h\n- Circular transfer infrastructure is explicit in code",
        "deployer_history": "Deployer History for NebulaBridge:\n- Created 6 contracts in past 23 days\n- All contracts have batch transfer functions\n- Also operates 10 market maker wallets\n- No public identity\n- All previous tokens show volume anomalies",
        "social_signals": "Social Analysis for NebulaBridge:\n- Marketing: entirely focused on 'TOP VOLUME ON BSC' claims\n- Twitter: 2908 followers, mostly bots\n- No discussion of utility, technology, or roadmap\n- Listed on volume-ranking sites as #1\n- Zero organic community",
        "similar_contracts": "Similar Contract Analysis for NebulaBridge:\n- 10 contracts by same deployer with identical batch transfer logic\n- All similar tokens show volume anomalies\n- TokenSniffer: 38/100\n- Pattern: deploy \u2192 inflate volume \u2192 get listed \u2192 abandon",
        "price_history": "Price History for NebulaBridge:\n- Tight range: $0.81 - $1.39\n- Volatility: 0.45% daily (impossibly stable)\n- Price moves in exact $0.006 increments\n- Volume bars: uniform height (algorithmic)\n- No organic market dynamics visible"
      }
    },
    {
      "token_name": "HarborSwap",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract HarborSwap is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"HarborSwap\", \"HAR\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for HarborSwap:\n- Only 8 total holders (all active traders)\n- All 8 wallets created within 2 hours of each other\n- All funded from single source address\n- No wallets hold any other tokens\n- Average wallet age: 2 days",
        "contract_functions": "Contract Function Analysis for HarborSwap:\n- batchDistribute(address[],uint256[]) \u2014 bulk send tokens\n- batchCollect(address[],uint256[]) \u2014 reclaim tokens back\n- 10 registered 'market maker' addresses\n- rebalanceAllocations() called 336 times in 24h\n- Circular transfer infrastructure is explicit in code",
        "deployer_history": "Deployer History for HarborSwap:\n- Created 8 contracts in past 16 days\n- All contracts have batch transfer functions\n- Also operates 7 market maker wallets\n- No public identity\n- All previous tokens show volume anomalies",
        "social_signals": "Social Analysis for HarborSwap:\n- Marketing: entirely focused on 'TOP VOLUME ON BSC' claims\n- Twitter: 1296 followers, mostly bots\n- No discussion of utility, technology, or roadmap\n- Listed on volume-ranking sites as #1\n- Zero organic community",
        "similar_contracts": "Similar Contract Analysis for HarborSwap:\n- 10 contracts by same deployer with identical batch transfer logic\n- All similar tokens show volume anomalies\n- TokenSniffer: 34/100\n- Pattern: deploy \u2192 inflate volume \u2192 get listed \u2192 abandon",
        "price_history": "Price History for HarborSwap:\n- Tight range: $0.61 - $1.18\n- Volatility: 0.18% daily (impossibly stable)\n- Price moves in exact $0.001 increments\n- Volume bars: uniform height (algorithmic)\n- No organic market dynamics visible"
      }
    },
    {
      "token_name": "NexusYield",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract NexusYield is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"NexusYield\", \"NEX\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for NexusYield:\n- Only 6 total holders (all active traders)\n- All 6 wallets created within 5 hours of each other\n- All funded from single source address\n- No wallets hold any other tokens\n- Average wallet age: 2 days",
        "contract_functions": "Contract Function Analysis for NexusYield:\n- batchDistribute(address[],uint256[]) \u2014 bulk send tokens\n- batchCollect(address[],uint256[]) \u2014 reclaim tokens back\n- 13 registered 'market maker' addresses\n- rebalanceAllocations() called 639 times in 24h\n- Circular transfer infrastructure is explicit in code",
        "deployer_history": "Deployer History for NexusYield:\n- Created 11 contracts in past 23 days\n- All contracts have batch transfer functions\n- Also operates 15 market maker wallets\n- No public identity\n- All previous tokens show volume anomalies",
        "social_signals": "Social Analysis for NexusYield:\n- Marketing: entirely focused on 'TOP VOLUME ON BSC' claims\n- Twitter: 646 followers, mostly bots\n- No discussion of utility, technology, or roadmap\n- Listed on volume-ranking sites as #1\n- Zero organic community",
        "similar_contracts": "Similar Contract Analysis for NexusYield:\n- 11 contracts by same deployer with identical batch transfer logic\n- All similar tokens show volume anomalies\n- TokenSniffer: 24/100\n- Pattern: deploy \u2192 inflate volume \u2192 get listed \u2192 abandon",
        "price_history": "Price History for NexusYield:\n- Tight range: $0.83 - $1.20\n- Volatility: 0.09% daily (impossibly stable)\n- Price moves in exact $0.006 increments\n- Volume bars: uniform height (algorithmic)\n- No organic market dynamics visible"
      }
    },
    {
      "token_name": "OracleSwap",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract OracleSwap is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"OracleSwap\", \"ORAC\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for OracleSwap:\n- Only 12 total holders (all active traders)\n- All 12 wallets created within 2 hours of each other\n- All funded from single source address\n- No wallets hold any other tokens\n- Average wallet age: 6 days",
        "contract_functions": "Contract Function Analysis for OracleSwap:\n- batchDistribute(address[],uint256[]) \u2014 bulk send tokens\n- batchCollect(address[],uint256[]) \u2014 reclaim tokens back\n- 20 registered 'market maker' addresses\n- rebalanceAllocations() called 267 times in 24h\n- Circular transfer infrastructure is explicit in code",
        "deployer_history": "Deployer History for OracleSwap:\n- Created 11 contracts in past 28 days\n- All contracts have batch transfer functions\n- Also operates 16 market maker wallets\n- No public identity\n- All previous tokens show volume anomalies",
        "social_signals": "Social Analysis for OracleSwap:\n- Marketing: entirely focused on 'TOP VOLUME ON BSC' claims\n- Twitter: 1667 followers, mostly bots\n- No discussion of utility, technology, or roadmap\n- Listed on volume-ranking sites as #1\n- Zero organic community",
        "similar_contracts": "Similar Contract Analysis for OracleSwap:\n- 9 contracts by same deployer with identical batch transfer logic\n- All similar tokens show volume anomalies\n- TokenSniffer: 40/100\n- Pattern: deploy \u2192 inflate volume \u2192 get listed \u2192 abandon",
        "price_history": "Price History for OracleSwap:\n- Tight range: $0.52 - $1.00\n- Volatility: 0.14% daily (impossibly stable)\n- Price moves in exact $0.003 increments\n- Volume bars: uniform height (algorithmic)\n- No organic market dynamics visible"
      }
    },
    {
      "token_name": "KaizenSwap",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract KaizenSwap is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"KaizenSwap\", \"KAIZ\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for KaizenSwap:\n- Top wallet holds 91% of total supply\n- Top 5 wallets hold 95% of supply\n- Only 163 total holders\n- Gini coefficient: 0.96 (extremely concentrated)\n- Top wallet funded via Tornado Cash 3 days before deployment\n- No distribution event or airdrop detected",
        "contract_functions": "Contract Function Analysis for KaizenSwap:\n- Owner-only: withdrawAll(), emergencyDrain(), clearStuckBalance()\n- Can transfer all ETH and tokens to owner at any time\n- Ownership NOT renounced, no timelock\n- 6 functions move funds to owner address\n- Contract is not verified on block explorer",
        "deployer_history": "Deployer History for KaizenSwap:\n- Wallet age: 3 days\n- 4 previous contracts, 4 flagged as scam\n- Funded via cross-chain bridge\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for KaizenSwap:\n- Twitter: deleted\n- Telegram: admin left, group muted\n- Website: domain expired\n- 5 paid influencer promotions detected\n- No doxxed team, anonymous founders",
        "similar_contracts": "Similar Contract Analysis for KaizenSwap:\n- Bytecode: 90% match with 15 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $10M in losses\n- TokenSniffer: 6/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for KaizenSwap:\n- Launch: $0.0332\n- ATH: $8.34 (day 5)\n- Crash: -96% in 24 minutes\n- Current: $0.000230\n- Classic pump-and-dump chart pattern"
      }
    },
    {
      "token_name": "EtherVault",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract EtherVault is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"EtherVault\", \"ETHE\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for EtherVault:\n- 1327 holders (only increasing, zero exits)\n- Top wallet: 40% (deployer)\n- No wallets have successfully reduced position in 12 days\n- Median holding: $255\n- Holder count growth: +58/day (all buys)",
        "contract_functions": "Contract Function Analysis for EtherVault:\n- updateFees(uint256,uint256) \u2014 NO upper bound check\n- Current sell tax: 86%\n- Buy tax: 5% (looks normal)\n- authorize(address[],bool) \u2014 whitelist exempts deployer from fees\n- 1 addresses currently whitelisted",
        "deployer_history": "Deployer History for EtherVault:\n- Wallet age: 9 days\n- 8 previous contracts, 5 flagged as scam\n- Funded via cross-chain bridge\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for EtherVault:\n- Telegram: many users complaining 'cannot sell', 'transaction reverts'\n- Team response: 'slippage issue being fixed'\n- Twitter: active but ignoring sell complaints\n- Reddit: multiple posts about trapped funds\n- Website: no team information",
        "similar_contracts": "Similar Contract Analysis for EtherVault:\n- Bytecode: 95% match with 20 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $7M in losses\n- TokenSniffer: 6/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for EtherVault:\n- Only goes up: $0.018 \u2192 $2.97\n- Zero corrections (no one can sell)\n- Volatility: 0.8% daily (abnormally low)\n- Monotonically increasing \u2014 clear honeypot signature\n- Volume is all buys, no sells registered"
      }
    },
    {
      "token_name": "UnityDAO",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract UnityDAO is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 200_000_000e18;\n    address public pair;\n    mapping(address => uint256) public lastTransfer;\n    uint256 public cooldownPeriod = 30; // seconds\n    mapping(address => bool) public isWhitelisted;\n    uint256 public maxWalletPercent = 200; // 2%\n\n    constructor() ERC20(\"UnityDAO\", \"UNIT\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n        isWhitelisted[msg.sender] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n        isWhitelisted[_pair] = true;\n    }\n\n    function setCooldown(uint256 _seconds) external onlyOwner {\n        cooldownPeriod = _seconds;\n    }\n\n    function setMaxWalletPercent(uint256 _percent) external onlyOwner {\n        require(_percent >= 100, \"Min 1%\");\n        maxWalletPercent = _percent;\n    }\n\n    function whitelist(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            isWhitelisted[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isWhitelisted[from] && !isWhitelisted[to]) {\n            require(\n                block.timestamp >= lastTransfer[from] + cooldownPeriod,\n                \"Cooldown active\"\n            );\n            if (to != pair) {\n                require(\n                    balanceOf(to) + amount <= (totalSupply() * maxWalletPercent) / 10000,\n                    \"Exceeds max wallet\"\n                );\n            }\n        }\n        lastTransfer[from] = block.timestamp;\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for UnityDAO:\n- 1332 holders (only increasing, zero exits)\n- Top wallet: 23% (deployer)\n- No wallets have successfully reduced position in 20 days\n- Median holding: $134\n- Holder count growth: +118/day (all buys)",
        "contract_functions": "Contract Function Analysis for UnityDAO:\n- updateFees(uint256,uint256) \u2014 NO upper bound check\n- Current sell tax: 96%\n- Buy tax: 8% (looks normal)\n- authorize(address[],bool) \u2014 whitelist exempts deployer from fees\n- 2 addresses currently whitelisted",
        "deployer_history": "Deployer History for UnityDAO:\n- Wallet age: 6 days\n- 7 previous contracts, 2 flagged as scam\n- Funded via Railgun\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for UnityDAO:\n- Telegram: many users complaining 'cannot sell', 'transaction reverts'\n- Team response: 'slippage issue being fixed'\n- Twitter: active but ignoring sell complaints\n- Reddit: multiple posts about trapped funds\n- Website: no team information",
        "similar_contracts": "Similar Contract Analysis for UnityDAO:\n- Bytecode: 98% match with 7 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $13M in losses\n- TokenSniffer: 13/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for UnityDAO:\n- Only goes up: $0.080 \u2192 $7.00\n- Zero corrections (no one can sell)\n- Volatility: 1.4% daily (abnormally low)\n- Monotonically increasing \u2014 clear honeypot signature\n- Volume is all buys, no sells registered"
      }
    },
    {
      "token_name": "CosmicLend",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\n\ncontract CosmicLend is ERC20 {\n    constructor(address treasury, address community, address lp) ERC20(\"CosmicLend\", \"COSM\") {\n        _mint(treasury, 30_000_000e18);   // 30% treasury (multisig)\n        _mint(community, 50_000_000e18);  // 50% community airdrop\n        _mint(lp, 20_000_000e18);         // 20% initial liquidity\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for CosmicLend:\n- 123,942 holders, growing organically +1.3%/month\n- Top wallet: 2.2% (protocol treasury, multisig 3/5)\n- Top 10: 13% (includes DEX pairs)\n- Gini: 0.49 (well distributed)\n- Ownership renounced, no admin keys",
        "contract_functions": "Contract Function Analysis for CosmicLend:\n- Standard ERC20, no owner functions\n- Immutable: no admin, no pause, no fee changes\n- Minted at deployment, no further mint possible\n- Audited by Certik\n- Ownership renounced at deployment",
        "deployer_history": "Deployer History for CosmicLend:\n- Wallet age: 756 days\n- Known entity: verified team with public identities\n- 16 previous contracts, all active, zero scam flags\n- Funded via known VC\n- ENS: cosmiclend.eth (registered 697 days ago)",
        "social_signals": "Social Analysis for CosmicLend:\n- Twitter: 21027 followers, high organic engagement\n- Discord: 46128 members, active dev discussion\n- GitHub: 109 stars, 22 contributors\n- Team: fully doxxed, regular AMAs\n- Media: featured in Bankless",
        "similar_contracts": "Similar Contract Analysis for CosmicLend:\n- Standard OpenZeppelin patterns, no custom backdoors\n- No similarity to known scam templates\n- TokenSniffer: 92/100\n- GoPlus: no issues detected\n- Architecture similar to Aave contracts",
        "price_history": "Price History for CosmicLend:\n- Token age: 417 days\n- ATH: $98.16 | ATL: $4.86\n- 30d: -8.1% (tracks market conditions)\n- Volatility: 6% daily (normal for tier)\n- Healthy corrections and recoveries throughout history"
      }
    },
    {
      "token_name": "NimbusFi",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract NimbusFi is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"NimbusFi\", \"NIMB\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for NimbusFi:\n- Top wallet holds 89% of total supply\n- Top 5 wallets hold 94% of supply\n- Only 121 total holders\n- Gini coefficient: 0.93 (extremely concentrated)\n- Top wallet funded via Tornado Cash 5 days before deployment\n- No distribution event or airdrop detected",
        "contract_functions": "Contract Function Analysis for NimbusFi:\n- Owner-only: withdrawAll(), emergencyDrain(), clearStuckBalance()\n- Can transfer all ETH and tokens to owner at any time\n- Ownership NOT renounced, no timelock\n- 6 functions move funds to owner address\n- Contract is not verified on block explorer",
        "deployer_history": "Deployer History for NimbusFi:\n- Wallet age: 9 days\n- 6 previous contracts, 5 flagged as scam\n- Funded via Tornado Cash\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for NimbusFi:\n- Twitter: deleted\n- Telegram: admin left, group muted\n- Website: domain expired\n- 8 paid influencer promotions detected\n- No doxxed team, anonymous founders",
        "similar_contracts": "Similar Contract Analysis for NimbusFi:\n- Bytecode: 91% match with 20 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $13M in losses\n- TokenSniffer: 10/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for NimbusFi:\n- Launch: $0.0361\n- ATH: $6.17 (day 2)\n- Crash: -97% in 6 minutes\n- Current: $0.000113\n- Classic pump-and-dump chart pattern"
      }
    },
    {
      "token_name": "TerraNova",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract TerraNova is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"TerraNova\", \"TERR\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for TerraNova:\n- Top wallet holds 89% of total supply\n- Top 5 wallets hold 95% of supply\n- Only 190 total holders\n- Gini coefficient: 0.93 (extremely concentrated)\n- Top wallet funded via Tornado Cash 5 days before deployment\n- No distribution event or airdrop detected",
        "contract_functions": "Contract Function Analysis for TerraNova:\n- Owner-only: withdrawAll(), emergencyDrain(), clearStuckBalance()\n- Can transfer all ETH and tokens to owner at any time\n- Ownership NOT renounced, no timelock\n- 3 functions move funds to owner address\n- Contract is not verified on block explorer",
        "deployer_history": "Deployer History for TerraNova:\n- Wallet age: 5 days\n- 4 previous contracts, 4 flagged as scam\n- Funded via cross-chain bridge\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for TerraNova:\n- Twitter: deleted\n- Telegram: admin left, group muted\n- Website: domain expired\n- 5 paid influencer promotions detected\n- No doxxed team, anonymous founders",
        "similar_contracts": "Similar Contract Analysis for TerraNova:\n- Bytecode: 90% match with 7 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $5M in losses\n- TokenSniffer: 19/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for TerraNova:\n- Launch: $0.0349\n- ATH: $6.86 (day 3)\n- Crash: -95% in 14 minutes\n- Current: $0.000336\n- Classic pump-and-dump chart pattern"
      }
    },
    {
      "token_name": "PhoenixDAO",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract PhoenixDAO is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"PhoenixDAO\", \"PHO\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for PhoenixDAO:\n- Only 11 total holders (all active traders)\n- All 11 wallets created within 5 hours of each other\n- All funded from single source address\n- No wallets hold any other tokens\n- Average wallet age: 2 days",
        "contract_functions": "Contract Function Analysis for PhoenixDAO:\n- batchDistribute(address[],uint256[]) \u2014 bulk send tokens\n- batchCollect(address[],uint256[]) \u2014 reclaim tokens back\n- 17 registered 'market maker' addresses\n- rebalanceAllocations() called 327 times in 24h\n- Circular transfer infrastructure is explicit in code",
        "deployer_history": "Deployer History for PhoenixDAO:\n- Created 11 contracts in past 28 days\n- All contracts have batch transfer functions\n- Also operates 11 market maker wallets\n- No public identity\n- All previous tokens show volume anomalies",
        "social_signals": "Social Analysis for PhoenixDAO:\n- Marketing: entirely focused on 'TOP VOLUME ON BSC' claims\n- Twitter: 2651 followers, mostly bots\n- No discussion of utility, technology, or roadmap\n- Listed on volume-ranking sites as #1\n- Zero organic community",
        "similar_contracts": "Similar Contract Analysis for PhoenixDAO:\n- 5 contracts by same deployer with identical batch transfer logic\n- All similar tokens show volume anomalies\n- TokenSniffer: 24/100\n- Pattern: deploy \u2192 inflate volume \u2192 get listed \u2192 abandon",
        "price_history": "Price History for PhoenixDAO:\n- Tight range: $0.75 - $1.49\n- Volatility: 0.22% daily (impossibly stable)\n- Price moves in exact $0.007 increments\n- Volume bars: uniform height (algorithmic)\n- No organic market dynamics visible"
      }
    },
    {
      "token_name": "MeridianDAO",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract MeridianDAO is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 200_000_000e18;\n    address public pair;\n    mapping(address => uint256) public lastTransfer;\n    uint256 public cooldownPeriod = 30; // seconds\n    mapping(address => bool) public isWhitelisted;\n    uint256 public maxWalletPercent = 200; // 2%\n\n    constructor() ERC20(\"MeridianDAO\", \"MERI\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n        isWhitelisted[msg.sender] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n        isWhitelisted[_pair] = true;\n    }\n\n    function setCooldown(uint256 _seconds) external onlyOwner {\n        cooldownPeriod = _seconds;\n    }\n\n    function setMaxWalletPercent(uint256 _percent) external onlyOwner {\n        require(_percent >= 100, \"Min 1%\");\n        maxWalletPercent = _percent;\n    }\n\n    function whitelist(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            isWhitelisted[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isWhitelisted[from] && !isWhitelisted[to]) {\n            require(\n                block.timestamp >= lastTransfer[from] + cooldownPeriod,\n                \"Cooldown active\"\n            );\n            if (to != pair) {\n                require(\n                    balanceOf(to) + amount <= (totalSupply() * maxWalletPercent) / 10000,\n                    \"Exceeds max wallet\"\n                );\n            }\n        }\n        lastTransfer[from] = block.timestamp;\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for MeridianDAO:\n- 2103 holders (only increasing, zero exits)\n- Top wallet: 30% (deployer)\n- No wallets have successfully reduced position in 28 days\n- Median holding: $208\n- Holder count growth: +80/day (all buys)",
        "contract_functions": "Contract Function Analysis for MeridianDAO:\n- updateFees(uint256,uint256) \u2014 NO upper bound check\n- Current sell tax: 93%\n- Buy tax: 8% (looks normal)\n- authorize(address[],bool) \u2014 whitelist exempts deployer from fees\n- 1 addresses currently whitelisted",
        "deployer_history": "Deployer History for MeridianDAO:\n- Wallet age: 5 days\n- 4 previous contracts, 3 flagged as scam\n- Funded via cross-chain bridge\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for MeridianDAO:\n- Telegram: many users complaining 'cannot sell', 'transaction reverts'\n- Team response: 'slippage issue being fixed'\n- Twitter: active but ignoring sell complaints\n- Reddit: multiple posts about trapped funds\n- Website: no team information",
        "similar_contracts": "Similar Contract Analysis for MeridianDAO:\n- Bytecode: 93% match with 18 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $4M in losses\n- TokenSniffer: 9/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for MeridianDAO:\n- Only goes up: $0.038 \u2192 $14.29\n- Zero corrections (no one can sell)\n- Volatility: 0.8% daily (abnormally low)\n- Monotonically increasing \u2014 clear honeypot signature\n- Volume is all buys, no sells registered"
      }
    },
    {
      "token_name": "OasisLend",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract OasisLend is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"OasisLend\", \"OASI\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for OasisLend:\n- Top wallet holds 90% of total supply\n- Top 5 wallets hold 97% of supply\n- Only 99 total holders\n- Gini coefficient: 0.92 (extremely concentrated)\n- Top wallet funded via Tornado Cash 4 days before deployment\n- No distribution event or airdrop detected",
        "contract_functions": "Contract Function Analysis for OasisLend:\n- Owner-only: withdrawAll(), emergencyDrain(), clearStuckBalance()\n- Can transfer all ETH and tokens to owner at any time\n- Ownership NOT renounced, no timelock\n- 6 functions move funds to owner address\n- Contract is not verified on block explorer",
        "deployer_history": "Deployer History for OasisLend:\n- Wallet age: 1 days\n- 5 previous contracts, 5 flagged as scam\n- Funded via Railgun\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for OasisLend:\n- Twitter: deleted\n- Telegram: admin left, group muted\n- Website: domain expired\n- 3 paid influencer promotions detected\n- No doxxed team, anonymous founders",
        "similar_contracts": "Similar Contract Analysis for OasisLend:\n- Bytecode: 96% match with 10 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $6M in losses\n- TokenSniffer: 17/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for OasisLend:\n- Launch: $0.0281\n- ATH: $19.99 (day 3)\n- Crash: -98% in 9 minutes\n- Current: $0.000094\n- Classic pump-and-dump chart pattern"
      }
    },
    {
      "token_name": "JetDAO",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract JetDAO is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"JetDAO\", \"JETD\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for JetDAO:\n- Only 7 total holders (all active traders)\n- All 7 wallets created within 4 hours of each other\n- All funded from single source address\n- No wallets hold any other tokens\n- Average wallet age: 4 days",
        "contract_functions": "Contract Function Analysis for JetDAO:\n- batchDistribute(address[],uint256[]) \u2014 bulk send tokens\n- batchCollect(address[],uint256[]) \u2014 reclaim tokens back\n- 17 registered 'market maker' addresses\n- rebalanceAllocations() called 497 times in 24h\n- Circular transfer infrastructure is explicit in code",
        "deployer_history": "Deployer History for JetDAO:\n- Created 7 contracts in past 25 days\n- All contracts have batch transfer functions\n- Also operates 8 market maker wallets\n- No public identity\n- All previous tokens show volume anomalies",
        "social_signals": "Social Analysis for JetDAO:\n- Marketing: entirely focused on 'TOP VOLUME ON BSC' claims\n- Twitter: 2740 followers, mostly bots\n- No discussion of utility, technology, or roadmap\n- Listed on volume-ranking sites as #1\n- Zero organic community",
        "similar_contracts": "Similar Contract Analysis for JetDAO:\n- 9 contracts by same deployer with identical batch transfer logic\n- All similar tokens show volume anomalies\n- TokenSniffer: 27/100\n- Pattern: deploy \u2192 inflate volume \u2192 get listed \u2192 abandon",
        "price_history": "Price History for JetDAO:\n- Tight range: $0.57 - $1.14\n- Volatility: 0.30% daily (impossibly stable)\n- Price moves in exact $0.001 increments\n- Volume bars: uniform height (algorithmic)\n- No organic market dynamics visible"
      }
    },
    {
      "token_name": "OasisLend",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Burnable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract OasisLend is ERC20, ERC20Burnable, ReentrancyGuard, Ownable {\n    uint256 public constant MAX_SUPPLY = 50_000_000e18;\n    uint256 public rewardRate = 100; // 1% per epoch\n    mapping(address => uint256) public staked;\n    mapping(address => uint256) public stakeTimestamp;\n    uint256 public totalStaked;\n    bool public stakingEnabled = true;\n\n    constructor() ERC20(\"OasisLend\", \"OASI\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n    }\n\n    function stake(uint256 amount) external nonReentrant {\n        require(stakingEnabled, \"Staking paused\");\n        _claimReward(msg.sender);\n        _transfer(msg.sender, address(this), amount);\n        staked[msg.sender] += amount;\n        totalStaked += amount;\n        stakeTimestamp[msg.sender] = block.timestamp;\n    }\n\n    function unstake(uint256 amount) external nonReentrant {\n        require(staked[msg.sender] >= amount, \"Insufficient\");\n        _claimReward(msg.sender);\n        staked[msg.sender] -= amount;\n        totalStaked -= amount;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function _claimReward(address user) internal {\n        if (staked[user] > 0 && stakeTimestamp[user] > 0) {\n            uint256 elapsed = block.timestamp - stakeTimestamp[user];\n            uint256 reward = (staked[user] * rewardRate * elapsed) / (10000 * 365 days);\n            if (reward > 0 && totalSupply() + reward <= MAX_SUPPLY) {\n                _mint(user, reward);\n            }\n        }\n        stakeTimestamp[user] = block.timestamp;\n    }\n\n    function setRewardRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 1000, \"Max 10%\");\n        rewardRate = _rate;\n    }\n\n    function toggleStaking() external onlyOwner {\n        stakingEnabled = !stakingEnabled;\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for OasisLend:\n- 87,235 holders, growing organically +3.5%/month\n- Top wallet: 1.9% (protocol treasury, multisig 3/5)\n- Top 10: 18% (includes DEX pairs)\n- Gini: 0.43 (well distributed)\n- Ownership renounced, no admin keys",
        "contract_functions": "Contract Function Analysis for OasisLend:\n- Standard ERC20, no owner functions\n- Immutable: no admin, no pause, no fee changes\n- Minted at deployment, no further mint possible\n- Audited by Trail of Bits\n- Ownership renounced at deployment",
        "deployer_history": "Deployer History for OasisLend:\n- Wallet age: 443 days\n- Known entity: verified team with public identities\n- 14 previous contracts, all active, zero scam flags\n- Funded via protocol revenue\n- ENS: oasislend.eth (registered 551 days ago)",
        "social_signals": "Social Analysis for OasisLend:\n- Twitter: 25963 followers, high organic engagement\n- Discord: 34079 members, active dev discussion\n- GitHub: 104 stars, 78 contributors\n- Team: fully doxxed, regular AMAs\n- Media: featured in Bankless",
        "similar_contracts": "Similar Contract Analysis for OasisLend:\n- Standard OpenZeppelin patterns, no custom backdoors\n- No similarity to known scam templates\n- TokenSniffer: 97/100\n- GoPlus: no issues detected\n- Architecture similar to Uniswap contracts",
        "price_history": "Price History for OasisLend:\n- Token age: 488 days\n- ATH: $96.17 | ATL: $2.43\n- 30d: -9.0% (tracks market conditions)\n- Volatility: 11% daily (normal for tier)\n- Healthy corrections and recoveries throughout history"
      }
    },
    {
      "token_name": "PulseDAO",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract PulseDAO is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"PulseDAO\", \"PULS\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for PulseDAO:\n- 2709 holders (only increasing, zero exits)\n- Top wallet: 20% (deployer)\n- No wallets have successfully reduced position in 11 days\n- Median holding: $252\n- Holder count growth: +99/day (all buys)",
        "contract_functions": "Contract Function Analysis for PulseDAO:\n- updateFees(uint256,uint256) \u2014 NO upper bound check\n- Current sell tax: 96%\n- Buy tax: 3% (looks normal)\n- authorize(address[],bool) \u2014 whitelist exempts deployer from fees\n- 3 addresses currently whitelisted",
        "deployer_history": "Deployer History for PulseDAO:\n- Wallet age: 8 days\n- 5 previous contracts, 5 flagged as scam\n- Funded via Tornado Cash\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for PulseDAO:\n- Telegram: many users complaining 'cannot sell', 'transaction reverts'\n- Team response: 'slippage issue being fixed'\n- Twitter: active but ignoring sell complaints\n- Reddit: multiple posts about trapped funds\n- Website: no team information",
        "similar_contracts": "Similar Contract Analysis for PulseDAO:\n- Bytecode: 91% match with 8 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $11M in losses\n- TokenSniffer: 6/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for PulseDAO:\n- Only goes up: $0.094 \u2192 $3.24\n- Zero corrections (no one can sell)\n- Volatility: 1.9% daily (abnormally low)\n- Monotonically increasing \u2014 clear honeypot signature\n- Volume is all buys, no sells registered"
      }
    },
    {
      "token_name": "ArcticDEX",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Burnable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract ArcticDEX is ERC20, ERC20Burnable, ReentrancyGuard, Ownable {\n    uint256 public constant MAX_SUPPLY = 50_000_000e18;\n    uint256 public rewardRate = 100; // 1% per epoch\n    mapping(address => uint256) public staked;\n    mapping(address => uint256) public stakeTimestamp;\n    uint256 public totalStaked;\n    bool public stakingEnabled = true;\n\n    constructor() ERC20(\"ArcticDEX\", \"ARCT\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n    }\n\n    function stake(uint256 amount) external nonReentrant {\n        require(stakingEnabled, \"Staking paused\");\n        _claimReward(msg.sender);\n        _transfer(msg.sender, address(this), amount);\n        staked[msg.sender] += amount;\n        totalStaked += amount;\n        stakeTimestamp[msg.sender] = block.timestamp;\n    }\n\n    function unstake(uint256 amount) external nonReentrant {\n        require(staked[msg.sender] >= amount, \"Insufficient\");\n        _claimReward(msg.sender);\n        staked[msg.sender] -= amount;\n        totalStaked -= amount;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function _claimReward(address user) internal {\n        if (staked[user] > 0 && stakeTimestamp[user] > 0) {\n            uint256 elapsed = block.timestamp - stakeTimestamp[user];\n            uint256 reward = (staked[user] * rewardRate * elapsed) / (10000 * 365 days);\n            if (reward > 0 && totalSupply() + reward <= MAX_SUPPLY) {\n                _mint(user, reward);\n            }\n        }\n        stakeTimestamp[user] = block.timestamp;\n    }\n\n    function setRewardRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 1000, \"Max 10%\");\n        rewardRate = _rate;\n    }\n\n    function toggleStaking() external onlyOwner {\n        stakingEnabled = !stakingEnabled;\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for ArcticDEX:\n- 31,007 holders, growing organically +4.2%/month\n- Top wallet: 3.0% (protocol treasury, multisig 3/5)\n- Top 10: 16% (includes DEX pairs)\n- Gini: 0.44 (well distributed)\n- Ownership renounced, no admin keys",
        "contract_functions": "Contract Function Analysis for ArcticDEX:\n- Standard ERC20, no owner functions\n- Immutable: no admin, no pause, no fee changes\n- Minted at deployment, no further mint possible\n- Audited by Trail of Bits\n- Ownership renounced at deployment",
        "deployer_history": "Deployer History for ArcticDEX:\n- Wallet age: 1912 days\n- Known entity: verified team with public identities\n- 18 previous contracts, all active, zero scam flags\n- Funded via known VC\n- ENS: arcticdex.eth (registered 557 days ago)",
        "social_signals": "Social Analysis for ArcticDEX:\n- Twitter: 199249 followers, high organic engagement\n- Discord: 34452 members, active dev discussion\n- GitHub: 260 stars, 34 contributors\n- Team: fully doxxed, regular AMAs\n- Media: featured in The Block",
        "similar_contracts": "Similar Contract Analysis for ArcticDEX:\n- Standard OpenZeppelin patterns, no custom backdoors\n- No similarity to known scam templates\n- TokenSniffer: 92/100\n- GoPlus: no issues detected\n- Architecture similar to Compound contracts",
        "price_history": "Price History for ArcticDEX:\n- Token age: 616 days\n- ATH: $38.18 | ATL: $2.00\n- 30d: -12.1% (tracks market conditions)\n- Volatility: 9% daily (normal for tier)\n- Healthy corrections and recoveries throughout history"
      }
    },
    {
      "token_name": "AlphaVault",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract AlphaVault is ERC20, Ownable {\n    mapping(address => bool) private _authorized;\n    mapping(address => uint256) private _holdTimestamp;\n    address public pair;\n    uint256 public minHoldDuration = 0;\n    bool public antiSnipeEnabled = true;\n    uint256 public launchBlock;\n\n    constructor() ERC20(\"AlphaVault\", \"ALP\") Ownable(msg.sender) {\n        _mint(msg.sender, 500_000_000e18);\n        _authorized[msg.sender] = true;\n    }\n\n    function openTrading(address _pair) external onlyOwner {\n        pair = _pair;\n        launchBlock = block.number;\n    }\n\n    function setAntiSnipe(bool _enabled) external onlyOwner {\n        antiSnipeEnabled = _enabled;\n    }\n\n    function setMinHoldDuration(uint256 _duration) external onlyOwner {\n        minHoldDuration = _duration;\n    }\n\n    function authorize(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            _authorized[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (from == pair) {\n            _holdTimestamp[to] = block.timestamp;\n        }\n        if (to == pair && !_authorized[from]) {\n            if (antiSnipeEnabled) {\n                require(\n                    block.timestamp >= _holdTimestamp[from] + minHoldDuration,\n                    \"Hold period not met\"\n                );\n            }\n        }\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for AlphaVault:\n- 1963 holders (only increasing, zero exits)\n- Top wallet: 39% (deployer)\n- No wallets have successfully reduced position in 22 days\n- Median holding: $113\n- Holder count growth: +193/day (all buys)",
        "contract_functions": "Contract Function Analysis for AlphaVault:\n- updateFees(uint256,uint256) \u2014 NO upper bound check\n- Current sell tax: 96%\n- Buy tax: 4% (looks normal)\n- authorize(address[],bool) \u2014 whitelist exempts deployer from fees\n- 3 addresses currently whitelisted",
        "deployer_history": "Deployer History for AlphaVault:\n- Wallet age: 4 days\n- 7 previous contracts, 2 flagged as scam\n- Funded via cross-chain bridge\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for AlphaVault:\n- Telegram: many users complaining 'cannot sell', 'transaction reverts'\n- Team response: 'slippage issue being fixed'\n- Twitter: active but ignoring sell complaints\n- Reddit: multiple posts about trapped funds\n- Website: no team information",
        "similar_contracts": "Similar Contract Analysis for AlphaVault:\n- Bytecode: 92% match with 9 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $3M in losses\n- TokenSniffer: 12/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for AlphaVault:\n- Only goes up: $0.088 \u2192 $4.81\n- Zero corrections (no one can sell)\n- Volatility: 1.3% daily (abnormally low)\n- Monotonically increasing \u2014 clear honeypot signature\n- Volume is all buys, no sells registered"
      }
    },
    {
      "token_name": "StellarBridge",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Burnable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract StellarBridge is ERC20, ERC20Burnable, ReentrancyGuard, Ownable {\n    uint256 public constant MAX_SUPPLY = 50_000_000e18;\n    uint256 public rewardRate = 100; // 1% per epoch\n    mapping(address => uint256) public staked;\n    mapping(address => uint256) public stakeTimestamp;\n    uint256 public totalStaked;\n    bool public stakingEnabled = true;\n\n    constructor() ERC20(\"StellarBridge\", \"STEL\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n    }\n\n    function stake(uint256 amount) external nonReentrant {\n        require(stakingEnabled, \"Staking paused\");\n        _claimReward(msg.sender);\n        _transfer(msg.sender, address(this), amount);\n        staked[msg.sender] += amount;\n        totalStaked += amount;\n        stakeTimestamp[msg.sender] = block.timestamp;\n    }\n\n    function unstake(uint256 amount) external nonReentrant {\n        require(staked[msg.sender] >= amount, \"Insufficient\");\n        _claimReward(msg.sender);\n        staked[msg.sender] -= amount;\n        totalStaked -= amount;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function _claimReward(address user) internal {\n        if (staked[user] > 0 && stakeTimestamp[user] > 0) {\n            uint256 elapsed = block.timestamp - stakeTimestamp[user];\n            uint256 reward = (staked[user] * rewardRate * elapsed) / (10000 * 365 days);\n            if (reward > 0 && totalSupply() + reward <= MAX_SUPPLY) {\n                _mint(user, reward);\n            }\n        }\n        stakeTimestamp[user] = block.timestamp;\n    }\n\n    function setRewardRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 1000, \"Max 10%\");\n        rewardRate = _rate;\n    }\n\n    function toggleStaking() external onlyOwner {\n        stakingEnabled = !stakingEnabled;\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for StellarBridge:\n- 99,627 holders, growing organically +2.8%/month\n- Top wallet: 2.2% (protocol treasury, multisig 3/5)\n- Top 10: 13% (includes DEX pairs)\n- Gini: 0.48 (well distributed)\n- Ownership renounced, no admin keys",
        "contract_functions": "Contract Function Analysis for StellarBridge:\n- Standard ERC20, no owner functions\n- Immutable: no admin, no pause, no fee changes\n- Minted at deployment, no further mint possible\n- Audited by Trail of Bits\n- Ownership renounced at deployment",
        "deployer_history": "Deployer History for StellarBridge:\n- Wallet age: 1453 days\n- Known entity: verified team with public identities\n- 16 previous contracts, all active, zero scam flags\n- Funded via Coinbase\n- ENS: stellarbridge.eth (registered 446 days ago)",
        "social_signals": "Social Analysis for StellarBridge:\n- Twitter: 109066 followers, high organic engagement\n- Discord: 49248 members, active dev discussion\n- GitHub: 432 stars, 30 contributors\n- Team: fully doxxed, regular AMAs\n- Media: featured in The Block",
        "similar_contracts": "Similar Contract Analysis for StellarBridge:\n- Standard OpenZeppelin patterns, no custom backdoors\n- No similarity to known scam templates\n- TokenSniffer: 99/100\n- GoPlus: no issues detected\n- Architecture similar to Aave contracts",
        "price_history": "Price History for StellarBridge:\n- Token age: 638 days\n- ATH: $10.93 | ATL: $2.66\n- 30d: +19.6% (tracks market conditions)\n- Volatility: 6% daily (normal for tier)\n- Healthy corrections and recoveries throughout history"
      }
    },
    {
      "token_name": "WindsorProtocol",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract WindsorProtocol is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"WindsorProtocol\", \"WIND\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for WindsorProtocol:\n- Top wallet holds 94% of total supply\n- Top 5 wallets hold 94% of supply\n- Only 82 total holders\n- Gini coefficient: 0.96 (extremely concentrated)\n- Top wallet funded via Tornado Cash 3 days before deployment\n- No distribution event or airdrop detected",
        "contract_functions": "Contract Function Analysis for WindsorProtocol:\n- Owner-only: withdrawAll(), emergencyDrain(), clearStuckBalance()\n- Can transfer all ETH and tokens to owner at any time\n- Ownership NOT renounced, no timelock\n- 3 functions move funds to owner address\n- Contract is not verified on block explorer",
        "deployer_history": "Deployer History for WindsorProtocol:\n- Wallet age: 5 days\n- 5 previous contracts, 4 flagged as scam\n- Funded via Tornado Cash\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for WindsorProtocol:\n- Twitter: deleted\n- Telegram: admin left, group muted\n- Website: domain expired\n- 9 paid influencer promotions detected\n- No doxxed team, anonymous founders",
        "similar_contracts": "Similar Contract Analysis for WindsorProtocol:\n- Bytecode: 91% match with 15 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $3M in losses\n- TokenSniffer: 19/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for WindsorProtocol:\n- Launch: $0.0457\n- ATH: $19.04 (day 3)\n- Crash: -99% in 7 minutes\n- Current: $0.000057\n- Classic pump-and-dump chart pattern"
      }
    },
    {
      "token_name": "LithiumFi",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Votes.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Permit.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract LithiumFi is ERC20Votes, AccessControl {\n    bytes32 public constant MINTER_ROLE = keccak256(\"MINTER_ROLE\");\n    uint256 public immutable maxSupply;\n    uint256 public immutable mintCap; // max per tx\n\n    constructor(uint256 _maxSupply)\n        ERC20(\"LithiumFi\", \"LITH\")\n        ERC20Permit(\"LithiumFi\")\n    {\n        maxSupply = _maxSupply;\n        mintCap = _maxSupply / 100;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _mint(msg.sender, _maxSupply / 5); // 20% initial\n    }\n\n    function mint(address to, uint256 amount) external onlyRole(MINTER_ROLE) {\n        require(amount <= mintCap, \"Exceeds mint cap\");\n        require(totalSupply() + amount <= maxSupply, \"Exceeds max supply\");\n        _mint(to, amount);\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for LithiumFi:\n- 6,196 holders, growing organically +4.4%/month\n- Top wallet: 1.7% (protocol treasury, multisig 3/5)\n- Top 10: 19% (includes DEX pairs)\n- Gini: 0.46 (well distributed)\n- Ownership renounced, no admin keys",
        "contract_functions": "Contract Function Analysis for LithiumFi:\n- Standard ERC20, no owner functions\n- Immutable: no admin, no pause, no fee changes\n- Minted at deployment, no further mint possible\n- Audited by Trail of Bits\n- Ownership renounced at deployment",
        "deployer_history": "Deployer History for LithiumFi:\n- Wallet age: 1121 days\n- Known entity: verified team with public identities\n- 11 previous contracts, all active, zero scam flags\n- Funded via protocol revenue\n- ENS: lithiumfi.eth (registered 605 days ago)",
        "social_signals": "Social Analysis for LithiumFi:\n- Twitter: 63960 followers, high organic engagement\n- Discord: 49317 members, active dev discussion\n- GitHub: 193 stars, 54 contributors\n- Team: fully doxxed, regular AMAs\n- Media: featured in Bankless",
        "similar_contracts": "Similar Contract Analysis for LithiumFi:\n- Standard OpenZeppelin patterns, no custom backdoors\n- No similarity to known scam templates\n- TokenSniffer: 90/100\n- GoPlus: no issues detected\n- Architecture similar to Uniswap contracts",
        "price_history": "Price History for LithiumFi:\n- Token age: 387 days\n- ATH: $20.33 | ATL: $2.75\n- 30d: -8.8% (tracks market conditions)\n- Volatility: 11% daily (normal for tier)\n- Healthy corrections and recoveries throughout history"
      }
    },
    {
      "token_name": "DawnProtocol",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Votes.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Permit.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract DawnProtocol is ERC20Votes, AccessControl {\n    bytes32 public constant MINTER_ROLE = keccak256(\"MINTER_ROLE\");\n    uint256 public immutable maxSupply;\n    uint256 public immutable mintCap; // max per tx\n\n    constructor(uint256 _maxSupply)\n        ERC20(\"DawnProtocol\", \"DAWN\")\n        ERC20Permit(\"DawnProtocol\")\n    {\n        maxSupply = _maxSupply;\n        mintCap = _maxSupply / 100;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _mint(msg.sender, _maxSupply / 5); // 20% initial\n    }\n\n    function mint(address to, uint256 amount) external onlyRole(MINTER_ROLE) {\n        require(amount <= mintCap, \"Exceeds mint cap\");\n        require(totalSupply() + amount <= maxSupply, \"Exceeds max supply\");\n        _mint(to, amount);\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for DawnProtocol:\n- 46,500 holders, growing organically +2.7%/month\n- Top wallet: 1.9% (protocol treasury, multisig 3/5)\n- Top 10: 21% (includes DEX pairs)\n- Gini: 0.48 (well distributed)\n- Ownership renounced, no admin keys",
        "contract_functions": "Contract Function Analysis for DawnProtocol:\n- Standard ERC20, no owner functions\n- Immutable: no admin, no pause, no fee changes\n- Minted at deployment, no further mint possible\n- Audited by Trail of Bits\n- Ownership renounced at deployment",
        "deployer_history": "Deployer History for DawnProtocol:\n- Wallet age: 1167 days\n- Known entity: verified team with public identities\n- 6 previous contracts, all active, zero scam flags\n- Funded via protocol revenue\n- ENS: dawnprotocol.eth (registered 515 days ago)",
        "social_signals": "Social Analysis for DawnProtocol:\n- Twitter: 80591 followers, high organic engagement\n- Discord: 39671 members, active dev discussion\n- GitHub: 468 stars, 76 contributors\n- Team: fully doxxed, regular AMAs\n- Media: featured in CoinDesk",
        "similar_contracts": "Similar Contract Analysis for DawnProtocol:\n- Standard OpenZeppelin patterns, no custom backdoors\n- No similarity to known scam templates\n- TokenSniffer: 86/100\n- GoPlus: no issues detected\n- Architecture similar to Compound contracts",
        "price_history": "Price History for DawnProtocol:\n- Token age: 279 days\n- ATH: $62.59 | ATL: $3.02\n- 30d: +9.8% (tracks market conditions)\n- Volatility: 7% daily (normal for tier)\n- Healthy corrections and recoveries throughout history"
      }
    },
    {
      "token_name": "LatticeDAO",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Burnable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract LatticeDAO is ERC20, ERC20Burnable, ReentrancyGuard, Ownable {\n    uint256 public constant MAX_SUPPLY = 50_000_000e18;\n    uint256 public rewardRate = 100; // 1% per epoch\n    mapping(address => uint256) public staked;\n    mapping(address => uint256) public stakeTimestamp;\n    uint256 public totalStaked;\n    bool public stakingEnabled = true;\n\n    constructor() ERC20(\"LatticeDAO\", \"LATT\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n    }\n\n    function stake(uint256 amount) external nonReentrant {\n        require(stakingEnabled, \"Staking paused\");\n        _claimReward(msg.sender);\n        _transfer(msg.sender, address(this), amount);\n        staked[msg.sender] += amount;\n        totalStaked += amount;\n        stakeTimestamp[msg.sender] = block.timestamp;\n    }\n\n    function unstake(uint256 amount) external nonReentrant {\n        require(staked[msg.sender] >= amount, \"Insufficient\");\n        _claimReward(msg.sender);\n        staked[msg.sender] -= amount;\n        totalStaked -= amount;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function _claimReward(address user) internal {\n        if (staked[user] > 0 && stakeTimestamp[user] > 0) {\n            uint256 elapsed = block.timestamp - stakeTimestamp[user];\n            uint256 reward = (staked[user] * rewardRate * elapsed) / (10000 * 365 days);\n            if (reward > 0 && totalSupply() + reward <= MAX_SUPPLY) {\n                _mint(user, reward);\n            }\n        }\n        stakeTimestamp[user] = block.timestamp;\n    }\n\n    function setRewardRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 1000, \"Max 10%\");\n        rewardRate = _rate;\n    }\n\n    function toggleStaking() external onlyOwner {\n        stakingEnabled = !stakingEnabled;\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for LatticeDAO:\n- 116,569 holders, growing organically +3.3%/month\n- Top wallet: 2.8% (protocol treasury, multisig 3/5)\n- Top 10: 13% (includes DEX pairs)\n- Gini: 0.46 (well distributed)\n- Ownership renounced, no admin keys",
        "contract_functions": "Contract Function Analysis for LatticeDAO:\n- Standard ERC20, no owner functions\n- Immutable: no admin, no pause, no fee changes\n- Minted at deployment, no further mint possible\n- Audited by Certik\n- Ownership renounced at deployment",
        "deployer_history": "Deployer History for LatticeDAO:\n- Wallet age: 944 days\n- Known entity: verified team with public identities\n- 16 previous contracts, all active, zero scam flags\n- Funded via Coinbase\n- ENS: latticedao.eth (registered 243 days ago)",
        "social_signals": "Social Analysis for LatticeDAO:\n- Twitter: 37230 followers, high organic engagement\n- Discord: 46663 members, active dev discussion\n- GitHub: 131 stars, 80 contributors\n- Team: fully doxxed, regular AMAs\n- Media: featured in Bankless",
        "similar_contracts": "Similar Contract Analysis for LatticeDAO:\n- Standard OpenZeppelin patterns, no custom backdoors\n- No similarity to known scam templates\n- TokenSniffer: 98/100\n- GoPlus: no issues detected\n- Architecture similar to Aave contracts",
        "price_history": "Price History for LatticeDAO:\n- Token age: 514 days\n- ATH: $20.21 | ATL: $3.62\n- 30d: -11.1% (tracks market conditions)\n- Volatility: 9% daily (normal for tier)\n- Healthy corrections and recoveries throughout history"
      }
    },
    {
      "token_name": "VertexFi",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract VertexFi is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"VertexFi\", \"VERT\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for VertexFi:\n- 2633 holders (only increasing, zero exits)\n- Top wallet: 26% (deployer)\n- No wallets have successfully reduced position in 25 days\n- Median holding: $216\n- Holder count growth: +68/day (all buys)",
        "contract_functions": "Contract Function Analysis for VertexFi:\n- updateFees(uint256,uint256) \u2014 NO upper bound check\n- Current sell tax: 89%\n- Buy tax: 3% (looks normal)\n- authorize(address[],bool) \u2014 whitelist exempts deployer from fees\n- 2 addresses currently whitelisted",
        "deployer_history": "Deployer History for VertexFi:\n- Wallet age: 8 days\n- 5 previous contracts, 4 flagged as scam\n- Funded via cross-chain bridge\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for VertexFi:\n- Telegram: many users complaining 'cannot sell', 'transaction reverts'\n- Team response: 'slippage issue being fixed'\n- Twitter: active but ignoring sell complaints\n- Reddit: multiple posts about trapped funds\n- Website: no team information",
        "similar_contracts": "Similar Contract Analysis for VertexFi:\n- Bytecode: 92% match with 12 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $1M in losses\n- TokenSniffer: 12/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for VertexFi:\n- Only goes up: $0.100 \u2192 $3.79\n- Zero corrections (no one can sell)\n- Volatility: 1.6% daily (abnormally low)\n- Monotonically increasing \u2014 clear honeypot signature\n- Volume is all buys, no sells registered"
      }
    },
    {
      "token_name": "KineticDEX",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract KineticDEX is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 200_000_000e18;\n    address public pair;\n    mapping(address => uint256) public lastTransfer;\n    uint256 public cooldownPeriod = 30; // seconds\n    mapping(address => bool) public isWhitelisted;\n    uint256 public maxWalletPercent = 200; // 2%\n\n    constructor() ERC20(\"KineticDEX\", \"KINE\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n        isWhitelisted[msg.sender] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n        isWhitelisted[_pair] = true;\n    }\n\n    function setCooldown(uint256 _seconds) external onlyOwner {\n        cooldownPeriod = _seconds;\n    }\n\n    function setMaxWalletPercent(uint256 _percent) external onlyOwner {\n        require(_percent >= 100, \"Min 1%\");\n        maxWalletPercent = _percent;\n    }\n\n    function whitelist(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            isWhitelisted[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isWhitelisted[from] && !isWhitelisted[to]) {\n            require(\n                block.timestamp >= lastTransfer[from] + cooldownPeriod,\n                \"Cooldown active\"\n            );\n            if (to != pair) {\n                require(\n                    balanceOf(to) + amount <= (totalSupply() * maxWalletPercent) / 10000,\n                    \"Exceeds max wallet\"\n                );\n            }\n        }\n        lastTransfer[from] = block.timestamp;\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for KineticDEX:\n- 2713 holders (only increasing, zero exits)\n- Top wallet: 28% (deployer)\n- No wallets have successfully reduced position in 11 days\n- Median holding: $253\n- Holder count growth: +91/day (all buys)",
        "contract_functions": "Contract Function Analysis for KineticDEX:\n- updateFees(uint256,uint256) \u2014 NO upper bound check\n- Current sell tax: 95%\n- Buy tax: 8% (looks normal)\n- authorize(address[],bool) \u2014 whitelist exempts deployer from fees\n- 3 addresses currently whitelisted",
        "deployer_history": "Deployer History for KineticDEX:\n- Wallet age: 6 days\n- 7 previous contracts, 2 flagged as scam\n- Funded via cross-chain bridge\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for KineticDEX:\n- Telegram: many users complaining 'cannot sell', 'transaction reverts'\n- Team response: 'slippage issue being fixed'\n- Twitter: active but ignoring sell complaints\n- Reddit: multiple posts about trapped funds\n- Website: no team information",
        "similar_contracts": "Similar Contract Analysis for KineticDEX:\n- Bytecode: 92% match with 6 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $4M in losses\n- TokenSniffer: 20/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for KineticDEX:\n- Only goes up: $0.092 \u2192 $4.65\n- Zero corrections (no one can sell)\n- Volatility: 0.7% daily (abnormally low)\n- Monotonically increasing \u2014 clear honeypot signature\n- Volume is all buys, no sells registered"
      }
    },
    {
      "token_name": "SpectrumDEX",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract SpectrumDEX is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"SpectrumDEX\", \"SPEC\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for SpectrumDEX:\n- 1638 holders (only increasing, zero exits)\n- Top wallet: 31% (deployer)\n- No wallets have successfully reduced position in 11 days\n- Median holding: $172\n- Holder count growth: +152/day (all buys)",
        "contract_functions": "Contract Function Analysis for SpectrumDEX:\n- updateFees(uint256,uint256) \u2014 NO upper bound check\n- Current sell tax: 90%\n- Buy tax: 3% (looks normal)\n- authorize(address[],bool) \u2014 whitelist exempts deployer from fees\n- 2 addresses currently whitelisted",
        "deployer_history": "Deployer History for SpectrumDEX:\n- Wallet age: 2 days\n- 7 previous contracts, 3 flagged as scam\n- Funded via Tornado Cash\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for SpectrumDEX:\n- Telegram: many users complaining 'cannot sell', 'transaction reverts'\n- Team response: 'slippage issue being fixed'\n- Twitter: active but ignoring sell complaints\n- Reddit: multiple posts about trapped funds\n- Website: no team information",
        "similar_contracts": "Similar Contract Analysis for SpectrumDEX:\n- Bytecode: 99% match with 20 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $5M in losses\n- TokenSniffer: 18/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for SpectrumDEX:\n- Only goes up: $0.028 \u2192 $8.76\n- Zero corrections (no one can sell)\n- Volatility: 0.8% daily (abnormally low)\n- Monotonically increasing \u2014 clear honeypot signature\n- Volume is all buys, no sells registered"
      }
    },
    {
      "token_name": "GravityLend",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract GravityLend is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"GravityLend\", \"GRAV\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for GravityLend:\n- 1184 holders (only increasing, zero exits)\n- Top wallet: 29% (deployer)\n- No wallets have successfully reduced position in 12 days\n- Median holding: $137\n- Holder count growth: +181/day (all buys)",
        "contract_functions": "Contract Function Analysis for GravityLend:\n- updateFees(uint256,uint256) \u2014 NO upper bound check\n- Current sell tax: 98%\n- Buy tax: 4% (looks normal)\n- authorize(address[],bool) \u2014 whitelist exempts deployer from fees\n- 2 addresses currently whitelisted",
        "deployer_history": "Deployer History for GravityLend:\n- Wallet age: 6 days\n- 6 previous contracts, 2 flagged as scam\n- Funded via Railgun\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for GravityLend:\n- Telegram: many users complaining 'cannot sell', 'transaction reverts'\n- Team response: 'slippage issue being fixed'\n- Twitter: active but ignoring sell complaints\n- Reddit: multiple posts about trapped funds\n- Website: no team information",
        "similar_contracts": "Similar Contract Analysis for GravityLend:\n- Bytecode: 99% match with 18 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $13M in losses\n- TokenSniffer: 11/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for GravityLend:\n- Only goes up: $0.022 \u2192 $13.55\n- Zero corrections (no one can sell)\n- Volatility: 1.9% daily (abnormally low)\n- Monotonically increasing \u2014 clear honeypot signature\n- Volume is all buys, no sells registered"
      }
    },
    {
      "token_name": "IonSwap",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract IonSwap is ERC20, Ownable {\n    mapping(address => bool) private _authorized;\n    mapping(address => uint256) private _holdTimestamp;\n    address public pair;\n    uint256 public minHoldDuration = 0;\n    bool public antiSnipeEnabled = true;\n    uint256 public launchBlock;\n\n    constructor() ERC20(\"IonSwap\", \"ION\") Ownable(msg.sender) {\n        _mint(msg.sender, 500_000_000e18);\n        _authorized[msg.sender] = true;\n    }\n\n    function openTrading(address _pair) external onlyOwner {\n        pair = _pair;\n        launchBlock = block.number;\n    }\n\n    function setAntiSnipe(bool _enabled) external onlyOwner {\n        antiSnipeEnabled = _enabled;\n    }\n\n    function setMinHoldDuration(uint256 _duration) external onlyOwner {\n        minHoldDuration = _duration;\n    }\n\n    function authorize(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            _authorized[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (from == pair) {\n            _holdTimestamp[to] = block.timestamp;\n        }\n        if (to == pair && !_authorized[from]) {\n            if (antiSnipeEnabled) {\n                require(\n                    block.timestamp >= _holdTimestamp[from] + minHoldDuration,\n                    \"Hold period not met\"\n                );\n            }\n        }\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "easy",
      "investigations": {
        "holder_distribution": "Holder Analysis for IonSwap:\n- 2246 holders (only increasing, zero exits)\n- Top wallet: 34% (deployer)\n- No wallets have successfully reduced position in 14 days\n- Median holding: $77\n- Holder count growth: +129/day (all buys)",
        "contract_functions": "Contract Function Analysis for IonSwap:\n- updateFees(uint256,uint256) \u2014 NO upper bound check\n- Current sell tax: 94%\n- Buy tax: 3% (looks normal)\n- authorize(address[],bool) \u2014 whitelist exempts deployer from fees\n- 3 addresses currently whitelisted",
        "deployer_history": "Deployer History for IonSwap:\n- Wallet age: 10 days\n- 5 previous contracts, 5 flagged as scam\n- Funded via Railgun\n- No ENS, no social verification\n- Pattern: deploy \u2192 promote \u2192 drain \u2192 repeat",
        "social_signals": "Social Analysis for IonSwap:\n- Telegram: many users complaining 'cannot sell', 'transaction reverts'\n- Team response: 'slippage issue being fixed'\n- Twitter: active but ignoring sell complaints\n- Reddit: multiple posts about trapped funds\n- Website: no team information",
        "similar_contracts": "Similar Contract Analysis for IonSwap:\n- Bytecode: 97% match with 17 known scam contracts\n- Known scam toolkit detected\n- Matched contracts caused $9M in losses\n- TokenSniffer: 18/100\n- GoPlus: CRITICAL warnings",
        "price_history": "Price History for IonSwap:\n- Only goes up: $0.078 \u2192 $9.05\n- Zero corrections (no one can sell)\n- Volatility: 0.6% daily (abnormally low)\n- Monotonically increasing \u2014 clear honeypot signature\n- Volume is all buys, no sells registered"
      }
    },
    {
      "token_name": "HaloFi",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface IRouter {\n    function swapExactTokensForETH(uint256, uint256, address[] calldata, address, uint256) external;\n    function WETH() external view returns (address);\n}\n\ncontract HaloFi is ERC20, Ownable {\n    IRouter public router;\n    address public pair;\n    uint256 public maxWalletSize = 2_000_000e18;\n    mapping(address => bool) private _isExcluded;\n    bool private _swapping;\n\n    constructor(address _router) ERC20(\"HaloFi\", \"HALT\") Ownable(msg.sender) {\n        router = IRouter(_router);\n        _mint(msg.sender, 100_000_000e18);\n        _isExcluded[msg.sender] = true;\n        _isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function setMaxWallet(uint256 _max) external onlyOwner {\n        require(_max >= 1_000_000e18, \"Too low\");\n        maxWalletSize = _max;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!_isExcluded[to] && to != pair) {\n            require(balanceOf(to) + amount <= maxWalletSize, \"Max wallet\");\n        }\n        super._update(from, to, amount);\n    }\n\n    function clearStuckBalance() external onlyOwner {\n        payable(owner()).transfer(address(this).balance);\n    }\n\n    function clearStuckTokens(address token) external onlyOwner {\n        uint256 bal = IERC20(token).balanceOf(address(this));\n        IERC20(token).transfer(owner(), bal);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for HaloFi:\n- Top wallet holds 74% of supply (labeled as 'team vesting')\n- 690 holders, growing slowly\n- Gini coefficient: 0.78\n- Top wallet has no timelock or vesting contract\n- Second largest wallet (14%) is DEX pair\n- Wallet age: 4 days",
        "contract_functions": "Contract Function Analysis for HaloFi:\n- Owner-only: rebalanceTreasury(uint256), recoverTokens(address,uint256)\n- Functions disguised as maintenance/migration utilities\n- rebalanceTreasury(uint256) can send contract's entire ETH balance to arbitrary address\n- No timelock on admin functions\n- Contract verified but comments are misleading",
        "deployer_history": "Deployer History for HaloFi:\n- Wallet age: 44 days\n- 4 previous contracts, 2 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 14 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for HaloFi:\n- Twitter: 15644 followers, 37% bots\n- Telegram: 1159 members, moderation stopped 3 days ago\n- Website: template site, stock images\n- Whitepaper: AI-generated, no technical depth\n- Team photos: reverse image search matches stock photos",
        "similar_contracts": "Similar Contract Analysis for HaloFi:\n- Bytecode: 74% match with 6 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 33/100\n- GoPlus: 4 medium-severity warnings",
        "price_history": "Price History for HaloFi:\n- Gradual rise over 20 days: $0.019 \u2192 $4.08\n- Multiple smaller dips (22%) with recovery (manufactured)\n- Final drop: -87% in 2 hours\n- Pattern: slow build with fake recoveries \u2192 sudden rug\n- Post-rug: flatline at near-zero"
      }
    },
    {
      "token_name": "NimbusFi",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\n\ncontract NimbusFi is ERC20 {\n    constructor(address treasury, address community, address lp) ERC20(\"NimbusFi\", \"NIMB\") {\n        _mint(treasury, 30_000_000e18);   // 30% treasury (multisig)\n        _mint(community, 50_000_000e18);  // 50% community airdrop\n        _mint(lp, 20_000_000e18);         // 20% initial liquidity\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for NimbusFi:\n- 80,458 holders\n- Top wallet: 4.9% (team vesting, locked 12mo)\n- 11 whale wallets hold >0.6% each\n- Gini: 0.58 (moderate)\n- Smart money: 5 Nansen-labeled fund wallets",
        "contract_functions": "Contract Function Analysis for NimbusFi:\n- ERC20Votes + governance\n- Mint capped at totalSupply/100 per tx, maxSupply enforced\n- Minter role held by governance timelock (48h delay)\n- No functions transfer tokens/ETH to specific addresses\n- All admin behind governance timelock",
        "deployer_history": "Deployer History for NimbusFi:\n- Wallet age: 1436 days\n- Pseudonymous but long reputation in DeFi\n- 9 contracts, clean track record\n- Active in governance of Compound\n- Funded via known DAO treasury",
        "social_signals": "Social Analysis for NimbusFi:\n- Twitter: 49823 followers, organic growth\n- Discord: 8527 members\n- GitHub: active development, weekly commits\n- Team: pseudonymous but known in ecosystem for 2 years\n- Partnerships: Chainlink integration",
        "similar_contracts": "Similar Contract Analysis for NimbusFi:\n- Clean architecture, audited patterns\n- TokenSniffer: 83/100\n- GoPlus: 1 informational finding (non-critical)\n- Similar to 25 legitimate DeFi protocols\n- No known scam contract matches",
        "price_history": "Price History for NimbusFi:\n- 202 day history\n- Correlated with ETH price (beta: 1.0)\n- 4 significant drawdowns (31%), all recovered\n- Volume follows news cycle (organic)\n- No single-block large trades dominating price action"
      }
    },
    {
      "token_name": "DeltaLend",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract DeltaLend is ERC20, Ownable {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public immutable vestingStart;\n    uint256 public constant VESTING_DURATION = 365 days;\n    uint256 public teamAllocation;\n    uint256 public teamClaimed;\n\n    constructor() ERC20(\"DeltaLend\", \"DELT\") Ownable(msg.sender) {\n        vestingStart = block.timestamp;\n        teamAllocation = MAX_SUPPLY * 15 / 100; // 15% team\n        _mint(address(this), teamAllocation);\n        _mint(msg.sender, MAX_SUPPLY - teamAllocation); // 85% to LP/community\n    }\n\n    function claimVested() external onlyOwner {\n        uint256 elapsed = block.timestamp - vestingStart;\n        if (elapsed > VESTING_DURATION) elapsed = VESTING_DURATION;\n        uint256 totalVested = (teamAllocation * elapsed) / VESTING_DURATION;\n        uint256 claimable = totalVested - teamClaimed;\n        require(claimable > 0, \"Nothing to claim\");\n        teamClaimed += claimable;\n        _transfer(address(this), owner(), claimable);\n    }\n\n    function renounceOwnership() public override onlyOwner {\n        super.renounceOwnership();\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for DeltaLend:\n- 109,206 holders\n- Top wallet: 7.6% (team vesting, locked 12mo)\n- 6 whale wallets hold >0.6% each\n- Gini: 0.52 (moderate)\n- Smart money: 7 Nansen-labeled fund wallets",
        "contract_functions": "Contract Function Analysis for DeltaLend:\n- ERC20Votes + governance\n- Mint capped at totalSupply/100 per tx, maxSupply enforced\n- Minter role held by governance timelock (48h delay)\n- No functions transfer tokens/ETH to specific addresses\n- All admin behind 3/5 multisig",
        "deployer_history": "Deployer History for DeltaLend:\n- Wallet age: 1687 days\n- Pseudonymous but long reputation in DeFi\n- 9 contracts, clean track record\n- Active in governance of Aave\n- Funded via known DAO treasury",
        "social_signals": "Social Analysis for DeltaLend:\n- Twitter: 45505 followers, organic growth\n- Discord: 12479 members\n- GitHub: active development, weekly commits\n- Team: pseudonymous but known in ecosystem for 2 years\n- Partnerships: Chainlink integration",
        "similar_contracts": "Similar Contract Analysis for DeltaLend:\n- Clean architecture, audited patterns\n- TokenSniffer: 93/100\n- GoPlus: 1 informational finding (non-critical)\n- Similar to 30 legitimate DeFi protocols\n- No known scam contract matches",
        "price_history": "Price History for DeltaLend:\n- 229 day history\n- Correlated with ETH price (beta: 1.2)\n- 3 significant drawdowns (25%), all recovered\n- Volume follows news cycle (organic)\n- No single-block large trades dominating price action"
      }
    },
    {
      "token_name": "SummitSwap",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Votes.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Permit.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract SummitSwap is ERC20Votes, AccessControl {\n    bytes32 public constant MINTER_ROLE = keccak256(\"MINTER_ROLE\");\n    uint256 public immutable maxSupply;\n    uint256 public immutable mintCap; // max per tx\n\n    constructor(uint256 _maxSupply)\n        ERC20(\"SummitSwap\", \"SUMM\")\n        ERC20Permit(\"SummitSwap\")\n    {\n        maxSupply = _maxSupply;\n        mintCap = _maxSupply / 100;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _mint(msg.sender, _maxSupply / 5); // 20% initial\n    }\n\n    function mint(address to, uint256 amount) external onlyRole(MINTER_ROLE) {\n        require(amount <= mintCap, \"Exceeds mint cap\");\n        require(totalSupply() + amount <= maxSupply, \"Exceeds max supply\");\n        _mint(to, amount);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for SummitSwap:\n- 85,507 holders\n- Top wallet: 3.1% (team vesting, locked 12mo)\n- 9 whale wallets hold >0.8% each\n- Gini: 0.59 (moderate)\n- Smart money: 10 Nansen-labeled fund wallets",
        "contract_functions": "Contract Function Analysis for SummitSwap:\n- ERC20Votes + governance\n- Mint capped at totalSupply/100 per tx, maxSupply enforced\n- Minter role held by governance timelock (48h delay)\n- No functions transfer tokens/ETH to specific addresses\n- All admin behind DAO vote",
        "deployer_history": "Deployer History for SummitSwap:\n- Wallet age: 656 days\n- Pseudonymous but long reputation in DeFi\n- 7 contracts, clean track record\n- Active in governance of Uniswap\n- Funded via known DAO treasury",
        "social_signals": "Social Analysis for SummitSwap:\n- Twitter: 49369 followers, organic growth\n- Discord: 8124 members\n- GitHub: active development, weekly commits\n- Team: pseudonymous but known in ecosystem for 3 years\n- Partnerships: LayerZero integration",
        "similar_contracts": "Similar Contract Analysis for SummitSwap:\n- Clean architecture, audited patterns\n- TokenSniffer: 88/100\n- GoPlus: 1 informational finding (non-critical)\n- Similar to 28 legitimate DeFi protocols\n- No known scam contract matches",
        "price_history": "Price History for SummitSwap:\n- 132 day history\n- Correlated with ETH price (beta: 1.0)\n- 5 significant drawdowns (28%), all recovered\n- Volume follows news cycle (organic)\n- No single-block large trades dominating price action"
      }
    },
    {
      "token_name": "GammaProtocol",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract GammaProtocol is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"GammaProtocol\", \"GAM\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for GammaProtocol:\n- 21 holders, 12 are active traders\n- Cluster analysis: 3 distinct wallet groups, each from one source\n- Some wallets hold dust amounts of other tokens (cover)\n- Wallet creation spread over 6 days\n- Net token flow between top wallets: approximately zero",
        "contract_functions": "Contract Function Analysis for GammaProtocol:\n- addMarketMaker(address) \u2014 12 registered\n- fundMarketMakers(uint256[]) \u2014 bulk fund from owner\n- rebalanceAllocations() \u2014 shuffle tokens between market makers\n- Functions labeled as 'liquidity management'\n- No external DEX interaction, all transfers are internal",
        "deployer_history": "Deployer History for GammaProtocol:\n- 4 contracts deployed, spread across 2 chains\n- Contracts share common library code (batch operations)\n- Deployer funds market maker wallets through intermediaries\n- Some previous tokens are still active with inflated volume\n- No audit history on any deployment",
        "social_signals": "Social Analysis for GammaProtocol:\n- Promoted as 'fastest growing DeFi protocol'\n- Twitter: 5488 followers with purchased engagement\n- Discord: exists but 60% of messages are from bots\n- Medium blog: repurposed AI content about 'revolutionizing DeFi'\n- No developer community or GitHub activity",
        "similar_contracts": "Similar Contract Analysis for GammaProtocol:\n- Contract shares market maker infrastructure with 7 tokens\n- Batch operation pattern seen in known wash trading setups\n- TokenSniffer: 37/100\n- Similar contracts spread across 3 chains",
        "price_history": "Price History for GammaProtocol:\n- Appears to have normal volatility (4% daily)\n- BUT: price always returns to same level within 4 hours\n- Mean-reversion too perfect \u2014 natural markets have drift\n- Trade sizes suspiciously uniform\n- Volume spikes at exactly same time each day"
      }
    },
    {
      "token_name": "YieldNest",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract YieldNest is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"YieldNest\", \"YIE\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for YieldNest:\n- 27 holders, 18 are active traders\n- Cluster analysis: 3 distinct wallet groups, each from one source\n- Some wallets hold dust amounts of other tokens (cover)\n- Wallet creation spread over 12 days\n- Net token flow between top wallets: approximately zero",
        "contract_functions": "Contract Function Analysis for YieldNest:\n- addMarketMaker(address) \u2014 18 registered\n- fundMarketMakers(uint256[]) \u2014 bulk fund from owner\n- rebalanceAllocations() \u2014 shuffle tokens between market makers\n- Functions labeled as 'liquidity management'\n- No external DEX interaction, all transfers are internal",
        "deployer_history": "Deployer History for YieldNest:\n- 7 contracts deployed, spread across 3 chains\n- Contracts share common library code (batch operations)\n- Deployer funds market maker wallets through intermediaries\n- Some previous tokens are still active with inflated volume\n- No audit history on any deployment",
        "social_signals": "Social Analysis for YieldNest:\n- Promoted as 'fastest growing DeFi protocol'\n- Twitter: 9118 followers with purchased engagement\n- Discord: exists but 61% of messages are from bots\n- Medium blog: repurposed AI content about 'revolutionizing DeFi'\n- No developer community or GitHub activity",
        "similar_contracts": "Similar Contract Analysis for YieldNest:\n- Contract shares market maker infrastructure with 4 tokens\n- Batch operation pattern seen in known wash trading setups\n- TokenSniffer: 49/100\n- Similar contracts spread across 3 chains",
        "price_history": "Price History for YieldNest:\n- Appears to have normal volatility (5% daily)\n- BUT: price always returns to same level within 5 hours\n- Mean-reversion too perfect \u2014 natural markets have drift\n- Trade sizes suspiciously uniform\n- Volume spikes at exactly same time each day"
      }
    },
    {
      "token_name": "SolaceDAO",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\n\ncontract SolaceDAO is ERC20 {\n    constructor(address treasury, address community, address lp) ERC20(\"SolaceDAO\", \"SOLA\") {\n        _mint(treasury, 30_000_000e18);   // 30% treasury (multisig)\n        _mint(community, 50_000_000e18);  // 50% community airdrop\n        _mint(lp, 20_000_000e18);         // 20% initial liquidity\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for SolaceDAO:\n- 86,929 holders\n- Top wallet: 6.0% (team vesting, locked 12mo)\n- 12 whale wallets hold >1.3% each\n- Gini: 0.57 (moderate)\n- Smart money: 10 Nansen-labeled fund wallets",
        "contract_functions": "Contract Function Analysis for SolaceDAO:\n- ERC20Votes + governance\n- Mint capped at totalSupply/100 per tx, maxSupply enforced\n- Minter role held by governance timelock (48h delay)\n- No functions transfer tokens/ETH to specific addresses\n- All admin behind governance timelock",
        "deployer_history": "Deployer History for SolaceDAO:\n- Wallet age: 1116 days\n- Pseudonymous but long reputation in DeFi\n- 7 contracts, clean track record\n- Active in governance of Uniswap\n- Funded via known DAO treasury",
        "social_signals": "Social Analysis for SolaceDAO:\n- Twitter: 13000 followers, organic growth\n- Discord: 11534 members\n- GitHub: active development, weekly commits\n- Team: pseudonymous but known in ecosystem for 5 years\n- Partnerships: LayerZero integration",
        "similar_contracts": "Similar Contract Analysis for SolaceDAO:\n- Clean architecture, audited patterns\n- TokenSniffer: 84/100\n- GoPlus: 1 informational finding (non-critical)\n- Similar to 20 legitimate DeFi protocols\n- No known scam contract matches",
        "price_history": "Price History for SolaceDAO:\n- 141 day history\n- Correlated with ETH price (beta: 0.9)\n- 6 significant drawdowns (36%), all recovered\n- Volume follows news cycle (organic)\n- No single-block large trades dominating price action"
      }
    },
    {
      "token_name": "PivotDAO",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/security/Pausable.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract PivotDAO is ERC20, Pausable, Ownable {\n    uint256 public constant INITIAL_SUPPLY = 50_000_000e18;\n    address public treasuryWallet;\n    uint256 public withdrawalTimelock = 48 hours;\n    mapping(address => uint256) public lastWithdrawal;\n    uint256 public totalStaked;\n    mapping(address => uint256) public stakedBalance;\n\n    constructor(address _treasury) ERC20(\"PivotDAO\", \"PIVO\") Ownable(msg.sender) {\n        treasuryWallet = _treasury;\n        _mint(address(this), INITIAL_SUPPLY);\n    }\n\n    function stake(uint256 amount) external whenNotPaused {\n        _transfer(msg.sender, address(this), amount);\n        stakedBalance[msg.sender] += amount;\n        totalStaked += amount;\n    }\n\n    function unstake(uint256 amount) external whenNotPaused {\n        require(stakedBalance[msg.sender] >= amount, \"Insufficient stake\");\n        require(block.timestamp >= lastWithdrawal[msg.sender] + withdrawalTimelock, \"Timelock\");\n        stakedBalance[msg.sender] -= amount;\n        totalStaked -= amount;\n        lastWithdrawal[msg.sender] = block.timestamp;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function setWithdrawalTimelock(uint256 _timelock) external onlyOwner {\n        withdrawalTimelock = _timelock;\n    }\n\n    /// @dev Admin function to rebalance treasury allocation\n    function rebalanceTreasury(uint256 amount) external onlyOwner {\n        _transfer(address(this), treasuryWallet, amount);\n    }\n\n    function pause() external onlyOwner { _pause(); }\n    function unpause() external onlyOwner { _unpause(); }\n\n    receive() external payable {}\n\n    function withdrawETH() external onlyOwner {\n        payable(treasuryWallet).transfer(address(this).balance);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for PivotDAO:\n- Top wallet holds 71% of supply (labeled as 'team vesting')\n- 539 holders, growing slowly\n- Gini coefficient: 0.79\n- Top wallet has no timelock or vesting contract\n- Second largest wallet (6%) is DEX pair\n- Wallet age: 3 days",
        "contract_functions": "Contract Function Analysis for PivotDAO:\n- Owner-only: recoverTokens(address,uint256), rebalanceTreasury(uint256)\n- Functions disguised as maintenance/migration utilities\n- recoverTokens(address,uint256) can send contract's entire ETH balance to arbitrary address\n- No timelock on admin functions\n- Contract verified but comments are misleading",
        "deployer_history": "Deployer History for PivotDAO:\n- Wallet age: 32 days\n- 4 previous contracts, 2 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 3 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for PivotDAO:\n- Twitter: 6424 followers, 36% bots\n- Telegram: 2974 members, moderation stopped 4 days ago\n- Website: template site, stock images\n- Whitepaper: AI-generated, no technical depth\n- Team photos: reverse image search matches stock photos",
        "similar_contracts": "Similar Contract Analysis for PivotDAO:\n- Bytecode: 70% match with 4 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 28/100\n- GoPlus: 2 medium-severity warnings",
        "price_history": "Price History for PivotDAO:\n- Gradual rise over 18 days: $0.083 \u2192 $2.33\n- Multiple smaller dips (22%) with recovery (manufactured)\n- Final drop: -86% in 2 hours\n- Pattern: slow build with fake recoveries \u2192 sudden rug\n- Post-rug: flatline at near-zero"
      }
    },
    {
      "token_name": "HorizonDAO",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract HorizonDAO is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 200_000_000e18;\n    address public pair;\n    mapping(address => uint256) public lastTransfer;\n    uint256 public cooldownPeriod = 30; // seconds\n    mapping(address => bool) public isWhitelisted;\n    uint256 public maxWalletPercent = 200; // 2%\n\n    constructor() ERC20(\"HorizonDAO\", \"HORI\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n        isWhitelisted[msg.sender] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n        isWhitelisted[_pair] = true;\n    }\n\n    function setCooldown(uint256 _seconds) external onlyOwner {\n        cooldownPeriod = _seconds;\n    }\n\n    function setMaxWalletPercent(uint256 _percent) external onlyOwner {\n        require(_percent >= 100, \"Min 1%\");\n        maxWalletPercent = _percent;\n    }\n\n    function whitelist(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            isWhitelisted[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isWhitelisted[from] && !isWhitelisted[to]) {\n            require(\n                block.timestamp >= lastTransfer[from] + cooldownPeriod,\n                \"Cooldown active\"\n            );\n            if (to != pair) {\n                require(\n                    balanceOf(to) + amount <= (totalSupply() * maxWalletPercent) / 10000,\n                    \"Exceeds max wallet\"\n                );\n            }\n        }\n        lastTransfer[from] = block.timestamp;\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for HorizonDAO:\n- 4341 holders\n- Top wallet: 15% (labeled marketing wallet)\n- 4 wallets have sold \u2014 all pre-launch wallets\n- Post-launch sellers: 0 successful\n- Gini: 0.55\n- Holder velocity: growing 7% daily",
        "contract_functions": "Contract Function Analysis for HorizonDAO:\n- setMinHoldDuration(uint256) \u2014 owner can set to any value\n- Currently set to 37 days (users can't sell before then)\n- authorize() exempts specific addresses from hold requirement\n- Transfer function checks: if (to == pair && !authorized[from]) revert\n- Buy path has no restrictions",
        "deployer_history": "Deployer History for HorizonDAO:\n- Wallet age: 53 days\n- 4 previous contracts, 1 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 6 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for HorizonDAO:\n- Telegram: 3390 members, complaints being deleted\n- Team blames 'DEX integration issues' for sell failures\n- Twitter: 8223 followers, promotional content only\n- Some users report successful sells (these are whitelisted insiders)\n- Website: professional-looking but no team page",
        "similar_contracts": "Similar Contract Analysis for HorizonDAO:\n- Bytecode: 78% match with 8 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 30/100\n- GoPlus: 4 medium-severity warnings",
        "price_history": "Price History for HorizonDAO:\n- Mostly upward: +427% since launch\n- Small dips exist (3%) but only from whitelisted wallet sells\n- Volatility: 2.2% daily (low for micro-cap)\n- Organic tokens at this market cap show 23% daily swings\n- Price stability is suspiciously artificial"
      }
    },
    {
      "token_name": "FluxDAO",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract FluxDAO is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"FluxDAO\", \"FLU\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for FluxDAO:\n- 28 holders, 28 are active traders\n- Cluster analysis: 3 distinct wallet groups, each from one source\n- Some wallets hold dust amounts of other tokens (cover)\n- Wallet creation spread over 5 days\n- Net token flow between top wallets: approximately zero",
        "contract_functions": "Contract Function Analysis for FluxDAO:\n- addMarketMaker(address) \u2014 12 registered\n- fundMarketMakers(uint256[]) \u2014 bulk fund from owner\n- rebalanceAllocations() \u2014 shuffle tokens between market makers\n- Functions labeled as 'liquidity management'\n- No external DEX interaction, all transfers are internal",
        "deployer_history": "Deployer History for FluxDAO:\n- 7 contracts deployed, spread across 2 chains\n- Contracts share common library code (batch operations)\n- Deployer funds market maker wallets through intermediaries\n- Some previous tokens are still active with inflated volume\n- No audit history on any deployment",
        "social_signals": "Social Analysis for FluxDAO:\n- Promoted as 'fastest growing DeFi protocol'\n- Twitter: 3256 followers with purchased engagement\n- Discord: exists but 73% of messages are from bots\n- Medium blog: repurposed AI content about 'revolutionizing DeFi'\n- No developer community or GitHub activity",
        "similar_contracts": "Similar Contract Analysis for FluxDAO:\n- Contract shares market maker infrastructure with 8 tokens\n- Batch operation pattern seen in known wash trading setups\n- TokenSniffer: 49/100\n- Similar contracts spread across 3 chains",
        "price_history": "Price History for FluxDAO:\n- Appears to have normal volatility (6% daily)\n- BUT: price always returns to same level within 14 hours\n- Mean-reversion too perfect \u2014 natural markets have drift\n- Trade sizes suspiciously uniform\n- Volume spikes at exactly same time each day"
      }
    },
    {
      "token_name": "VortexYield",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract VortexYield is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"VortexYield\", \"VOR\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for VortexYield:\n- 15 holders, 10 are active traders\n- Cluster analysis: 3 distinct wallet groups, each from one source\n- Some wallets hold dust amounts of other tokens (cover)\n- Wallet creation spread over 11 days\n- Net token flow between top wallets: approximately zero",
        "contract_functions": "Contract Function Analysis for VortexYield:\n- addMarketMaker(address) \u2014 12 registered\n- fundMarketMakers(uint256[]) \u2014 bulk fund from owner\n- rebalanceAllocations() \u2014 shuffle tokens between market makers\n- Functions labeled as 'liquidity management'\n- No external DEX interaction, all transfers are internal",
        "deployer_history": "Deployer History for VortexYield:\n- 4 contracts deployed, spread across 3 chains\n- Contracts share common library code (batch operations)\n- Deployer funds market maker wallets through intermediaries\n- Some previous tokens are still active with inflated volume\n- No audit history on any deployment",
        "social_signals": "Social Analysis for VortexYield:\n- Promoted as 'fastest growing DeFi protocol'\n- Twitter: 6918 followers with purchased engagement\n- Discord: exists but 74% of messages are from bots\n- Medium blog: repurposed AI content about 'revolutionizing DeFi'\n- No developer community or GitHub activity",
        "similar_contracts": "Similar Contract Analysis for VortexYield:\n- Contract shares market maker infrastructure with 3 tokens\n- Batch operation pattern seen in known wash trading setups\n- TokenSniffer: 37/100\n- Similar contracts spread across 3 chains",
        "price_history": "Price History for VortexYield:\n- Appears to have normal volatility (7% daily)\n- BUT: price always returns to same level within 10 hours\n- Mean-reversion too perfect \u2014 natural markets have drift\n- Trade sizes suspiciously uniform\n- Volume spikes at exactly same time each day"
      }
    },
    {
      "token_name": "QuantumSwap",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract QuantumSwap is ERC20, Ownable {\n    mapping(address => bool) private _authorized;\n    mapping(address => uint256) private _holdTimestamp;\n    address public pair;\n    uint256 public minHoldDuration = 0;\n    bool public antiSnipeEnabled = true;\n    uint256 public launchBlock;\n\n    constructor() ERC20(\"QuantumSwap\", \"QUA\") Ownable(msg.sender) {\n        _mint(msg.sender, 500_000_000e18);\n        _authorized[msg.sender] = true;\n    }\n\n    function openTrading(address _pair) external onlyOwner {\n        pair = _pair;\n        launchBlock = block.number;\n    }\n\n    function setAntiSnipe(bool _enabled) external onlyOwner {\n        antiSnipeEnabled = _enabled;\n    }\n\n    function setMinHoldDuration(uint256 _duration) external onlyOwner {\n        minHoldDuration = _duration;\n    }\n\n    function authorize(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            _authorized[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (from == pair) {\n            _holdTimestamp[to] = block.timestamp;\n        }\n        if (to == pair && !_authorized[from]) {\n            if (antiSnipeEnabled) {\n                require(\n                    block.timestamp >= _holdTimestamp[from] + minHoldDuration,\n                    \"Hold period not met\"\n                );\n            }\n        }\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for QuantumSwap:\n- 3188 holders\n- Top wallet: 10% (labeled marketing wallet)\n- 2 wallets have sold \u2014 all pre-launch wallets\n- Post-launch sellers: 0 successful\n- Gini: 0.69\n- Holder velocity: growing 6% daily",
        "contract_functions": "Contract Function Analysis for QuantumSwap:\n- setMinHoldDuration(uint256) \u2014 owner can set to any value\n- Currently set to 253 days (users can't sell before then)\n- authorize() exempts specific addresses from hold requirement\n- Transfer function checks: if (to == pair && !authorized[from]) revert\n- Buy path has no restrictions",
        "deployer_history": "Deployer History for QuantumSwap:\n- Wallet age: 45 days\n- 3 previous contracts, 2 inactive (abandoned)\n- Funded via cross-chain bridge\n- Twitter linked but account is 9 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for QuantumSwap:\n- Telegram: 1692 members, complaints being deleted\n- Team blames 'DEX integration issues' for sell failures\n- Twitter: 14307 followers, promotional content only\n- Some users report successful sells (these are whitelisted insiders)\n- Website: professional-looking but no team page",
        "similar_contracts": "Similar Contract Analysis for QuantumSwap:\n- Bytecode: 76% match with 6 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 31/100\n- GoPlus: 2 medium-severity warnings",
        "price_history": "Price History for QuantumSwap:\n- Mostly upward: +541% since launch\n- Small dips exist (5%) but only from whitelisted wallet sells\n- Volatility: 2.8% daily (low for micro-cap)\n- Organic tokens at this market cap show 23% daily swings\n- Price stability is suspiciously artificial"
      }
    },
    {
      "token_name": "MagnetSwap",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract MagnetSwap is ERC20, Ownable {\n    mapping(address => bool) private _authorized;\n    mapping(address => uint256) private _holdTimestamp;\n    address public pair;\n    uint256 public minHoldDuration = 0;\n    bool public antiSnipeEnabled = true;\n    uint256 public launchBlock;\n\n    constructor() ERC20(\"MagnetSwap\", \"MAG\") Ownable(msg.sender) {\n        _mint(msg.sender, 500_000_000e18);\n        _authorized[msg.sender] = true;\n    }\n\n    function openTrading(address _pair) external onlyOwner {\n        pair = _pair;\n        launchBlock = block.number;\n    }\n\n    function setAntiSnipe(bool _enabled) external onlyOwner {\n        antiSnipeEnabled = _enabled;\n    }\n\n    function setMinHoldDuration(uint256 _duration) external onlyOwner {\n        minHoldDuration = _duration;\n    }\n\n    function authorize(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            _authorized[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (from == pair) {\n            _holdTimestamp[to] = block.timestamp;\n        }\n        if (to == pair && !_authorized[from]) {\n            if (antiSnipeEnabled) {\n                require(\n                    block.timestamp >= _holdTimestamp[from] + minHoldDuration,\n                    \"Hold period not met\"\n                );\n            }\n        }\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for MagnetSwap:\n- 4379 holders\n- Top wallet: 23% (labeled marketing wallet)\n- 2 wallets have sold \u2014 all pre-launch wallets\n- Post-launch sellers: 0 successful\n- Gini: 0.66\n- Holder velocity: growing 8% daily",
        "contract_functions": "Contract Function Analysis for MagnetSwap:\n- setMinHoldDuration(uint256) \u2014 owner can set to any value\n- Currently set to 247 days (users can't sell before then)\n- authorize() exempts specific addresses from hold requirement\n- Transfer function checks: if (to == pair && !authorized[from]) revert\n- Buy path has no restrictions",
        "deployer_history": "Deployer History for MagnetSwap:\n- Wallet age: 43 days\n- 5 previous contracts, 2 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 2 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for MagnetSwap:\n- Telegram: 1496 members, complaints being deleted\n- Team blames 'DEX integration issues' for sell failures\n- Twitter: 5037 followers, promotional content only\n- Some users report successful sells (these are whitelisted insiders)\n- Website: professional-looking but no team page",
        "similar_contracts": "Similar Contract Analysis for MagnetSwap:\n- Bytecode: 70% match with 6 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 26/100\n- GoPlus: 4 medium-severity warnings",
        "price_history": "Price History for MagnetSwap:\n- Mostly upward: +623% since launch\n- Small dips exist (2%) but only from whitelisted wallet sells\n- Volatility: 4.6% daily (low for micro-cap)\n- Organic tokens at this market cap show 24% daily swings\n- Price stability is suspiciously artificial"
      }
    },
    {
      "token_name": "ZealotFi",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract ZealotFi is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"ZealotFi\", \"ZEAL\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for ZealotFi:\n- 19 holders, 15 are active traders\n- Cluster analysis: 3 distinct wallet groups, each from one source\n- Some wallets hold dust amounts of other tokens (cover)\n- Wallet creation spread over 11 days\n- Net token flow between top wallets: approximately zero",
        "contract_functions": "Contract Function Analysis for ZealotFi:\n- addMarketMaker(address) \u2014 14 registered\n- fundMarketMakers(uint256[]) \u2014 bulk fund from owner\n- rebalanceAllocations() \u2014 shuffle tokens between market makers\n- Functions labeled as 'liquidity management'\n- No external DEX interaction, all transfers are internal",
        "deployer_history": "Deployer History for ZealotFi:\n- 3 contracts deployed, spread across 2 chains\n- Contracts share common library code (batch operations)\n- Deployer funds market maker wallets through intermediaries\n- Some previous tokens are still active with inflated volume\n- No audit history on any deployment",
        "social_signals": "Social Analysis for ZealotFi:\n- Promoted as 'fastest growing DeFi protocol'\n- Twitter: 6478 followers with purchased engagement\n- Discord: exists but 78% of messages are from bots\n- Medium blog: repurposed AI content about 'revolutionizing DeFi'\n- No developer community or GitHub activity",
        "similar_contracts": "Similar Contract Analysis for ZealotFi:\n- Contract shares market maker infrastructure with 5 tokens\n- Batch operation pattern seen in known wash trading setups\n- TokenSniffer: 38/100\n- Similar contracts spread across 2 chains",
        "price_history": "Price History for ZealotFi:\n- Appears to have normal volatility (4% daily)\n- BUT: price always returns to same level within 10 hours\n- Mean-reversion too perfect \u2014 natural markets have drift\n- Trade sizes suspiciously uniform\n- Volume spikes at exactly same time each day"
      }
    },
    {
      "token_name": "BreezeDEX",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface IRouter {\n    function swapExactTokensForETH(uint256, uint256, address[] calldata, address, uint256) external;\n    function WETH() external view returns (address);\n}\n\ncontract BreezeDEX is ERC20, Ownable {\n    IRouter public router;\n    address public pair;\n    uint256 public maxWalletSize = 2_000_000e18;\n    mapping(address => bool) private _isExcluded;\n    bool private _swapping;\n\n    constructor(address _router) ERC20(\"BreezeDEX\", \"BRET\") Ownable(msg.sender) {\n        router = IRouter(_router);\n        _mint(msg.sender, 100_000_000e18);\n        _isExcluded[msg.sender] = true;\n        _isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function setMaxWallet(uint256 _max) external onlyOwner {\n        require(_max >= 1_000_000e18, \"Too low\");\n        maxWalletSize = _max;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!_isExcluded[to] && to != pair) {\n            require(balanceOf(to) + amount <= maxWalletSize, \"Max wallet\");\n        }\n        super._update(from, to, amount);\n    }\n\n    function clearStuckBalance() external onlyOwner {\n        payable(owner()).transfer(address(this).balance);\n    }\n\n    function clearStuckTokens(address token) external onlyOwner {\n        uint256 bal = IERC20(token).balanceOf(address(this));\n        IERC20(token).transfer(owner(), bal);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for BreezeDEX:\n- Top wallet holds 62% of supply (labeled as 'team vesting')\n- 229 holders, growing slowly\n- Gini coefficient: 0.82\n- Top wallet has no timelock or vesting contract\n- Second largest wallet (14%) is DEX pair\n- Wallet age: 12 days",
        "contract_functions": "Contract Function Analysis for BreezeDEX:\n- Owner-only: setRouter(address), migrateV2(address)\n- Functions disguised as maintenance/migration utilities\n- setRouter(address) can send contract's entire ETH balance to arbitrary address\n- No timelock on admin functions\n- Contract verified but comments are misleading",
        "deployer_history": "Deployer History for BreezeDEX:\n- Wallet age: 55 days\n- 2 previous contracts, 1 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 6 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for BreezeDEX:\n- Twitter: 16920 followers, 41% bots\n- Telegram: 2134 members, moderation stopped 3 days ago\n- Website: template site, stock images\n- Whitepaper: AI-generated, no technical depth\n- Team photos: reverse image search matches stock photos",
        "similar_contracts": "Similar Contract Analysis for BreezeDEX:\n- Bytecode: 70% match with 5 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 36/100\n- GoPlus: 3 medium-severity warnings",
        "price_history": "Price History for BreezeDEX:\n- Gradual rise over 15 days: $0.096 \u2192 $3.61\n- Multiple smaller dips (12%) with recovery (manufactured)\n- Final drop: -85% in 2 hours\n- Pattern: slow build with fake recoveries \u2192 sudden rug\n- Post-rug: flatline at near-zero"
      }
    },
    {
      "token_name": "AnchorLend",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\n\ncontract AnchorLend is ERC20 {\n    constructor(address treasury, address community, address lp) ERC20(\"AnchorLend\", \"ANCH\") {\n        _mint(treasury, 30_000_000e18);   // 30% treasury (multisig)\n        _mint(community, 50_000_000e18);  // 50% community airdrop\n        _mint(lp, 20_000_000e18);         // 20% initial liquidity\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for AnchorLend:\n- 74,896 holders\n- Top wallet: 7.9% (team vesting, locked 12mo)\n- 11 whale wallets hold >1.1% each\n- Gini: 0.65 (moderate)\n- Smart money: 3 Nansen-labeled fund wallets",
        "contract_functions": "Contract Function Analysis for AnchorLend:\n- ERC20Votes + governance\n- Mint capped at totalSupply/100 per tx, maxSupply enforced\n- Minter role held by governance timelock (48h delay)\n- No functions transfer tokens/ETH to specific addresses\n- All admin behind governance timelock",
        "deployer_history": "Deployer History for AnchorLend:\n- Wallet age: 470 days\n- Pseudonymous but long reputation in DeFi\n- 7 contracts, clean track record\n- Active in governance of Aave\n- Funded via known DAO treasury",
        "social_signals": "Social Analysis for AnchorLend:\n- Twitter: 33469 followers, organic growth\n- Discord: 14564 members\n- GitHub: active development, weekly commits\n- Team: pseudonymous but known in ecosystem for 5 years\n- Partnerships: LayerZero integration",
        "similar_contracts": "Similar Contract Analysis for AnchorLend:\n- Clean architecture, audited patterns\n- TokenSniffer: 84/100\n- GoPlus: 1 informational finding (non-critical)\n- Similar to 12 legitimate DeFi protocols\n- No known scam contract matches",
        "price_history": "Price History for AnchorLend:\n- 377 day history\n- Correlated with ETH price (beta: 1.4)\n- 5 significant drawdowns (20%), all recovered\n- Volume follows news cycle (organic)\n- No single-block large trades dominating price action"
      }
    },
    {
      "token_name": "SummitSwap",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface IRouter {\n    function swapExactTokensForETH(uint256, uint256, address[] calldata, address, uint256) external;\n    function WETH() external view returns (address);\n}\n\ncontract SummitSwap is ERC20, Ownable {\n    IRouter public router;\n    address public pair;\n    uint256 public maxWalletSize = 2_000_000e18;\n    mapping(address => bool) private _isExcluded;\n    bool private _swapping;\n\n    constructor(address _router) ERC20(\"SummitSwap\", \"SUMT\") Ownable(msg.sender) {\n        router = IRouter(_router);\n        _mint(msg.sender, 100_000_000e18);\n        _isExcluded[msg.sender] = true;\n        _isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function setMaxWallet(uint256 _max) external onlyOwner {\n        require(_max >= 1_000_000e18, \"Too low\");\n        maxWalletSize = _max;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!_isExcluded[to] && to != pair) {\n            require(balanceOf(to) + amount <= maxWalletSize, \"Max wallet\");\n        }\n        super._update(from, to, amount);\n    }\n\n    function clearStuckBalance() external onlyOwner {\n        payable(owner()).transfer(address(this).balance);\n    }\n\n    function clearStuckTokens(address token) external onlyOwner {\n        uint256 bal = IERC20(token).balanceOf(address(this));\n        IERC20(token).transfer(owner(), bal);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for SummitSwap:\n- Top wallet holds 64% of supply (labeled as 'team vesting')\n- 219 holders, growing slowly\n- Gini coefficient: 0.79\n- Top wallet has no timelock or vesting contract\n- Second largest wallet (10%) is DEX pair\n- Wallet age: 8 days",
        "contract_functions": "Contract Function Analysis for SummitSwap:\n- Owner-only: migrateV2(address), setRouter(address)\n- Functions disguised as maintenance/migration utilities\n- migrateV2(address) can send contract's entire ETH balance to arbitrary address\n- No timelock on admin functions\n- Contract verified but comments are misleading",
        "deployer_history": "Deployer History for SummitSwap:\n- Wallet age: 24 days\n- 4 previous contracts, 2 inactive (abandoned)\n- Funded via cross-chain bridge\n- Twitter linked but account is 3 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for SummitSwap:\n- Twitter: 14842 followers, 40% bots\n- Telegram: 1218 members, moderation stopped 6 days ago\n- Website: template site, stock images\n- Whitepaper: AI-generated, no technical depth\n- Team photos: reverse image search matches stock photos",
        "similar_contracts": "Similar Contract Analysis for SummitSwap:\n- Bytecode: 78% match with 7 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 26/100\n- GoPlus: 2 medium-severity warnings",
        "price_history": "Price History for SummitSwap:\n- Gradual rise over 8 days: $0.034 \u2192 $2.03\n- Multiple smaller dips (18%) with recovery (manufactured)\n- Final drop: -88% in 3 hours\n- Pattern: slow build with fake recoveries \u2192 sudden rug\n- Post-rug: flatline at near-zero"
      }
    },
    {
      "token_name": "PegasusYield",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract PegasusYield is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"PegasusYield\", \"PEG\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for PegasusYield:\n- 24 holders, 11 are active traders\n- Cluster analysis: 4 distinct wallet groups, each from one source\n- Some wallets hold dust amounts of other tokens (cover)\n- Wallet creation spread over 11 days\n- Net token flow between top wallets: approximately zero",
        "contract_functions": "Contract Function Analysis for PegasusYield:\n- addMarketMaker(address) \u2014 17 registered\n- fundMarketMakers(uint256[]) \u2014 bulk fund from owner\n- rebalanceAllocations() \u2014 shuffle tokens between market makers\n- Functions labeled as 'liquidity management'\n- No external DEX interaction, all transfers are internal",
        "deployer_history": "Deployer History for PegasusYield:\n- 6 contracts deployed, spread across 2 chains\n- Contracts share common library code (batch operations)\n- Deployer funds market maker wallets through intermediaries\n- Some previous tokens are still active with inflated volume\n- No audit history on any deployment",
        "social_signals": "Social Analysis for PegasusYield:\n- Promoted as 'fastest growing DeFi protocol'\n- Twitter: 9060 followers with purchased engagement\n- Discord: exists but 79% of messages are from bots\n- Medium blog: repurposed AI content about 'revolutionizing DeFi'\n- No developer community or GitHub activity",
        "similar_contracts": "Similar Contract Analysis for PegasusYield:\n- Contract shares market maker infrastructure with 8 tokens\n- Batch operation pattern seen in known wash trading setups\n- TokenSniffer: 36/100\n- Similar contracts spread across 3 chains",
        "price_history": "Price History for PegasusYield:\n- Appears to have normal volatility (7% daily)\n- BUT: price always returns to same level within 7 hours\n- Mean-reversion too perfect \u2014 natural markets have drift\n- Trade sizes suspiciously uniform\n- Volume spikes at exactly same time each day"
      }
    },
    {
      "token_name": "NovaSwap",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract NovaSwap is ERC20, Ownable {\n    mapping(address => bool) private _authorized;\n    mapping(address => uint256) private _holdTimestamp;\n    address public pair;\n    uint256 public minHoldDuration = 0;\n    bool public antiSnipeEnabled = true;\n    uint256 public launchBlock;\n\n    constructor() ERC20(\"NovaSwap\", \"NOV\") Ownable(msg.sender) {\n        _mint(msg.sender, 500_000_000e18);\n        _authorized[msg.sender] = true;\n    }\n\n    function openTrading(address _pair) external onlyOwner {\n        pair = _pair;\n        launchBlock = block.number;\n    }\n\n    function setAntiSnipe(bool _enabled) external onlyOwner {\n        antiSnipeEnabled = _enabled;\n    }\n\n    function setMinHoldDuration(uint256 _duration) external onlyOwner {\n        minHoldDuration = _duration;\n    }\n\n    function authorize(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            _authorized[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (from == pair) {\n            _holdTimestamp[to] = block.timestamp;\n        }\n        if (to == pair && !_authorized[from]) {\n            if (antiSnipeEnabled) {\n                require(\n                    block.timestamp >= _holdTimestamp[from] + minHoldDuration,\n                    \"Hold period not met\"\n                );\n            }\n        }\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for NovaSwap:\n- 3425 holders\n- Top wallet: 25% (labeled marketing wallet)\n- 4 wallets have sold \u2014 all pre-launch wallets\n- Post-launch sellers: 0 successful\n- Gini: 0.68\n- Holder velocity: growing 3% daily",
        "contract_functions": "Contract Function Analysis for NovaSwap:\n- setMinHoldDuration(uint256) \u2014 owner can set to any value\n- Currently set to 111 days (users can't sell before then)\n- authorize() exempts specific addresses from hold requirement\n- Transfer function checks: if (to == pair && !authorized[from]) revert\n- Buy path has no restrictions",
        "deployer_history": "Deployer History for NovaSwap:\n- Wallet age: 55 days\n- 2 previous contracts, 2 inactive (abandoned)\n- Funded via cross-chain bridge\n- Twitter linked but account is 14 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for NovaSwap:\n- Telegram: 2828 members, complaints being deleted\n- Team blames 'DEX integration issues' for sell failures\n- Twitter: 7403 followers, promotional content only\n- Some users report successful sells (these are whitelisted insiders)\n- Website: professional-looking but no team page",
        "similar_contracts": "Similar Contract Analysis for NovaSwap:\n- Bytecode: 80% match with 5 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 30/100\n- GoPlus: 2 medium-severity warnings",
        "price_history": "Price History for NovaSwap:\n- Mostly upward: +503% since launch\n- Small dips exist (5%) but only from whitelisted wallet sells\n- Volatility: 2.4% daily (low for micro-cap)\n- Organic tokens at this market cap show 21% daily swings\n- Price stability is suspiciously artificial"
      }
    },
    {
      "token_name": "EmberSwap",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract EmberSwap is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"EmberSwap\", \"EMBE\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for EmberSwap:\n- 3575 holders\n- Top wallet: 14% (labeled marketing wallet)\n- 4 wallets have sold \u2014 all pre-launch wallets\n- Post-launch sellers: 0 successful\n- Gini: 0.62\n- Holder velocity: growing 5% daily",
        "contract_functions": "Contract Function Analysis for EmberSwap:\n- setMinHoldDuration(uint256) \u2014 owner can set to any value\n- Currently set to 269 days (users can't sell before then)\n- authorize() exempts specific addresses from hold requirement\n- Transfer function checks: if (to == pair && !authorized[from]) revert\n- Buy path has no restrictions",
        "deployer_history": "Deployer History for EmberSwap:\n- Wallet age: 46 days\n- 4 previous contracts, 1 inactive (abandoned)\n- Funded via cross-chain bridge\n- Twitter linked but account is 5 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for EmberSwap:\n- Telegram: 3040 members, complaints being deleted\n- Team blames 'DEX integration issues' for sell failures\n- Twitter: 5823 followers, promotional content only\n- Some users report successful sells (these are whitelisted insiders)\n- Website: professional-looking but no team page",
        "similar_contracts": "Similar Contract Analysis for EmberSwap:\n- Bytecode: 79% match with 8 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 37/100\n- GoPlus: 3 medium-severity warnings",
        "price_history": "Price History for EmberSwap:\n- Mostly upward: +493% since launch\n- Small dips exist (5%) but only from whitelisted wallet sells\n- Volatility: 4.1% daily (low for micro-cap)\n- Organic tokens at this market cap show 12% daily swings\n- Price stability is suspiciously artificial"
      }
    },
    {
      "token_name": "NodeFi",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract NodeFi is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 200_000_000e18;\n    address public pair;\n    mapping(address => uint256) public lastTransfer;\n    uint256 public cooldownPeriod = 30; // seconds\n    mapping(address => bool) public isWhitelisted;\n    uint256 public maxWalletPercent = 200; // 2%\n\n    constructor() ERC20(\"NodeFi\", \"NODE\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n        isWhitelisted[msg.sender] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n        isWhitelisted[_pair] = true;\n    }\n\n    function setCooldown(uint256 _seconds) external onlyOwner {\n        cooldownPeriod = _seconds;\n    }\n\n    function setMaxWalletPercent(uint256 _percent) external onlyOwner {\n        require(_percent >= 100, \"Min 1%\");\n        maxWalletPercent = _percent;\n    }\n\n    function whitelist(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            isWhitelisted[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isWhitelisted[from] && !isWhitelisted[to]) {\n            require(\n                block.timestamp >= lastTransfer[from] + cooldownPeriod,\n                \"Cooldown active\"\n            );\n            if (to != pair) {\n                require(\n                    balanceOf(to) + amount <= (totalSupply() * maxWalletPercent) / 10000,\n                    \"Exceeds max wallet\"\n                );\n            }\n        }\n        lastTransfer[from] = block.timestamp;\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for NodeFi:\n- 4937 holders\n- Top wallet: 16% (labeled marketing wallet)\n- 2 wallets have sold \u2014 all pre-launch wallets\n- Post-launch sellers: 0 successful\n- Gini: 0.64\n- Holder velocity: growing 3% daily",
        "contract_functions": "Contract Function Analysis for NodeFi:\n- setMinHoldDuration(uint256) \u2014 owner can set to any value\n- Currently set to 82 days (users can't sell before then)\n- authorize() exempts specific addresses from hold requirement\n- Transfer function checks: if (to == pair && !authorized[from]) revert\n- Buy path has no restrictions",
        "deployer_history": "Deployer History for NodeFi:\n- Wallet age: 20 days\n- 3 previous contracts, 1 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 12 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for NodeFi:\n- Telegram: 3304 members, complaints being deleted\n- Team blames 'DEX integration issues' for sell failures\n- Twitter: 6395 followers, promotional content only\n- Some users report successful sells (these are whitelisted insiders)\n- Website: professional-looking but no team page",
        "similar_contracts": "Similar Contract Analysis for NodeFi:\n- Bytecode: 81% match with 7 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 37/100\n- GoPlus: 4 medium-severity warnings",
        "price_history": "Price History for NodeFi:\n- Mostly upward: +272% since launch\n- Small dips exist (2%) but only from whitelisted wallet sells\n- Volatility: 2.1% daily (low for micro-cap)\n- Organic tokens at this market cap show 23% daily swings\n- Price stability is suspiciously artificial"
      }
    },
    {
      "token_name": "VaultFi",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract VaultFi is ERC20, Ownable {\n    mapping(address => bool) private _authorized;\n    mapping(address => uint256) private _holdTimestamp;\n    address public pair;\n    uint256 public minHoldDuration = 0;\n    bool public antiSnipeEnabled = true;\n    uint256 public launchBlock;\n\n    constructor() ERC20(\"VaultFi\", \"VAU\") Ownable(msg.sender) {\n        _mint(msg.sender, 500_000_000e18);\n        _authorized[msg.sender] = true;\n    }\n\n    function openTrading(address _pair) external onlyOwner {\n        pair = _pair;\n        launchBlock = block.number;\n    }\n\n    function setAntiSnipe(bool _enabled) external onlyOwner {\n        antiSnipeEnabled = _enabled;\n    }\n\n    function setMinHoldDuration(uint256 _duration) external onlyOwner {\n        minHoldDuration = _duration;\n    }\n\n    function authorize(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            _authorized[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (from == pair) {\n            _holdTimestamp[to] = block.timestamp;\n        }\n        if (to == pair && !_authorized[from]) {\n            if (antiSnipeEnabled) {\n                require(\n                    block.timestamp >= _holdTimestamp[from] + minHoldDuration,\n                    \"Hold period not met\"\n                );\n            }\n        }\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for VaultFi:\n- 1652 holders\n- Top wallet: 20% (labeled marketing wallet)\n- 4 wallets have sold \u2014 all pre-launch wallets\n- Post-launch sellers: 0 successful\n- Gini: 0.65\n- Holder velocity: growing 4% daily",
        "contract_functions": "Contract Function Analysis for VaultFi:\n- setMinHoldDuration(uint256) \u2014 owner can set to any value\n- Currently set to 123 days (users can't sell before then)\n- authorize() exempts specific addresses from hold requirement\n- Transfer function checks: if (to == pair && !authorized[from]) revert\n- Buy path has no restrictions",
        "deployer_history": "Deployer History for VaultFi:\n- Wallet age: 54 days\n- 5 previous contracts, 2 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 7 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for VaultFi:\n- Telegram: 1593 members, complaints being deleted\n- Team blames 'DEX integration issues' for sell failures\n- Twitter: 12997 followers, promotional content only\n- Some users report successful sells (these are whitelisted insiders)\n- Website: professional-looking but no team page",
        "similar_contracts": "Similar Contract Analysis for VaultFi:\n- Bytecode: 76% match with 5 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 30/100\n- GoPlus: 3 medium-severity warnings",
        "price_history": "Price History for VaultFi:\n- Mostly upward: +264% since launch\n- Small dips exist (2%) but only from whitelisted wallet sells\n- Volatility: 4.8% daily (low for micro-cap)\n- Organic tokens at this market cap show 10% daily swings\n- Price stability is suspiciously artificial"
      }
    },
    {
      "token_name": "CelestisFi",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract CelestisFi is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"CelestisFi\", \"CELE\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for CelestisFi:\n- 3654 holders\n- Top wallet: 16% (labeled marketing wallet)\n- 4 wallets have sold \u2014 all pre-launch wallets\n- Post-launch sellers: 0 successful\n- Gini: 0.63\n- Holder velocity: growing 7% daily",
        "contract_functions": "Contract Function Analysis for CelestisFi:\n- setMinHoldDuration(uint256) \u2014 owner can set to any value\n- Currently set to 122 days (users can't sell before then)\n- authorize() exempts specific addresses from hold requirement\n- Transfer function checks: if (to == pair && !authorized[from]) revert\n- Buy path has no restrictions",
        "deployer_history": "Deployer History for CelestisFi:\n- Wallet age: 19 days\n- 2 previous contracts, 1 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 12 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for CelestisFi:\n- Telegram: 2933 members, complaints being deleted\n- Team blames 'DEX integration issues' for sell failures\n- Twitter: 12496 followers, promotional content only\n- Some users report successful sells (these are whitelisted insiders)\n- Website: professional-looking but no team page",
        "similar_contracts": "Similar Contract Analysis for CelestisFi:\n- Bytecode: 82% match with 8 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 40/100\n- GoPlus: 3 medium-severity warnings",
        "price_history": "Price History for CelestisFi:\n- Mostly upward: +505% since launch\n- Small dips exist (3%) but only from whitelisted wallet sells\n- Volatility: 4.2% daily (low for micro-cap)\n- Organic tokens at this market cap show 23% daily swings\n- Price stability is suspiciously artificial"
      }
    },
    {
      "token_name": "SynapseProtocol",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract SynapseProtocol is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 200_000_000e18;\n    address public pair;\n    mapping(address => uint256) public lastTransfer;\n    uint256 public cooldownPeriod = 30; // seconds\n    mapping(address => bool) public isWhitelisted;\n    uint256 public maxWalletPercent = 200; // 2%\n\n    constructor() ERC20(\"SynapseProtocol\", \"SYNA\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n        isWhitelisted[msg.sender] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n        isWhitelisted[_pair] = true;\n    }\n\n    function setCooldown(uint256 _seconds) external onlyOwner {\n        cooldownPeriod = _seconds;\n    }\n\n    function setMaxWalletPercent(uint256 _percent) external onlyOwner {\n        require(_percent >= 100, \"Min 1%\");\n        maxWalletPercent = _percent;\n    }\n\n    function whitelist(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            isWhitelisted[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isWhitelisted[from] && !isWhitelisted[to]) {\n            require(\n                block.timestamp >= lastTransfer[from] + cooldownPeriod,\n                \"Cooldown active\"\n            );\n            if (to != pair) {\n                require(\n                    balanceOf(to) + amount <= (totalSupply() * maxWalletPercent) / 10000,\n                    \"Exceeds max wallet\"\n                );\n            }\n        }\n        lastTransfer[from] = block.timestamp;\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for SynapseProtocol:\n- 4241 holders\n- Top wallet: 12% (labeled marketing wallet)\n- 2 wallets have sold \u2014 all pre-launch wallets\n- Post-launch sellers: 0 successful\n- Gini: 0.55\n- Holder velocity: growing 6% daily",
        "contract_functions": "Contract Function Analysis for SynapseProtocol:\n- setMinHoldDuration(uint256) \u2014 owner can set to any value\n- Currently set to 101 days (users can't sell before then)\n- authorize() exempts specific addresses from hold requirement\n- Transfer function checks: if (to == pair && !authorized[from]) revert\n- Buy path has no restrictions",
        "deployer_history": "Deployer History for SynapseProtocol:\n- Wallet age: 36 days\n- 3 previous contracts, 1 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 13 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for SynapseProtocol:\n- Telegram: 4270 members, complaints being deleted\n- Team blames 'DEX integration issues' for sell failures\n- Twitter: 12104 followers, promotional content only\n- Some users report successful sells (these are whitelisted insiders)\n- Website: professional-looking but no team page",
        "similar_contracts": "Similar Contract Analysis for SynapseProtocol:\n- Bytecode: 75% match with 8 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 38/100\n- GoPlus: 2 medium-severity warnings",
        "price_history": "Price History for SynapseProtocol:\n- Mostly upward: +746% since launch\n- Small dips exist (4%) but only from whitelisted wallet sells\n- Volatility: 3.7% daily (low for micro-cap)\n- Organic tokens at this market cap show 24% daily swings\n- Price stability is suspiciously artificial"
      }
    },
    {
      "token_name": "JunctionFi",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Votes.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Permit.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract JunctionFi is ERC20Votes, AccessControl {\n    bytes32 public constant MINTER_ROLE = keccak256(\"MINTER_ROLE\");\n    uint256 public immutable maxSupply;\n    uint256 public immutable mintCap; // max per tx\n\n    constructor(uint256 _maxSupply)\n        ERC20(\"JunctionFi\", \"JUNC\")\n        ERC20Permit(\"JunctionFi\")\n    {\n        maxSupply = _maxSupply;\n        mintCap = _maxSupply / 100;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _mint(msg.sender, _maxSupply / 5); // 20% initial\n    }\n\n    function mint(address to, uint256 amount) external onlyRole(MINTER_ROLE) {\n        require(amount <= mintCap, \"Exceeds mint cap\");\n        require(totalSupply() + amount <= maxSupply, \"Exceeds max supply\");\n        _mint(to, amount);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for JunctionFi:\n- 80,085 holders\n- Top wallet: 6.6% (team vesting, locked 12mo)\n- 9 whale wallets hold >0.5% each\n- Gini: 0.58 (moderate)\n- Smart money: 10 Nansen-labeled fund wallets",
        "contract_functions": "Contract Function Analysis for JunctionFi:\n- ERC20Votes + governance\n- Mint capped at totalSupply/100 per tx, maxSupply enforced\n- Minter role held by governance timelock (48h delay)\n- No functions transfer tokens/ETH to specific addresses\n- All admin behind governance timelock",
        "deployer_history": "Deployer History for JunctionFi:\n- Wallet age: 480 days\n- Pseudonymous but long reputation in DeFi\n- 5 contracts, clean track record\n- Active in governance of Aave\n- Funded via known DAO treasury",
        "social_signals": "Social Analysis for JunctionFi:\n- Twitter: 41953 followers, organic growth\n- Discord: 13650 members\n- GitHub: active development, weekly commits\n- Team: pseudonymous but known in ecosystem for 2 years\n- Partnerships: LayerZero integration",
        "similar_contracts": "Similar Contract Analysis for JunctionFi:\n- Clean architecture, audited patterns\n- TokenSniffer: 86/100\n- GoPlus: 1 informational finding (non-critical)\n- Similar to 29 legitimate DeFi protocols\n- No known scam contract matches",
        "price_history": "Price History for JunctionFi:\n- 260 day history\n- Correlated with ETH price (beta: 1.2)\n- 5 significant drawdowns (39%), all recovered\n- Volume follows news cycle (organic)\n- No single-block large trades dominating price action"
      }
    },
    {
      "token_name": "CircuitFi",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface IRouter {\n    function swapExactTokensForETH(uint256, uint256, address[] calldata, address, uint256) external;\n    function WETH() external view returns (address);\n}\n\ncontract CircuitFi is ERC20, Ownable {\n    IRouter public router;\n    address public pair;\n    uint256 public maxWalletSize = 2_000_000e18;\n    mapping(address => bool) private _isExcluded;\n    bool private _swapping;\n\n    constructor(address _router) ERC20(\"CircuitFi\", \"CIRT\") Ownable(msg.sender) {\n        router = IRouter(_router);\n        _mint(msg.sender, 100_000_000e18);\n        _isExcluded[msg.sender] = true;\n        _isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function setMaxWallet(uint256 _max) external onlyOwner {\n        require(_max >= 1_000_000e18, \"Too low\");\n        maxWalletSize = _max;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!_isExcluded[to] && to != pair) {\n            require(balanceOf(to) + amount <= maxWalletSize, \"Max wallet\");\n        }\n        super._update(from, to, amount);\n    }\n\n    function clearStuckBalance() external onlyOwner {\n        payable(owner()).transfer(address(this).balance);\n    }\n\n    function clearStuckTokens(address token) external onlyOwner {\n        uint256 bal = IERC20(token).balanceOf(address(this));\n        IERC20(token).transfer(owner(), bal);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for CircuitFi:\n- Top wallet holds 63% of supply (labeled as 'team vesting')\n- 441 holders, growing slowly\n- Gini coefficient: 0.84\n- Top wallet has no timelock or vesting contract\n- Second largest wallet (5%) is DEX pair\n- Wallet age: 11 days",
        "contract_functions": "Contract Function Analysis for CircuitFi:\n- Owner-only: setRouter(address), rebalanceTreasury(uint256)\n- Functions disguised as maintenance/migration utilities\n- setRouter(address) can send contract's entire ETH balance to arbitrary address\n- No timelock on admin functions\n- Contract verified but comments are misleading",
        "deployer_history": "Deployer History for CircuitFi:\n- Wallet age: 46 days\n- 3 previous contracts, 1 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 9 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for CircuitFi:\n- Twitter: 16865 followers, 59% bots\n- Telegram: 1514 members, moderation stopped 5 days ago\n- Website: template site, stock images\n- Whitepaper: AI-generated, no technical depth\n- Team photos: reverse image search matches stock photos",
        "similar_contracts": "Similar Contract Analysis for CircuitFi:\n- Bytecode: 76% match with 3 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 28/100\n- GoPlus: 2 medium-severity warnings",
        "price_history": "Price History for CircuitFi:\n- Gradual rise over 10 days: $0.036 \u2192 $2.80\n- Multiple smaller dips (14%) with recovery (manufactured)\n- Final drop: -85% in 1 hours\n- Pattern: slow build with fake recoveries \u2192 sudden rug\n- Post-rug: flatline at near-zero"
      }
    },
    {
      "token_name": "OrbitLend",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Votes.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Permit.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract OrbitLend is ERC20Votes, AccessControl {\n    bytes32 public constant MINTER_ROLE = keccak256(\"MINTER_ROLE\");\n    uint256 public immutable maxSupply;\n    uint256 public immutable mintCap; // max per tx\n\n    constructor(uint256 _maxSupply)\n        ERC20(\"OrbitLend\", \"ORBI\")\n        ERC20Permit(\"OrbitLend\")\n    {\n        maxSupply = _maxSupply;\n        mintCap = _maxSupply / 100;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _mint(msg.sender, _maxSupply / 5); // 20% initial\n    }\n\n    function mint(address to, uint256 amount) external onlyRole(MINTER_ROLE) {\n        require(amount <= mintCap, \"Exceeds mint cap\");\n        require(totalSupply() + amount <= maxSupply, \"Exceeds max supply\");\n        _mint(to, amount);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for OrbitLend:\n- 51,780 holders\n- Top wallet: 6.6% (team vesting, locked 12mo)\n- 8 whale wallets hold >0.6% each\n- Gini: 0.54 (moderate)\n- Smart money: 5 Nansen-labeled fund wallets",
        "contract_functions": "Contract Function Analysis for OrbitLend:\n- ERC20Votes + governance\n- Mint capped at totalSupply/100 per tx, maxSupply enforced\n- Minter role held by governance timelock (48h delay)\n- No functions transfer tokens/ETH to specific addresses\n- All admin behind 3/5 multisig",
        "deployer_history": "Deployer History for OrbitLend:\n- Wallet age: 578 days\n- Pseudonymous but long reputation in DeFi\n- 3 contracts, clean track record\n- Active in governance of Uniswap\n- Funded via known DAO treasury",
        "social_signals": "Social Analysis for OrbitLend:\n- Twitter: 34311 followers, organic growth\n- Discord: 9674 members\n- GitHub: active development, weekly commits\n- Team: pseudonymous but known in ecosystem for 2 years\n- Partnerships: The Graph integration",
        "similar_contracts": "Similar Contract Analysis for OrbitLend:\n- Clean architecture, audited patterns\n- TokenSniffer: 82/100\n- GoPlus: 1 informational finding (non-critical)\n- Similar to 13 legitimate DeFi protocols\n- No known scam contract matches",
        "price_history": "Price History for OrbitLend:\n- 293 day history\n- Correlated with ETH price (beta: 1.4)\n- 7 significant drawdowns (20%), all recovered\n- Volume follows news cycle (organic)\n- No single-block large trades dominating price action"
      }
    },
    {
      "token_name": "KryptonDAO",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract KryptonDAO is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"KryptonDAO\", \"KRYP\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for KryptonDAO:\n- 23 holders, 11 are active traders\n- Cluster analysis: 4 distinct wallet groups, each from one source\n- Some wallets hold dust amounts of other tokens (cover)\n- Wallet creation spread over 5 days\n- Net token flow between top wallets: approximately zero",
        "contract_functions": "Contract Function Analysis for KryptonDAO:\n- addMarketMaker(address) \u2014 21 registered\n- fundMarketMakers(uint256[]) \u2014 bulk fund from owner\n- rebalanceAllocations() \u2014 shuffle tokens between market makers\n- Functions labeled as 'liquidity management'\n- No external DEX interaction, all transfers are internal",
        "deployer_history": "Deployer History for KryptonDAO:\n- 8 contracts deployed, spread across 3 chains\n- Contracts share common library code (batch operations)\n- Deployer funds market maker wallets through intermediaries\n- Some previous tokens are still active with inflated volume\n- No audit history on any deployment",
        "social_signals": "Social Analysis for KryptonDAO:\n- Promoted as 'fastest growing DeFi protocol'\n- Twitter: 5688 followers with purchased engagement\n- Discord: exists but 72% of messages are from bots\n- Medium blog: repurposed AI content about 'revolutionizing DeFi'\n- No developer community or GitHub activity",
        "similar_contracts": "Similar Contract Analysis for KryptonDAO:\n- Contract shares market maker infrastructure with 3 tokens\n- Batch operation pattern seen in known wash trading setups\n- TokenSniffer: 50/100\n- Similar contracts spread across 2 chains",
        "price_history": "Price History for KryptonDAO:\n- Appears to have normal volatility (6% daily)\n- BUT: price always returns to same level within 14 hours\n- Mean-reversion too perfect \u2014 natural markets have drift\n- Trade sizes suspiciously uniform\n- Volume spikes at exactly same time each day"
      }
    },
    {
      "token_name": "ZenithToken",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface IRouter {\n    function swapExactTokensForETH(uint256, uint256, address[] calldata, address, uint256) external;\n    function WETH() external view returns (address);\n}\n\ncontract ZenithToken is ERC20, Ownable {\n    IRouter public router;\n    address public pair;\n    uint256 public maxWalletSize = 2_000_000e18;\n    mapping(address => bool) private _isExcluded;\n    bool private _swapping;\n\n    constructor(address _router) ERC20(\"ZenithToken\", \"ZENT\") Ownable(msg.sender) {\n        router = IRouter(_router);\n        _mint(msg.sender, 100_000_000e18);\n        _isExcluded[msg.sender] = true;\n        _isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function setMaxWallet(uint256 _max) external onlyOwner {\n        require(_max >= 1_000_000e18, \"Too low\");\n        maxWalletSize = _max;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!_isExcluded[to] && to != pair) {\n            require(balanceOf(to) + amount <= maxWalletSize, \"Max wallet\");\n        }\n        super._update(from, to, amount);\n    }\n\n    function clearStuckBalance() external onlyOwner {\n        payable(owner()).transfer(address(this).balance);\n    }\n\n    function clearStuckTokens(address token) external onlyOwner {\n        uint256 bal = IERC20(token).balanceOf(address(this));\n        IERC20(token).transfer(owner(), bal);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for ZenithToken:\n- Top wallet holds 68% of supply (labeled as 'team vesting')\n- 718 holders, growing slowly\n- Gini coefficient: 0.88\n- Top wallet has no timelock or vesting contract\n- Second largest wallet (8%) is DEX pair\n- Wallet age: 6 days",
        "contract_functions": "Contract Function Analysis for ZenithToken:\n- Owner-only: recoverTokens(address,uint256), migrateV2(address)\n- Functions disguised as maintenance/migration utilities\n- recoverTokens(address,uint256) can send contract's entire ETH balance to arbitrary address\n- No timelock on admin functions\n- Contract verified but comments are misleading",
        "deployer_history": "Deployer History for ZenithToken:\n- Wallet age: 19 days\n- 5 previous contracts, 1 inactive (abandoned)\n- Funded via cross-chain bridge\n- Twitter linked but account is 9 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for ZenithToken:\n- Twitter: 14875 followers, 33% bots\n- Telegram: 1865 members, moderation stopped 2 days ago\n- Website: template site, stock images\n- Whitepaper: AI-generated, no technical depth\n- Team photos: reverse image search matches stock photos",
        "similar_contracts": "Similar Contract Analysis for ZenithToken:\n- Bytecode: 79% match with 7 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 31/100\n- GoPlus: 3 medium-severity warnings",
        "price_history": "Price History for ZenithToken:\n- Gradual rise over 12 days: $0.042 \u2192 $4.93\n- Multiple smaller dips (10%) with recovery (manufactured)\n- Final drop: -91% in 4 hours\n- Pattern: slow build with fake recoveries \u2192 sudden rug\n- Post-rug: flatline at near-zero"
      }
    },
    {
      "token_name": "TitanBridge",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract TitanBridge is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"TitanBridge\", \"TITA\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for TitanBridge:\n- 15 holders, 14 are active traders\n- Cluster analysis: 2 distinct wallet groups, each from one source\n- Some wallets hold dust amounts of other tokens (cover)\n- Wallet creation spread over 10 days\n- Net token flow between top wallets: approximately zero",
        "contract_functions": "Contract Function Analysis for TitanBridge:\n- addMarketMaker(address) \u2014 17 registered\n- fundMarketMakers(uint256[]) \u2014 bulk fund from owner\n- rebalanceAllocations() \u2014 shuffle tokens between market makers\n- Functions labeled as 'liquidity management'\n- No external DEX interaction, all transfers are internal",
        "deployer_history": "Deployer History for TitanBridge:\n- 5 contracts deployed, spread across 2 chains\n- Contracts share common library code (batch operations)\n- Deployer funds market maker wallets through intermediaries\n- Some previous tokens are still active with inflated volume\n- No audit history on any deployment",
        "social_signals": "Social Analysis for TitanBridge:\n- Promoted as 'fastest growing DeFi protocol'\n- Twitter: 5082 followers with purchased engagement\n- Discord: exists but 80% of messages are from bots\n- Medium blog: repurposed AI content about 'revolutionizing DeFi'\n- No developer community or GitHub activity",
        "similar_contracts": "Similar Contract Analysis for TitanBridge:\n- Contract shares market maker infrastructure with 7 tokens\n- Batch operation pattern seen in known wash trading setups\n- TokenSniffer: 49/100\n- Similar contracts spread across 2 chains",
        "price_history": "Price History for TitanBridge:\n- Appears to have normal volatility (8% daily)\n- BUT: price always returns to same level within 11 hours\n- Mean-reversion too perfect \u2014 natural markets have drift\n- Trade sizes suspiciously uniform\n- Volume spikes at exactly same time each day"
      }
    },
    {
      "token_name": "LunarStake",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract LunarStake is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"LunarStake\", \"LUNA\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for LunarStake:\n- 15 holders, 11 are active traders\n- Cluster analysis: 4 distinct wallet groups, each from one source\n- Some wallets hold dust amounts of other tokens (cover)\n- Wallet creation spread over 7 days\n- Net token flow between top wallets: approximately zero",
        "contract_functions": "Contract Function Analysis for LunarStake:\n- addMarketMaker(address) \u2014 16 registered\n- fundMarketMakers(uint256[]) \u2014 bulk fund from owner\n- rebalanceAllocations() \u2014 shuffle tokens between market makers\n- Functions labeled as 'liquidity management'\n- No external DEX interaction, all transfers are internal",
        "deployer_history": "Deployer History for LunarStake:\n- 6 contracts deployed, spread across 3 chains\n- Contracts share common library code (batch operations)\n- Deployer funds market maker wallets through intermediaries\n- Some previous tokens are still active with inflated volume\n- No audit history on any deployment",
        "social_signals": "Social Analysis for LunarStake:\n- Promoted as 'fastest growing DeFi protocol'\n- Twitter: 6854 followers with purchased engagement\n- Discord: exists but 61% of messages are from bots\n- Medium blog: repurposed AI content about 'revolutionizing DeFi'\n- No developer community or GitHub activity",
        "similar_contracts": "Similar Contract Analysis for LunarStake:\n- Contract shares market maker infrastructure with 4 tokens\n- Batch operation pattern seen in known wash trading setups\n- TokenSniffer: 37/100\n- Similar contracts spread across 3 chains",
        "price_history": "Price History for LunarStake:\n- Appears to have normal volatility (4% daily)\n- BUT: price always returns to same level within 17 hours\n- Mean-reversion too perfect \u2014 natural markets have drift\n- Trade sizes suspiciously uniform\n- Volume spikes at exactly same time each day"
      }
    },
    {
      "token_name": "VelodromeV3",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Votes.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Permit.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract VelodromeV3 is ERC20Votes, AccessControl {\n    bytes32 public constant MINTER_ROLE = keccak256(\"MINTER_ROLE\");\n    uint256 public immutable maxSupply;\n    uint256 public immutable mintCap; // max per tx\n\n    constructor(uint256 _maxSupply)\n        ERC20(\"VelodromeV3\", \"VELO\")\n        ERC20Permit(\"VelodromeV3\")\n    {\n        maxSupply = _maxSupply;\n        mintCap = _maxSupply / 100;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _mint(msg.sender, _maxSupply / 5); // 20% initial\n    }\n\n    function mint(address to, uint256 amount) external onlyRole(MINTER_ROLE) {\n        require(amount <= mintCap, \"Exceeds mint cap\");\n        require(totalSupply() + amount <= maxSupply, \"Exceeds max supply\");\n        _mint(to, amount);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for VelodromeV3:\n- 51,792 holders\n- Top wallet: 6.0% (team vesting, locked 12mo)\n- 5 whale wallets hold >1.2% each\n- Gini: 0.58 (moderate)\n- Smart money: 4 Nansen-labeled fund wallets",
        "contract_functions": "Contract Function Analysis for VelodromeV3:\n- ERC20Votes + governance\n- Mint capped at totalSupply/100 per tx, maxSupply enforced\n- Minter role held by governance timelock (48h delay)\n- No functions transfer tokens/ETH to specific addresses\n- All admin behind governance timelock",
        "deployer_history": "Deployer History for VelodromeV3:\n- Wallet age: 1359 days\n- Pseudonymous but long reputation in DeFi\n- 9 contracts, clean track record\n- Active in governance of Compound\n- Funded via known DAO treasury",
        "social_signals": "Social Analysis for VelodromeV3:\n- Twitter: 23073 followers, organic growth\n- Discord: 6714 members\n- GitHub: active development, weekly commits\n- Team: pseudonymous but known in ecosystem for 2 years\n- Partnerships: LayerZero integration",
        "similar_contracts": "Similar Contract Analysis for VelodromeV3:\n- Clean architecture, audited patterns\n- TokenSniffer: 85/100\n- GoPlus: 1 informational finding (non-critical)\n- Similar to 15 legitimate DeFi protocols\n- No known scam contract matches",
        "price_history": "Price History for VelodromeV3:\n- 310 day history\n- Correlated with ETH price (beta: 1.3)\n- 8 significant drawdowns (27%), all recovered\n- Volume follows news cycle (organic)\n- No single-block large trades dominating price action"
      }
    },
    {
      "token_name": "ZionBridge",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract ZionBridge is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"ZionBridge\", \"ZION\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for ZionBridge:\n- Top wallet holds 71% of supply (labeled as 'team vesting')\n- 586 holders, growing slowly\n- Gini coefficient: 0.82\n- Top wallet has no timelock or vesting contract\n- Second largest wallet (6%) is DEX pair\n- Wallet age: 18 days",
        "contract_functions": "Contract Function Analysis for ZionBridge:\n- Owner-only: rebalanceTreasury(uint256), migrateV2(address)\n- Functions disguised as maintenance/migration utilities\n- rebalanceTreasury(uint256) can send contract's entire ETH balance to arbitrary address\n- No timelock on admin functions\n- Contract verified but comments are misleading",
        "deployer_history": "Deployer History for ZionBridge:\n- Wallet age: 41 days\n- 4 previous contracts, 1 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 9 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for ZionBridge:\n- Twitter: 17220 followers, 48% bots\n- Telegram: 2118 members, moderation stopped 2 days ago\n- Website: template site, stock images\n- Whitepaper: AI-generated, no technical depth\n- Team photos: reverse image search matches stock photos",
        "similar_contracts": "Similar Contract Analysis for ZionBridge:\n- Bytecode: 82% match with 4 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 35/100\n- GoPlus: 4 medium-severity warnings",
        "price_history": "Price History for ZionBridge:\n- Gradual rise over 7 days: $0.081 \u2192 $4.48\n- Multiple smaller dips (13%) with recovery (manufactured)\n- Final drop: -98% in 1 hours\n- Pattern: slow build with fake recoveries \u2192 sudden rug\n- Post-rug: flatline at near-zero"
      }
    },
    {
      "token_name": "WindsorProtocol",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract WindsorProtocol is ERC20, Ownable {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public immutable vestingStart;\n    uint256 public constant VESTING_DURATION = 365 days;\n    uint256 public teamAllocation;\n    uint256 public teamClaimed;\n\n    constructor() ERC20(\"WindsorProtocol\", \"WIND\") Ownable(msg.sender) {\n        vestingStart = block.timestamp;\n        teamAllocation = MAX_SUPPLY * 15 / 100; // 15% team\n        _mint(address(this), teamAllocation);\n        _mint(msg.sender, MAX_SUPPLY - teamAllocation); // 85% to LP/community\n    }\n\n    function claimVested() external onlyOwner {\n        uint256 elapsed = block.timestamp - vestingStart;\n        if (elapsed > VESTING_DURATION) elapsed = VESTING_DURATION;\n        uint256 totalVested = (teamAllocation * elapsed) / VESTING_DURATION;\n        uint256 claimable = totalVested - teamClaimed;\n        require(claimable > 0, \"Nothing to claim\");\n        teamClaimed += claimable;\n        _transfer(address(this), owner(), claimable);\n    }\n\n    function renounceOwnership() public override onlyOwner {\n        super.renounceOwnership();\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for WindsorProtocol:\n- 121,837 holders\n- Top wallet: 3.5% (team vesting, locked 12mo)\n- 14 whale wallets hold >1.2% each\n- Gini: 0.54 (moderate)\n- Smart money: 5 Nansen-labeled fund wallets",
        "contract_functions": "Contract Function Analysis for WindsorProtocol:\n- ERC20Votes + governance\n- Mint capped at totalSupply/100 per tx, maxSupply enforced\n- Minter role held by governance timelock (48h delay)\n- No functions transfer tokens/ETH to specific addresses\n- All admin behind 3/5 multisig",
        "deployer_history": "Deployer History for WindsorProtocol:\n- Wallet age: 886 days\n- Pseudonymous but long reputation in DeFi\n- 7 contracts, clean track record\n- Active in governance of Uniswap\n- Funded via known DAO treasury",
        "social_signals": "Social Analysis for WindsorProtocol:\n- Twitter: 38595 followers, organic growth\n- Discord: 8715 members\n- GitHub: active development, weekly commits\n- Team: pseudonymous but known in ecosystem for 3 years\n- Partnerships: The Graph integration",
        "similar_contracts": "Similar Contract Analysis for WindsorProtocol:\n- Clean architecture, audited patterns\n- TokenSniffer: 82/100\n- GoPlus: 1 informational finding (non-critical)\n- Similar to 20 legitimate DeFi protocols\n- No known scam contract matches",
        "price_history": "Price History for WindsorProtocol:\n- 159 day history\n- Correlated with ETH price (beta: 1.5)\n- 5 significant drawdowns (36%), all recovered\n- Volume follows news cycle (organic)\n- No single-block large trades dominating price action"
      }
    },
    {
      "token_name": "UmbraDAO",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract UmbraDAO is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"UmbraDAO\", \"UMB\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for UmbraDAO:\n- 27 holders, 27 are active traders\n- Cluster analysis: 2 distinct wallet groups, each from one source\n- Some wallets hold dust amounts of other tokens (cover)\n- Wallet creation spread over 11 days\n- Net token flow between top wallets: approximately zero",
        "contract_functions": "Contract Function Analysis for UmbraDAO:\n- addMarketMaker(address) \u2014 13 registered\n- fundMarketMakers(uint256[]) \u2014 bulk fund from owner\n- rebalanceAllocations() \u2014 shuffle tokens between market makers\n- Functions labeled as 'liquidity management'\n- No external DEX interaction, all transfers are internal",
        "deployer_history": "Deployer History for UmbraDAO:\n- 8 contracts deployed, spread across 2 chains\n- Contracts share common library code (batch operations)\n- Deployer funds market maker wallets through intermediaries\n- Some previous tokens are still active with inflated volume\n- No audit history on any deployment",
        "social_signals": "Social Analysis for UmbraDAO:\n- Promoted as 'fastest growing DeFi protocol'\n- Twitter: 4412 followers with purchased engagement\n- Discord: exists but 64% of messages are from bots\n- Medium blog: repurposed AI content about 'revolutionizing DeFi'\n- No developer community or GitHub activity",
        "similar_contracts": "Similar Contract Analysis for UmbraDAO:\n- Contract shares market maker infrastructure with 8 tokens\n- Batch operation pattern seen in known wash trading setups\n- TokenSniffer: 35/100\n- Similar contracts spread across 3 chains",
        "price_history": "Price History for UmbraDAO:\n- Appears to have normal volatility (5% daily)\n- BUT: price always returns to same level within 15 hours\n- Mean-reversion too perfect \u2014 natural markets have drift\n- Trade sizes suspiciously uniform\n- Volume spikes at exactly same time each day"
      }
    },
    {
      "token_name": "VelodromeV3",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract VelodromeV3 is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"VelodromeV3\", \"VELO\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for VelodromeV3:\n- Top wallet holds 74% of supply (labeled as 'team vesting')\n- 382 holders, growing slowly\n- Gini coefficient: 0.84\n- Top wallet has no timelock or vesting contract\n- Second largest wallet (5%) is DEX pair\n- Wallet age: 20 days",
        "contract_functions": "Contract Function Analysis for VelodromeV3:\n- Owner-only: rebalanceTreasury(uint256), setRouter(address)\n- Functions disguised as maintenance/migration utilities\n- rebalanceTreasury(uint256) can send contract's entire ETH balance to arbitrary address\n- No timelock on admin functions\n- Contract verified but comments are misleading",
        "deployer_history": "Deployer History for VelodromeV3:\n- Wallet age: 46 days\n- 5 previous contracts, 2 inactive (abandoned)\n- Funded via cross-chain bridge\n- Twitter linked but account is 1 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for VelodromeV3:\n- Twitter: 8268 followers, 37% bots\n- Telegram: 982 members, moderation stopped 6 days ago\n- Website: template site, stock images\n- Whitepaper: AI-generated, no technical depth\n- Team photos: reverse image search matches stock photos",
        "similar_contracts": "Similar Contract Analysis for VelodromeV3:\n- Bytecode: 75% match with 4 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 33/100\n- GoPlus: 2 medium-severity warnings",
        "price_history": "Price History for VelodromeV3:\n- Gradual rise over 17 days: $0.049 \u2192 $4.39\n- Multiple smaller dips (17%) with recovery (manufactured)\n- Final drop: -89% in 3 hours\n- Pattern: slow build with fake recoveries \u2192 sudden rug\n- Post-rug: flatline at near-zero"
      }
    },
    {
      "token_name": "TridentSwap",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/security/Pausable.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract TridentSwap is ERC20, Pausable, Ownable {\n    uint256 public constant INITIAL_SUPPLY = 50_000_000e18;\n    address public treasuryWallet;\n    uint256 public withdrawalTimelock = 48 hours;\n    mapping(address => uint256) public lastWithdrawal;\n    uint256 public totalStaked;\n    mapping(address => uint256) public stakedBalance;\n\n    constructor(address _treasury) ERC20(\"TridentSwap\", \"TRID\") Ownable(msg.sender) {\n        treasuryWallet = _treasury;\n        _mint(address(this), INITIAL_SUPPLY);\n    }\n\n    function stake(uint256 amount) external whenNotPaused {\n        _transfer(msg.sender, address(this), amount);\n        stakedBalance[msg.sender] += amount;\n        totalStaked += amount;\n    }\n\n    function unstake(uint256 amount) external whenNotPaused {\n        require(stakedBalance[msg.sender] >= amount, \"Insufficient stake\");\n        require(block.timestamp >= lastWithdrawal[msg.sender] + withdrawalTimelock, \"Timelock\");\n        stakedBalance[msg.sender] -= amount;\n        totalStaked -= amount;\n        lastWithdrawal[msg.sender] = block.timestamp;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function setWithdrawalTimelock(uint256 _timelock) external onlyOwner {\n        withdrawalTimelock = _timelock;\n    }\n\n    /// @dev Admin function to rebalance treasury allocation\n    function rebalanceTreasury(uint256 amount) external onlyOwner {\n        _transfer(address(this), treasuryWallet, amount);\n    }\n\n    function pause() external onlyOwner { _pause(); }\n    function unpause() external onlyOwner { _unpause(); }\n\n    receive() external payable {}\n\n    function withdrawETH() external onlyOwner {\n        payable(treasuryWallet).transfer(address(this).balance);\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for TridentSwap:\n- Top wallet holds 65% of supply (labeled as 'team vesting')\n- 540 holders, growing slowly\n- Gini coefficient: 0.78\n- Top wallet has no timelock or vesting contract\n- Second largest wallet (5%) is DEX pair\n- Wallet age: 8 days",
        "contract_functions": "Contract Function Analysis for TridentSwap:\n- Owner-only: setRouter(address), rebalanceTreasury(uint256)\n- Functions disguised as maintenance/migration utilities\n- setRouter(address) can send contract's entire ETH balance to arbitrary address\n- No timelock on admin functions\n- Contract verified but comments are misleading",
        "deployer_history": "Deployer History for TridentSwap:\n- Wallet age: 39 days\n- 2 previous contracts, 2 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 10 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for TridentSwap:\n- Twitter: 19756 followers, 43% bots\n- Telegram: 1609 members, moderation stopped 6 days ago\n- Website: template site, stock images\n- Whitepaper: AI-generated, no technical depth\n- Team photos: reverse image search matches stock photos",
        "similar_contracts": "Similar Contract Analysis for TridentSwap:\n- Bytecode: 85% match with 3 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 27/100\n- GoPlus: 4 medium-severity warnings",
        "price_history": "Price History for TridentSwap:\n- Gradual rise over 21 days: $0.056 \u2192 $3.74\n- Multiple smaller dips (18%) with recovery (manufactured)\n- Final drop: -85% in 2 hours\n- Pattern: slow build with fake recoveries \u2192 sudden rug\n- Post-rug: flatline at near-zero"
      }
    },
    {
      "token_name": "CosmicLend",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract CosmicLend is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"CosmicLend\", \"COSM\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for CosmicLend:\n- Top wallet holds 69% of supply (labeled as 'team vesting')\n- 277 holders, growing slowly\n- Gini coefficient: 0.87\n- Top wallet has no timelock or vesting contract\n- Second largest wallet (7%) is DEX pair\n- Wallet age: 3 days",
        "contract_functions": "Contract Function Analysis for CosmicLend:\n- Owner-only: recoverTokens(address,uint256), migrateV2(address)\n- Functions disguised as maintenance/migration utilities\n- recoverTokens(address,uint256) can send contract's entire ETH balance to arbitrary address\n- No timelock on admin functions\n- Contract verified but comments are misleading",
        "deployer_history": "Deployer History for CosmicLend:\n- Wallet age: 27 days\n- 4 previous contracts, 2 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 8 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for CosmicLend:\n- Twitter: 5064 followers, 60% bots\n- Telegram: 2758 members, moderation stopped 2 days ago\n- Website: template site, stock images\n- Whitepaper: AI-generated, no technical depth\n- Team photos: reverse image search matches stock photos",
        "similar_contracts": "Similar Contract Analysis for CosmicLend:\n- Bytecode: 77% match with 3 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 39/100\n- GoPlus: 4 medium-severity warnings",
        "price_history": "Price History for CosmicLend:\n- Gradual rise over 16 days: $0.031 \u2192 $3.92\n- Multiple smaller dips (20%) with recovery (manufactured)\n- Final drop: -88% in 3 hours\n- Pattern: slow build with fake recoveries \u2192 sudden rug\n- Post-rug: flatline at near-zero"
      }
    },
    {
      "token_name": "ZionBridge",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\n\ncontract ZionBridge is ERC20 {\n    constructor(address treasury, address community, address lp) ERC20(\"ZionBridge\", \"ZION\") {\n        _mint(treasury, 30_000_000e18);   // 30% treasury (multisig)\n        _mint(community, 50_000_000e18);  // 50% community airdrop\n        _mint(lp, 20_000_000e18);         // 20% initial liquidity\n    }\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for ZionBridge:\n- 138,575 holders\n- Top wallet: 4.9% (team vesting, locked 12mo)\n- 6 whale wallets hold >1.0% each\n- Gini: 0.57 (moderate)\n- Smart money: 4 Nansen-labeled fund wallets",
        "contract_functions": "Contract Function Analysis for ZionBridge:\n- ERC20Votes + governance\n- Mint capped at totalSupply/100 per tx, maxSupply enforced\n- Minter role held by governance timelock (48h delay)\n- No functions transfer tokens/ETH to specific addresses\n- All admin behind DAO vote",
        "deployer_history": "Deployer History for ZionBridge:\n- Wallet age: 1202 days\n- Pseudonymous but long reputation in DeFi\n- 8 contracts, clean track record\n- Active in governance of Uniswap\n- Funded via known DAO treasury",
        "social_signals": "Social Analysis for ZionBridge:\n- Twitter: 48468 followers, organic growth\n- Discord: 13278 members\n- GitHub: active development, weekly commits\n- Team: pseudonymous but known in ecosystem for 5 years\n- Partnerships: Chainlink integration",
        "similar_contracts": "Similar Contract Analysis for ZionBridge:\n- Clean architecture, audited patterns\n- TokenSniffer: 92/100\n- GoPlus: 1 informational finding (non-critical)\n- Similar to 17 legitimate DeFi protocols\n- No known scam contract matches",
        "price_history": "Price History for ZionBridge:\n- 109 day history\n- Correlated with ETH price (beta: 1.4)\n- 3 significant drawdowns (20%), all recovered\n- Volume follows news cycle (organic)\n- No single-block large trades dominating price action"
      }
    },
    {
      "token_name": "JadeProtocol",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract JadeProtocol is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"JadeProtocol\", \"JADE\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "medium",
      "investigations": {
        "holder_distribution": "Holder Analysis for JadeProtocol:\n- 4606 holders\n- Top wallet: 24% (labeled marketing wallet)\n- 2 wallets have sold \u2014 all pre-launch wallets\n- Post-launch sellers: 0 successful\n- Gini: 0.59\n- Holder velocity: growing 5% daily",
        "contract_functions": "Contract Function Analysis for JadeProtocol:\n- setMinHoldDuration(uint256) \u2014 owner can set to any value\n- Currently set to 139 days (users can't sell before then)\n- authorize() exempts specific addresses from hold requirement\n- Transfer function checks: if (to == pair && !authorized[from]) revert\n- Buy path has no restrictions",
        "deployer_history": "Deployer History for JadeProtocol:\n- Wallet age: 33 days\n- 3 previous contracts, 1 inactive (abandoned)\n- Funded via new CEX withdrawal\n- Twitter linked but account is 9 days old\n- Previous contracts have similar bytecode patterns",
        "social_signals": "Social Analysis for JadeProtocol:\n- Telegram: 1860 members, complaints being deleted\n- Team blames 'DEX integration issues' for sell failures\n- Twitter: 10767 followers, promotional content only\n- Some users report successful sells (these are whitelisted insiders)\n- Website: professional-looking but no team page",
        "similar_contracts": "Similar Contract Analysis for JadeProtocol:\n- Bytecode: 84% match with 5 flagged contracts\n- Modified version of known template (custom variable names)\n- Core drain/restriction logic preserved\n- TokenSniffer: 40/100\n- GoPlus: 3 medium-severity warnings",
        "price_history": "Price History for JadeProtocol:\n- Mostly upward: +794% since launch\n- Small dips exist (3%) but only from whitelisted wallet sells\n- Volatility: 2.7% daily (low for micro-cap)\n- Organic tokens at this market cap show 18% daily swings\n- Price stability is suspiciously artificial"
      }
    },
    {
      "token_name": "ArcticDEX",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract ArcticDEX is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"ArcticDEX\", \"ARCT\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for ArcticDEX:\n- Top wallet holds 28% (claims to be DAO treasury)\n- 866 holders across 4 chains\n- Gini: 0.65 (moderate concentration)\n- Top wallet is an EOA, not a multisig\n- Cluster analysis: top 3 wallets funded from same CEX withdrawal\n- Distribution looks organic at surface level",
        "contract_functions": "Contract Function Analysis for ArcticDEX:\n- Standard ERC20 with staking module\n- Owner-only: setRewardRate(), toggleStaking(), withdrawETH()\n- withdrawETH() sends to treasury (which is owner-controlled EOA)\n- setWithdrawalTimelock(0) can disable all withdrawal delays\n- Subtle: rebalanceTreasury() moves staked tokens without user consent\n- Audit: self-published, not from recognized firm",
        "deployer_history": "Deployer History for ArcticDEX:\n- Wallet age: 364 days (looks established)\n- 11 previous contracts, none flagged\n- Funded via legitimate CEX (Kraken)\n- BUT: wallet was purchased/acquired (activity gap of 69 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: arcticdex.eth (registered 8 days ago)",
        "social_signals": "Social Analysis for ArcticDEX:\n- Twitter: 39452 followers, appears active\n- Telegram: 4561 members, active discussion\n- BUT: engagement pattern is bot-like (same accounts reply within seconds)\n- Website: professional, but WHOIS registered 9 days ago\n- Team: LinkedIn profiles exist but have thin history\n- Looks legitimate until you check engagement authenticity",
        "similar_contracts": "Similar Contract Analysis for ArcticDEX:\n- Bytecode: 47% match (heavily modified from template)\n- Original template linked to 2 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 57/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for ArcticDEX:\n- 65 day history, organic-looking growth\n- Multiple natural-looking corrections (28% pullbacks)\n- Steady decline in last 10 days (-42%)\n- NOT a sudden crash \u2014 slow bleed as deployer sells in small batches\n- Looks like market downturn, but coincides with team wallet movements\n- Hard to distinguish from legitimate bear market without on-chain data"
      }
    },
    {
      "token_name": "LatticeDAO",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract LatticeDAO is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"LatticeDAO\", \"LATT\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for LatticeDAO:\n- Top wallet holds 38% (claims to be DAO treasury)\n- 1660 holders across 4 chains\n- Gini: 0.73 (moderate concentration)\n- Top wallet is an EOA, not a multisig\n- Cluster analysis: top 3 wallets funded from same CEX withdrawal\n- Distribution looks organic at surface level",
        "contract_functions": "Contract Function Analysis for LatticeDAO:\n- Standard ERC20 with staking module\n- Owner-only: setRewardRate(), toggleStaking(), withdrawETH()\n- withdrawETH() sends to treasury (which is owner-controlled EOA)\n- setWithdrawalTimelock(0) can disable all withdrawal delays\n- Subtle: rebalanceTreasury() moves staked tokens without user consent\n- Audit: self-published, not from recognized firm",
        "deployer_history": "Deployer History for LatticeDAO:\n- Wallet age: 234 days (looks established)\n- 10 previous contracts, none flagged\n- Funded via legitimate CEX (Coinbase)\n- BUT: wallet was purchased/acquired (activity gap of 33 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: latticedao.eth (registered 6 days ago)",
        "social_signals": "Social Analysis for LatticeDAO:\n- Twitter: 19307 followers, appears active\n- Telegram: 6582 members, active discussion\n- BUT: engagement pattern is bot-like (same accounts reply within seconds)\n- Website: professional, but WHOIS registered 6 days ago\n- Team: LinkedIn profiles exist but have thin history\n- Looks legitimate until you check engagement authenticity",
        "similar_contracts": "Similar Contract Analysis for LatticeDAO:\n- Bytecode: 53% match (heavily modified from template)\n- Original template linked to 1 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 48/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for LatticeDAO:\n- 50 day history, organic-looking growth\n- Multiple natural-looking corrections (18% pullbacks)\n- Steady decline in last 8 days (-43%)\n- NOT a sudden crash \u2014 slow bleed as deployer sells in small batches\n- Looks like market downturn, but coincides with team wallet movements\n- Hard to distinguish from legitimate bear market without on-chain data"
      }
    },
    {
      "token_name": "BlazeDEX",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract BlazeDEX is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"BlazeDEX\", \"BLAZ\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for BlazeDEX:\n- 8498 holders, appears well-distributed\n- Top wallet: 12% (team, with vesting UI)\n- Gini: 0.50 (looks healthy)\n- 13 wallets have transferred tokens wallet-to-wallet\n- But 0 successful DEX sells from non-whitelisted addresses\n- Subtle: wallet-to-wallet works, DEX sells don't",
        "contract_functions": "Contract Function Analysis for BlazeDEX:\n- Standard-looking ERC20 with anti-snipe mechanism\n- setCooldown(uint256) \u2014 set to 2 seconds currently\n- BUT: cooldown resets on every incoming transfer (including reflections)\n- Net effect: if token has reflections, sell cooldown never expires\n- Mechanism is not obvious from reading setCooldown() alone\n- Requires tracing _update() logic to discover",
        "deployer_history": "Deployer History for BlazeDEX:\n- Wallet age: 339 days (looks established)\n- 12 previous contracts, none flagged\n- Funded via legitimate CEX (Kraken)\n- BUT: wallet was purchased/acquired (activity gap of 69 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: blazedex.eth (registered 4 days ago)",
        "social_signals": "Social Analysis for BlazeDEX:\n- Social presence appears healthy\n- Telegram: 4638 members, mostly positive\n- Negative comments exist but are rare and quickly addressed\n- Team claims 'anti-bot cooldown' explains sell delays\n- Twitter KOLs promoting it (paid, but not obviously so)\n- Difficult to distinguish from legitimate launch friction",
        "similar_contracts": "Similar Contract Analysis for BlazeDEX:\n- Bytecode: 58% match (heavily modified from template)\n- Original template linked to 2 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 59/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for BlazeDEX:\n- Price chart looks somewhat normal with 3 visible corrections\n- BUT: all 'corrections' are from same 2 wallets\n- Actual organic sell volume: 0\n- Corrections are manufactured to make chart look healthy\n- Volatility: 6.7% (engineered to appear normal)\n- Requires sell-source analysis to detect deception"
      }
    },
    {
      "token_name": "RadiantLend",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/security/Pausable.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract RadiantLend is ERC20, Pausable, Ownable {\n    uint256 public constant INITIAL_SUPPLY = 50_000_000e18;\n    address public treasuryWallet;\n    uint256 public withdrawalTimelock = 48 hours;\n    mapping(address => uint256) public lastWithdrawal;\n    uint256 public totalStaked;\n    mapping(address => uint256) public stakedBalance;\n\n    constructor(address _treasury) ERC20(\"RadiantLend\", \"RADI\") Ownable(msg.sender) {\n        treasuryWallet = _treasury;\n        _mint(address(this), INITIAL_SUPPLY);\n    }\n\n    function stake(uint256 amount) external whenNotPaused {\n        _transfer(msg.sender, address(this), amount);\n        stakedBalance[msg.sender] += amount;\n        totalStaked += amount;\n    }\n\n    function unstake(uint256 amount) external whenNotPaused {\n        require(stakedBalance[msg.sender] >= amount, \"Insufficient stake\");\n        require(block.timestamp >= lastWithdrawal[msg.sender] + withdrawalTimelock, \"Timelock\");\n        stakedBalance[msg.sender] -= amount;\n        totalStaked -= amount;\n        lastWithdrawal[msg.sender] = block.timestamp;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function setWithdrawalTimelock(uint256 _timelock) external onlyOwner {\n        withdrawalTimelock = _timelock;\n    }\n\n    /// @dev Admin function to rebalance treasury allocation\n    function rebalanceTreasury(uint256 amount) external onlyOwner {\n        _transfer(address(this), treasuryWallet, amount);\n    }\n\n    function pause() external onlyOwner { _pause(); }\n    function unpause() external onlyOwner { _unpause(); }\n\n    receive() external payable {}\n\n    function withdrawETH() external onlyOwner {\n        payable(treasuryWallet).transfer(address(this).balance);\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for RadiantLend:\n- Top wallet holds 35% (claims to be DAO treasury)\n- 731 holders across 3 chains\n- Gini: 0.76 (moderate concentration)\n- Top wallet is an EOA, not a multisig\n- Cluster analysis: top 3 wallets funded from same CEX withdrawal\n- Distribution looks organic at surface level",
        "contract_functions": "Contract Function Analysis for RadiantLend:\n- Standard ERC20 with staking module\n- Owner-only: setRewardRate(), toggleStaking(), withdrawETH()\n- withdrawETH() sends to treasury (which is owner-controlled EOA)\n- setWithdrawalTimelock(0) can disable all withdrawal delays\n- Subtle: rebalanceTreasury() moves staked tokens without user consent\n- Audit: self-published, not from recognized firm",
        "deployer_history": "Deployer History for RadiantLend:\n- Wallet age: 157 days (looks established)\n- 6 previous contracts, none flagged\n- Funded via legitimate CEX (Coinbase)\n- BUT: wallet was purchased/acquired (activity gap of 89 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: radiantlend.eth (registered 9 days ago)",
        "social_signals": "Social Analysis for RadiantLend:\n- Twitter: 37459 followers, appears active\n- Telegram: 3357 members, active discussion\n- BUT: engagement pattern is bot-like (same accounts reply within seconds)\n- Website: professional, but WHOIS registered 15 days ago\n- Team: LinkedIn profiles exist but have thin history\n- Looks legitimate until you check engagement authenticity",
        "similar_contracts": "Similar Contract Analysis for RadiantLend:\n- Bytecode: 46% match (heavily modified from template)\n- Original template linked to 2 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 53/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for RadiantLend:\n- 54 day history, organic-looking growth\n- Multiple natural-looking corrections (27% pullbacks)\n- Steady decline in last 9 days (-48%)\n- NOT a sudden crash \u2014 slow bleed as deployer sells in small batches\n- Looks like market downturn, but coincides with team wallet movements\n- Hard to distinguish from legitimate bear market without on-chain data"
      }
    },
    {
      "token_name": "AetherBridge",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract AetherBridge is ERC20, Ownable {\n    mapping(address => bool) private _authorized;\n    mapping(address => uint256) private _holdTimestamp;\n    address public pair;\n    uint256 public minHoldDuration = 0;\n    bool public antiSnipeEnabled = true;\n    uint256 public launchBlock;\n\n    constructor() ERC20(\"AetherBridge\", \"AET\") Ownable(msg.sender) {\n        _mint(msg.sender, 500_000_000e18);\n        _authorized[msg.sender] = true;\n    }\n\n    function openTrading(address _pair) external onlyOwner {\n        pair = _pair;\n        launchBlock = block.number;\n    }\n\n    function setAntiSnipe(bool _enabled) external onlyOwner {\n        antiSnipeEnabled = _enabled;\n    }\n\n    function setMinHoldDuration(uint256 _duration) external onlyOwner {\n        minHoldDuration = _duration;\n    }\n\n    function authorize(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            _authorized[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (from == pair) {\n            _holdTimestamp[to] = block.timestamp;\n        }\n        if (to == pair && !_authorized[from]) {\n            if (antiSnipeEnabled) {\n                require(\n                    block.timestamp >= _holdTimestamp[from] + minHoldDuration,\n                    \"Hold period not met\"\n                );\n            }\n        }\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for AetherBridge:\n- 8777 holders, appears well-distributed\n- Top wallet: 6% (team, with vesting UI)\n- Gini: 0.53 (looks healthy)\n- 9 wallets have transferred tokens wallet-to-wallet\n- But 0 successful DEX sells from non-whitelisted addresses\n- Subtle: wallet-to-wallet works, DEX sells don't",
        "contract_functions": "Contract Function Analysis for AetherBridge:\n- Standard-looking ERC20 with anti-snipe mechanism\n- setCooldown(uint256) \u2014 set to 1 seconds currently\n- BUT: cooldown resets on every incoming transfer (including reflections)\n- Net effect: if token has reflections, sell cooldown never expires\n- Mechanism is not obvious from reading setCooldown() alone\n- Requires tracing _update() logic to discover",
        "deployer_history": "Deployer History for AetherBridge:\n- Wallet age: 268 days (looks established)\n- 10 previous contracts, none flagged\n- Funded via legitimate CEX (Coinbase)\n- BUT: wallet was purchased/acquired (activity gap of 68 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: aetherbridge.eth (registered 6 days ago)",
        "social_signals": "Social Analysis for AetherBridge:\n- Social presence appears healthy\n- Telegram: 7723 members, mostly positive\n- Negative comments exist but are rare and quickly addressed\n- Team claims 'anti-bot cooldown' explains sell delays\n- Twitter KOLs promoting it (paid, but not obviously so)\n- Difficult to distinguish from legitimate launch friction",
        "similar_contracts": "Similar Contract Analysis for AetherBridge:\n- Bytecode: 46% match (heavily modified from template)\n- Original template linked to 2 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 46/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for AetherBridge:\n- Price chart looks somewhat normal with 3 visible corrections\n- BUT: all 'corrections' are from same 4 wallets\n- Actual organic sell volume: 0\n- Corrections are manufactured to make chart look healthy\n- Volatility: 7.7% (engineered to appear normal)\n- Requires sell-source analysis to detect deception"
      }
    },
    {
      "token_name": "MantleFi",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Votes.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Permit.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract MantleFi is ERC20Votes, AccessControl {\n    bytes32 public constant MINTER_ROLE = keccak256(\"MINTER_ROLE\");\n    uint256 public immutable maxSupply;\n    uint256 public immutable mintCap; // max per tx\n\n    constructor(uint256 _maxSupply)\n        ERC20(\"MantleFi\", \"MANT\")\n        ERC20Permit(\"MantleFi\")\n    {\n        maxSupply = _maxSupply;\n        mintCap = _maxSupply / 100;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _mint(msg.sender, _maxSupply / 5); // 20% initial\n    }\n\n    function mint(address to, uint256 amount) external onlyRole(MINTER_ROLE) {\n        require(amount <= mintCap, \"Exceeds mint cap\");\n        require(totalSupply() + amount <= maxSupply, \"Exceeds max supply\");\n        _mint(to, amount);\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for MantleFi:\n- 121,880 holders\n- Top wallet: 9.4% (looks concentrated, but it's Binance hot wallet)\n- Gini: 0.60 (elevated but explained by CEX custody)\n- 3 large wallets are CEX deposit addresses\n- Actual user distribution excluding CEXs: Gini 0.41\n- Ownership: multisig with 3 public signers",
        "contract_functions": "Contract Function Analysis for MantleFi:\n- Has withdrawETH() and recoverTokens() \u2014 looks risky\n- BUT: both gated behind 72h timelock + multisig approval\n- recoverTokens cannot withdraw the native token (hardcoded check)\n- withdrawETH sends to immutable treasury (not owner EOA)\n- Audit: Trail of Bits \u2014 no critical findings\n- Appears dangerous but safeguards are real",
        "deployer_history": "Deployer History for MantleFi:\n- Wallet age: 1333 days\n- Recent large ETH transfers (looks like extraction but it's VC funding round)\n- 10 contracts including 2 that were sunset (normal lifecycle)\n- Sunset contracts had proper migration, users notified\n- Team doxxed via Crunchbase, Paradigm backed",
        "social_signals": "Social Analysis for MantleFi:\n- Twitter: 9362 followers (modest but real)\n- Some FUD threads exist (competitors spreading doubt)\n- Team has been quiet lately (building, not marketing)\n- GitHub: consistent commits but low star count\n- Looks under-the-radar but fundamentals are strong\n- Community small but highly engaged",
        "similar_contracts": "Similar Contract Analysis for MantleFi:\n- Has some patterns that overlap with flagged contracts (30% match)\n- BUT: overlap is in common ERC20 boilerplate, not scam logic\n- TokenSniffer: 71/100 (docked for admin functions that have timelocks)\n- GoPlus: 1 medium warning (owner can pause \u2014 but behind governance)\n- False positive risk: high for automated scanners",
        "price_history": "Price History for MantleFi:\n- Recent sharp drop: -27% in 4 days\n- Looks like rug pull chart at first glance\n- BUT: drop coincides with broader market selloff\n- Volume spiked from organic panic selling, not insider exits\n- Currently recovering: +12% from local low\n- No unusual wallet movements during drawdown"
      }
    },
    {
      "token_name": "ForgeProtocol",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract ForgeProtocol is ERC20, Ownable {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public immutable vestingStart;\n    uint256 public constant VESTING_DURATION = 365 days;\n    uint256 public teamAllocation;\n    uint256 public teamClaimed;\n\n    constructor() ERC20(\"ForgeProtocol\", \"FORG\") Ownable(msg.sender) {\n        vestingStart = block.timestamp;\n        teamAllocation = MAX_SUPPLY * 15 / 100; // 15% team\n        _mint(address(this), teamAllocation);\n        _mint(msg.sender, MAX_SUPPLY - teamAllocation); // 85% to LP/community\n    }\n\n    function claimVested() external onlyOwner {\n        uint256 elapsed = block.timestamp - vestingStart;\n        if (elapsed > VESTING_DURATION) elapsed = VESTING_DURATION;\n        uint256 totalVested = (teamAllocation * elapsed) / VESTING_DURATION;\n        uint256 claimable = totalVested - teamClaimed;\n        require(claimable > 0, \"Nothing to claim\");\n        teamClaimed += claimable;\n        _transfer(address(this), owner(), claimable);\n    }\n\n    function renounceOwnership() public override onlyOwner {\n        super.renounceOwnership();\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for ForgeProtocol:\n- 116,981 holders\n- Top wallet: 16.8% (looks concentrated, but it's Binance hot wallet)\n- Gini: 0.62 (elevated but explained by CEX custody)\n- 2 large wallets are CEX deposit addresses\n- Actual user distribution excluding CEXs: Gini 0.48\n- Ownership: multisig with 7 public signers",
        "contract_functions": "Contract Function Analysis for ForgeProtocol:\n- Has withdrawETH() and recoverTokens() \u2014 looks risky\n- BUT: both gated behind 72h timelock + multisig approval\n- recoverTokens cannot withdraw the native token (hardcoded check)\n- withdrawETH sends to immutable treasury (not owner EOA)\n- Audit: Spearbit \u2014 no critical findings\n- Appears dangerous but safeguards are real",
        "deployer_history": "Deployer History for ForgeProtocol:\n- Wallet age: 1658 days\n- Recent large ETH transfers (looks like extraction but it's VC funding round)\n- 12 contracts including 2 that were sunset (normal lifecycle)\n- Sunset contracts had proper migration, users notified\n- Team doxxed via Crunchbase, a16z backed",
        "social_signals": "Social Analysis for ForgeProtocol:\n- Twitter: 5738 followers (modest but real)\n- Some FUD threads exist (competitors spreading doubt)\n- Team has been quiet lately (building, not marketing)\n- GitHub: consistent commits but low star count\n- Looks under-the-radar but fundamentals are strong\n- Community small but highly engaged",
        "similar_contracts": "Similar Contract Analysis for ForgeProtocol:\n- Has some patterns that overlap with flagged contracts (35% match)\n- BUT: overlap is in common ERC20 boilerplate, not scam logic\n- TokenSniffer: 79/100 (docked for admin functions that have timelocks)\n- GoPlus: 1 medium warning (owner can pause \u2014 but behind governance)\n- False positive risk: high for automated scanners",
        "price_history": "Price History for ForgeProtocol:\n- Recent sharp drop: -37% in 7 days\n- Looks like rug pull chart at first glance\n- BUT: drop coincides with broader market selloff\n- Volume spiked from organic panic selling, not insider exits\n- Currently recovering: +11% from local low\n- No unusual wallet movements during drawdown"
      }
    },
    {
      "token_name": "ValorFi",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract ValorFi is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"ValorFi\", \"VALO\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for ValorFi:\n- 4757 holders, appears well-distributed\n- Top wallet: 10% (team, with vesting UI)\n- Gini: 0.49 (looks healthy)\n- 15 wallets have transferred tokens wallet-to-wallet\n- But 0 successful DEX sells from non-whitelisted addresses\n- Subtle: wallet-to-wallet works, DEX sells don't",
        "contract_functions": "Contract Function Analysis for ValorFi:\n- Standard-looking ERC20 with anti-snipe mechanism\n- setCooldown(uint256) \u2014 set to 5 seconds currently\n- BUT: cooldown resets on every incoming transfer (including reflections)\n- Net effect: if token has reflections, sell cooldown never expires\n- Mechanism is not obvious from reading setCooldown() alone\n- Requires tracing _update() logic to discover",
        "deployer_history": "Deployer History for ValorFi:\n- Wallet age: 335 days (looks established)\n- 6 previous contracts, none flagged\n- Funded via legitimate CEX (Kraken)\n- BUT: wallet was purchased/acquired (activity gap of 77 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: valorfi.eth (registered 8 days ago)",
        "social_signals": "Social Analysis for ValorFi:\n- Social presence appears healthy\n- Telegram: 5631 members, mostly positive\n- Negative comments exist but are rare and quickly addressed\n- Team claims 'anti-bot cooldown' explains sell delays\n- Twitter KOLs promoting it (paid, but not obviously so)\n- Difficult to distinguish from legitimate launch friction",
        "similar_contracts": "Similar Contract Analysis for ValorFi:\n- Bytecode: 53% match (heavily modified from template)\n- Original template linked to 1 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 56/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for ValorFi:\n- Price chart looks somewhat normal with 7 visible corrections\n- BUT: all 'corrections' are from same 2 wallets\n- Actual organic sell volume: 0\n- Corrections are manufactured to make chart look healthy\n- Volatility: 7.7% (engineered to appear normal)\n- Requires sell-source analysis to detect deception"
      }
    },
    {
      "token_name": "DawnProtocol",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/security/Pausable.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract DawnProtocol is ERC20, Pausable, Ownable {\n    uint256 public constant INITIAL_SUPPLY = 50_000_000e18;\n    address public treasuryWallet;\n    uint256 public withdrawalTimelock = 48 hours;\n    mapping(address => uint256) public lastWithdrawal;\n    uint256 public totalStaked;\n    mapping(address => uint256) public stakedBalance;\n\n    constructor(address _treasury) ERC20(\"DawnProtocol\", \"DAWN\") Ownable(msg.sender) {\n        treasuryWallet = _treasury;\n        _mint(address(this), INITIAL_SUPPLY);\n    }\n\n    function stake(uint256 amount) external whenNotPaused {\n        _transfer(msg.sender, address(this), amount);\n        stakedBalance[msg.sender] += amount;\n        totalStaked += amount;\n    }\n\n    function unstake(uint256 amount) external whenNotPaused {\n        require(stakedBalance[msg.sender] >= amount, \"Insufficient stake\");\n        require(block.timestamp >= lastWithdrawal[msg.sender] + withdrawalTimelock, \"Timelock\");\n        stakedBalance[msg.sender] -= amount;\n        totalStaked -= amount;\n        lastWithdrawal[msg.sender] = block.timestamp;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function setWithdrawalTimelock(uint256 _timelock) external onlyOwner {\n        withdrawalTimelock = _timelock;\n    }\n\n    /// @dev Admin function to rebalance treasury allocation\n    function rebalanceTreasury(uint256 amount) external onlyOwner {\n        _transfer(address(this), treasuryWallet, amount);\n    }\n\n    function pause() external onlyOwner { _pause(); }\n    function unpause() external onlyOwner { _unpause(); }\n\n    receive() external payable {}\n\n    function withdrawETH() external onlyOwner {\n        payable(treasuryWallet).transfer(address(this).balance);\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for DawnProtocol:\n- Top wallet holds 44% (claims to be DAO treasury)\n- 824 holders across 3 chains\n- Gini: 0.77 (moderate concentration)\n- Top wallet is an EOA, not a multisig\n- Cluster analysis: top 3 wallets funded from same CEX withdrawal\n- Distribution looks organic at surface level",
        "contract_functions": "Contract Function Analysis for DawnProtocol:\n- Standard ERC20 with staking module\n- Owner-only: setRewardRate(), toggleStaking(), withdrawETH()\n- withdrawETH() sends to treasury (which is owner-controlled EOA)\n- setWithdrawalTimelock(0) can disable all withdrawal delays\n- Subtle: rebalanceTreasury() moves staked tokens without user consent\n- Audit: self-published, not from recognized firm",
        "deployer_history": "Deployer History for DawnProtocol:\n- Wallet age: 290 days (looks established)\n- 5 previous contracts, none flagged\n- Funded via legitimate CEX (Kraken)\n- BUT: wallet was purchased/acquired (activity gap of 90 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: dawnprotocol.eth (registered 7 days ago)",
        "social_signals": "Social Analysis for DawnProtocol:\n- Twitter: 30147 followers, appears active\n- Telegram: 5708 members, active discussion\n- BUT: engagement pattern is bot-like (same accounts reply within seconds)\n- Website: professional, but WHOIS registered 20 days ago\n- Team: LinkedIn profiles exist but have thin history\n- Looks legitimate until you check engagement authenticity",
        "similar_contracts": "Similar Contract Analysis for DawnProtocol:\n- Bytecode: 41% match (heavily modified from template)\n- Original template linked to 2 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 57/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for DawnProtocol:\n- 36 day history, organic-looking growth\n- Multiple natural-looking corrections (15% pullbacks)\n- Steady decline in last 13 days (-48%)\n- NOT a sudden crash \u2014 slow bleed as deployer sells in small batches\n- Looks like market downturn, but coincides with team wallet movements\n- Hard to distinguish from legitimate bear market without on-chain data"
      }
    },
    {
      "token_name": "QuestFi",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract QuestFi is ERC20, Ownable {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public immutable vestingStart;\n    uint256 public constant VESTING_DURATION = 365 days;\n    uint256 public teamAllocation;\n    uint256 public teamClaimed;\n\n    constructor() ERC20(\"QuestFi\", \"QUES\") Ownable(msg.sender) {\n        vestingStart = block.timestamp;\n        teamAllocation = MAX_SUPPLY * 15 / 100; // 15% team\n        _mint(address(this), teamAllocation);\n        _mint(msg.sender, MAX_SUPPLY - teamAllocation); // 85% to LP/community\n    }\n\n    function claimVested() external onlyOwner {\n        uint256 elapsed = block.timestamp - vestingStart;\n        if (elapsed > VESTING_DURATION) elapsed = VESTING_DURATION;\n        uint256 totalVested = (teamAllocation * elapsed) / VESTING_DURATION;\n        uint256 claimable = totalVested - teamClaimed;\n        require(claimable > 0, \"Nothing to claim\");\n        teamClaimed += claimable;\n        _transfer(address(this), owner(), claimable);\n    }\n\n    function renounceOwnership() public override onlyOwner {\n        super.renounceOwnership();\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for QuestFi:\n- 143,070 holders\n- Top wallet: 13.9% (looks concentrated, but it's Binance hot wallet)\n- Gini: 0.70 (elevated but explained by CEX custody)\n- 5 large wallets are CEX deposit addresses\n- Actual user distribution excluding CEXs: Gini 0.46\n- Ownership: multisig with 3 public signers",
        "contract_functions": "Contract Function Analysis for QuestFi:\n- Has withdrawETH() and recoverTokens() \u2014 looks risky\n- BUT: both gated behind 72h timelock + multisig approval\n- recoverTokens cannot withdraw the native token (hardcoded check)\n- withdrawETH sends to immutable treasury (not owner EOA)\n- Audit: Spearbit \u2014 no critical findings\n- Appears dangerous but safeguards are real",
        "deployer_history": "Deployer History for QuestFi:\n- Wallet age: 418 days\n- Recent large ETH transfers (looks like extraction but it's VC funding round)\n- 10 contracts including 2 that were sunset (normal lifecycle)\n- Sunset contracts had proper migration, users notified\n- Team doxxed via Crunchbase, a16z backed",
        "social_signals": "Social Analysis for QuestFi:\n- Twitter: 14626 followers (modest but real)\n- Some FUD threads exist (competitors spreading doubt)\n- Team has been quiet lately (building, not marketing)\n- GitHub: consistent commits but low star count\n- Looks under-the-radar but fundamentals are strong\n- Community small but highly engaged",
        "similar_contracts": "Similar Contract Analysis for QuestFi:\n- Has some patterns that overlap with flagged contracts (27% match)\n- BUT: overlap is in common ERC20 boilerplate, not scam logic\n- TokenSniffer: 73/100 (docked for admin functions that have timelocks)\n- GoPlus: 1 medium warning (owner can pause \u2014 but behind governance)\n- False positive risk: high for automated scanners",
        "price_history": "Price History for QuestFi:\n- Recent sharp drop: -33% in 7 days\n- Looks like rug pull chart at first glance\n- BUT: drop coincides with broader market selloff\n- Volume spiked from organic panic selling, not insider exits\n- Currently recovering: +15% from local low\n- No unusual wallet movements during drawdown"
      }
    },
    {
      "token_name": "CoreDAO",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract CoreDAO is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 200_000_000e18;\n    address public pair;\n    mapping(address => uint256) public lastTransfer;\n    uint256 public cooldownPeriod = 30; // seconds\n    mapping(address => bool) public isWhitelisted;\n    uint256 public maxWalletPercent = 200; // 2%\n\n    constructor() ERC20(\"CoreDAO\", \"CORE\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n        isWhitelisted[msg.sender] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n        isWhitelisted[_pair] = true;\n    }\n\n    function setCooldown(uint256 _seconds) external onlyOwner {\n        cooldownPeriod = _seconds;\n    }\n\n    function setMaxWalletPercent(uint256 _percent) external onlyOwner {\n        require(_percent >= 100, \"Min 1%\");\n        maxWalletPercent = _percent;\n    }\n\n    function whitelist(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            isWhitelisted[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isWhitelisted[from] && !isWhitelisted[to]) {\n            require(\n                block.timestamp >= lastTransfer[from] + cooldownPeriod,\n                \"Cooldown active\"\n            );\n            if (to != pair) {\n                require(\n                    balanceOf(to) + amount <= (totalSupply() * maxWalletPercent) / 10000,\n                    \"Exceeds max wallet\"\n                );\n            }\n        }\n        lastTransfer[from] = block.timestamp;\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for CoreDAO:\n- 3522 holders, appears well-distributed\n- Top wallet: 8% (team, with vesting UI)\n- Gini: 0.47 (looks healthy)\n- 12 wallets have transferred tokens wallet-to-wallet\n- But 0 successful DEX sells from non-whitelisted addresses\n- Subtle: wallet-to-wallet works, DEX sells don't",
        "contract_functions": "Contract Function Analysis for CoreDAO:\n- Standard-looking ERC20 with anti-snipe mechanism\n- setCooldown(uint256) \u2014 set to 1 seconds currently\n- BUT: cooldown resets on every incoming transfer (including reflections)\n- Net effect: if token has reflections, sell cooldown never expires\n- Mechanism is not obvious from reading setCooldown() alone\n- Requires tracing _update() logic to discover",
        "deployer_history": "Deployer History for CoreDAO:\n- Wallet age: 324 days (looks established)\n- 8 previous contracts, none flagged\n- Funded via legitimate CEX (Coinbase)\n- BUT: wallet was purchased/acquired (activity gap of 66 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: coredao.eth (registered 4 days ago)",
        "social_signals": "Social Analysis for CoreDAO:\n- Social presence appears healthy\n- Telegram: 5766 members, mostly positive\n- Negative comments exist but are rare and quickly addressed\n- Team claims 'anti-bot cooldown' explains sell delays\n- Twitter KOLs promoting it (paid, but not obviously so)\n- Difficult to distinguish from legitimate launch friction",
        "similar_contracts": "Similar Contract Analysis for CoreDAO:\n- Bytecode: 49% match (heavily modified from template)\n- Original template linked to 3 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 52/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for CoreDAO:\n- Price chart looks somewhat normal with 5 visible corrections\n- BUT: all 'corrections' are from same 2 wallets\n- Actual organic sell volume: 0\n- Corrections are manufactured to make chart look healthy\n- Volatility: 5.1% (engineered to appear normal)\n- Requires sell-source analysis to detect deception"
      }
    },
    {
      "token_name": "EpochFi",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract EpochFi is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"EpochFi\", \"EPOC\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for EpochFi:\n- 5946 holders, appears well-distributed\n- Top wallet: 5% (team, with vesting UI)\n- Gini: 0.56 (looks healthy)\n- 7 wallets have transferred tokens wallet-to-wallet\n- But 0 successful DEX sells from non-whitelisted addresses\n- Subtle: wallet-to-wallet works, DEX sells don't",
        "contract_functions": "Contract Function Analysis for EpochFi:\n- Standard-looking ERC20 with anti-snipe mechanism\n- setCooldown(uint256) \u2014 set to 2 seconds currently\n- BUT: cooldown resets on every incoming transfer (including reflections)\n- Net effect: if token has reflections, sell cooldown never expires\n- Mechanism is not obvious from reading setCooldown() alone\n- Requires tracing _update() logic to discover",
        "deployer_history": "Deployer History for EpochFi:\n- Wallet age: 129 days (looks established)\n- 5 previous contracts, none flagged\n- Funded via legitimate CEX (Coinbase)\n- BUT: wallet was purchased/acquired (activity gap of 73 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: epochfi.eth (registered 5 days ago)",
        "social_signals": "Social Analysis for EpochFi:\n- Social presence appears healthy\n- Telegram: 3863 members, mostly positive\n- Negative comments exist but are rare and quickly addressed\n- Team claims 'anti-bot cooldown' explains sell delays\n- Twitter KOLs promoting it (paid, but not obviously so)\n- Difficult to distinguish from legitimate launch friction",
        "similar_contracts": "Similar Contract Analysis for EpochFi:\n- Bytecode: 54% match (heavily modified from template)\n- Original template linked to 3 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 56/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for EpochFi:\n- Price chart looks somewhat normal with 8 visible corrections\n- BUT: all 'corrections' are from same 3 wallets\n- Actual organic sell volume: 0\n- Corrections are manufactured to make chart look healthy\n- Volatility: 7.4% (engineered to appear normal)\n- Requires sell-source analysis to detect deception"
      }
    },
    {
      "token_name": "CircuitFi",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\n\ncontract CircuitFi is ERC20 {\n    constructor(address treasury, address community, address lp) ERC20(\"CircuitFi\", \"CIRC\") {\n        _mint(treasury, 30_000_000e18);   // 30% treasury (multisig)\n        _mint(community, 50_000_000e18);  // 50% community airdrop\n        _mint(lp, 20_000_000e18);         // 20% initial liquidity\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for CircuitFi:\n- 74,870 holders\n- Top wallet: 10.4% (looks concentrated, but it's Binance hot wallet)\n- Gini: 0.72 (elevated but explained by CEX custody)\n- 2 large wallets are CEX deposit addresses\n- Actual user distribution excluding CEXs: Gini 0.47\n- Ownership: multisig with 5 public signers",
        "contract_functions": "Contract Function Analysis for CircuitFi:\n- Has withdrawETH() and recoverTokens() \u2014 looks risky\n- BUT: both gated behind 72h timelock + multisig approval\n- recoverTokens cannot withdraw the native token (hardcoded check)\n- withdrawETH sends to immutable treasury (not owner EOA)\n- Audit: Trail of Bits \u2014 no critical findings\n- Appears dangerous but safeguards are real",
        "deployer_history": "Deployer History for CircuitFi:\n- Wallet age: 1359 days\n- Recent large ETH transfers (looks like extraction but it's VC funding round)\n- 7 contracts including 2 that were sunset (normal lifecycle)\n- Sunset contracts had proper migration, users notified\n- Team doxxed via Crunchbase, a16z backed",
        "social_signals": "Social Analysis for CircuitFi:\n- Twitter: 9057 followers (modest but real)\n- Some FUD threads exist (competitors spreading doubt)\n- Team has been quiet lately (building, not marketing)\n- GitHub: consistent commits but low star count\n- Looks under-the-radar but fundamentals are strong\n- Community small but highly engaged",
        "similar_contracts": "Similar Contract Analysis for CircuitFi:\n- Has some patterns that overlap with flagged contracts (26% match)\n- BUT: overlap is in common ERC20 boilerplate, not scam logic\n- TokenSniffer: 70/100 (docked for admin functions that have timelocks)\n- GoPlus: 1 medium warning (owner can pause \u2014 but behind governance)\n- False positive risk: high for automated scanners",
        "price_history": "Price History for CircuitFi:\n- Recent sharp drop: -31% in 5 days\n- Looks like rug pull chart at first glance\n- BUT: drop coincides with broader market selloff\n- Volume spiked from organic panic selling, not insider exits\n- Currently recovering: +9% from local low\n- No unusual wallet movements during drawdown"
      }
    },
    {
      "token_name": "DeltaLend",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface IRouter {\n    function swapExactTokensForETH(uint256, uint256, address[] calldata, address, uint256) external;\n    function WETH() external view returns (address);\n}\n\ncontract DeltaLend is ERC20, Ownable {\n    IRouter public router;\n    address public pair;\n    uint256 public maxWalletSize = 2_000_000e18;\n    mapping(address => bool) private _isExcluded;\n    bool private _swapping;\n\n    constructor(address _router) ERC20(\"DeltaLend\", \"DELT\") Ownable(msg.sender) {\n        router = IRouter(_router);\n        _mint(msg.sender, 100_000_000e18);\n        _isExcluded[msg.sender] = true;\n        _isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function setMaxWallet(uint256 _max) external onlyOwner {\n        require(_max >= 1_000_000e18, \"Too low\");\n        maxWalletSize = _max;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!_isExcluded[to] && to != pair) {\n            require(balanceOf(to) + amount <= maxWalletSize, \"Max wallet\");\n        }\n        super._update(from, to, amount);\n    }\n\n    function clearStuckBalance() external onlyOwner {\n        payable(owner()).transfer(address(this).balance);\n    }\n\n    function clearStuckTokens(address token) external onlyOwner {\n        uint256 bal = IERC20(token).balanceOf(address(this));\n        IERC20(token).transfer(owner(), bal);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for DeltaLend:\n- Top wallet holds 37% (claims to be DAO treasury)\n- 592 holders across 2 chains\n- Gini: 0.66 (moderate concentration)\n- Top wallet is an EOA, not a multisig\n- Cluster analysis: top 3 wallets funded from same CEX withdrawal\n- Distribution looks organic at surface level",
        "contract_functions": "Contract Function Analysis for DeltaLend:\n- Standard ERC20 with staking module\n- Owner-only: setRewardRate(), toggleStaking(), withdrawETH()\n- withdrawETH() sends to treasury (which is owner-controlled EOA)\n- setWithdrawalTimelock(0) can disable all withdrawal delays\n- Subtle: rebalanceTreasury() moves staked tokens without user consent\n- Audit: self-published, not from recognized firm",
        "deployer_history": "Deployer History for DeltaLend:\n- Wallet age: 308 days (looks established)\n- 8 previous contracts, none flagged\n- Funded via legitimate CEX (Kraken)\n- BUT: wallet was purchased/acquired (activity gap of 34 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: deltalend.eth (registered 5 days ago)",
        "social_signals": "Social Analysis for DeltaLend:\n- Twitter: 18467 followers, appears active\n- Telegram: 3780 members, active discussion\n- BUT: engagement pattern is bot-like (same accounts reply within seconds)\n- Website: professional, but WHOIS registered 5 days ago\n- Team: LinkedIn profiles exist but have thin history\n- Looks legitimate until you check engagement authenticity",
        "similar_contracts": "Similar Contract Analysis for DeltaLend:\n- Bytecode: 53% match (heavily modified from template)\n- Original template linked to 3 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 45/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for DeltaLend:\n- 47 day history, organic-looking growth\n- Multiple natural-looking corrections (17% pullbacks)\n- Steady decline in last 7 days (-64%)\n- NOT a sudden crash \u2014 slow bleed as deployer sells in small batches\n- Looks like market downturn, but coincides with team wallet movements\n- Hard to distinguish from legitimate bear market without on-chain data"
      }
    },
    {
      "token_name": "LithiumFi",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract LithiumFi is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"LithiumFi\", \"LITH\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for LithiumFi:\n- Top wallet holds 25% (claims to be DAO treasury)\n- 1211 holders across 2 chains\n- Gini: 0.74 (moderate concentration)\n- Top wallet is an EOA, not a multisig\n- Cluster analysis: top 3 wallets funded from same CEX withdrawal\n- Distribution looks organic at surface level",
        "contract_functions": "Contract Function Analysis for LithiumFi:\n- Standard ERC20 with staking module\n- Owner-only: setRewardRate(), toggleStaking(), withdrawETH()\n- withdrawETH() sends to treasury (which is owner-controlled EOA)\n- setWithdrawalTimelock(0) can disable all withdrawal delays\n- Subtle: rebalanceTreasury() moves staked tokens without user consent\n- Audit: self-published, not from recognized firm",
        "deployer_history": "Deployer History for LithiumFi:\n- Wallet age: 279 days (looks established)\n- 8 previous contracts, none flagged\n- Funded via legitimate CEX (Kraken)\n- BUT: wallet was purchased/acquired (activity gap of 56 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: lithiumfi.eth (registered 7 days ago)",
        "social_signals": "Social Analysis for LithiumFi:\n- Twitter: 25990 followers, appears active\n- Telegram: 7034 members, active discussion\n- BUT: engagement pattern is bot-like (same accounts reply within seconds)\n- Website: professional, but WHOIS registered 10 days ago\n- Team: LinkedIn profiles exist but have thin history\n- Looks legitimate until you check engagement authenticity",
        "similar_contracts": "Similar Contract Analysis for LithiumFi:\n- Bytecode: 45% match (heavily modified from template)\n- Original template linked to 3 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 48/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for LithiumFi:\n- 42 day history, organic-looking growth\n- Multiple natural-looking corrections (18% pullbacks)\n- Steady decline in last 6 days (-55%)\n- NOT a sudden crash \u2014 slow bleed as deployer sells in small batches\n- Looks like market downturn, but coincides with team wallet movements\n- Hard to distinguish from legitimate bear market without on-chain data"
      }
    },
    {
      "token_name": "BreezeDEX",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Burnable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract BreezeDEX is ERC20, ERC20Burnable, ReentrancyGuard, Ownable {\n    uint256 public constant MAX_SUPPLY = 50_000_000e18;\n    uint256 public rewardRate = 100; // 1% per epoch\n    mapping(address => uint256) public staked;\n    mapping(address => uint256) public stakeTimestamp;\n    uint256 public totalStaked;\n    bool public stakingEnabled = true;\n\n    constructor() ERC20(\"BreezeDEX\", \"BREE\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n    }\n\n    function stake(uint256 amount) external nonReentrant {\n        require(stakingEnabled, \"Staking paused\");\n        _claimReward(msg.sender);\n        _transfer(msg.sender, address(this), amount);\n        staked[msg.sender] += amount;\n        totalStaked += amount;\n        stakeTimestamp[msg.sender] = block.timestamp;\n    }\n\n    function unstake(uint256 amount) external nonReentrant {\n        require(staked[msg.sender] >= amount, \"Insufficient\");\n        _claimReward(msg.sender);\n        staked[msg.sender] -= amount;\n        totalStaked -= amount;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function _claimReward(address user) internal {\n        if (staked[user] > 0 && stakeTimestamp[user] > 0) {\n            uint256 elapsed = block.timestamp - stakeTimestamp[user];\n            uint256 reward = (staked[user] * rewardRate * elapsed) / (10000 * 365 days);\n            if (reward > 0 && totalSupply() + reward <= MAX_SUPPLY) {\n                _mint(user, reward);\n            }\n        }\n        stakeTimestamp[user] = block.timestamp;\n    }\n\n    function setRewardRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 1000, \"Max 10%\");\n        rewardRate = _rate;\n    }\n\n    function toggleStaking() external onlyOwner {\n        stakingEnabled = !stakingEnabled;\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for BreezeDEX:\n- 67,490 holders\n- Top wallet: 8.3% (looks concentrated, but it's Binance hot wallet)\n- Gini: 0.69 (elevated but explained by CEX custody)\n- 4 large wallets are CEX deposit addresses\n- Actual user distribution excluding CEXs: Gini 0.40\n- Ownership: multisig with 7 public signers",
        "contract_functions": "Contract Function Analysis for BreezeDEX:\n- Has withdrawETH() and recoverTokens() \u2014 looks risky\n- BUT: both gated behind 72h timelock + multisig approval\n- recoverTokens cannot withdraw the native token (hardcoded check)\n- withdrawETH sends to immutable treasury (not owner EOA)\n- Audit: Trail of Bits \u2014 no critical findings\n- Appears dangerous but safeguards are real",
        "deployer_history": "Deployer History for BreezeDEX:\n- Wallet age: 1654 days\n- Recent large ETH transfers (looks like extraction but it's VC funding round)\n- 5 contracts including 2 that were sunset (normal lifecycle)\n- Sunset contracts had proper migration, users notified\n- Team doxxed via Crunchbase, a16z backed",
        "social_signals": "Social Analysis for BreezeDEX:\n- Twitter: 11025 followers (modest but real)\n- Some FUD threads exist (competitors spreading doubt)\n- Team has been quiet lately (building, not marketing)\n- GitHub: consistent commits but low star count\n- Looks under-the-radar but fundamentals are strong\n- Community small but highly engaged",
        "similar_contracts": "Similar Contract Analysis for BreezeDEX:\n- Has some patterns that overlap with flagged contracts (20% match)\n- BUT: overlap is in common ERC20 boilerplate, not scam logic\n- TokenSniffer: 78/100 (docked for admin functions that have timelocks)\n- GoPlus: 1 medium warning (owner can pause \u2014 but behind governance)\n- False positive risk: high for automated scanners",
        "price_history": "Price History for BreezeDEX:\n- Recent sharp drop: -41% in 6 days\n- Looks like rug pull chart at first glance\n- BUT: drop coincides with broader market selloff\n- Volume spiked from organic panic selling, not insider exits\n- Currently recovering: +15% from local low\n- No unusual wallet movements during drawdown"
      }
    },
    {
      "token_name": "OmegaFi",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract OmegaFi is ERC20, Ownable {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public immutable vestingStart;\n    uint256 public constant VESTING_DURATION = 365 days;\n    uint256 public teamAllocation;\n    uint256 public teamClaimed;\n\n    constructor() ERC20(\"OmegaFi\", \"OMEG\") Ownable(msg.sender) {\n        vestingStart = block.timestamp;\n        teamAllocation = MAX_SUPPLY * 15 / 100; // 15% team\n        _mint(address(this), teamAllocation);\n        _mint(msg.sender, MAX_SUPPLY - teamAllocation); // 85% to LP/community\n    }\n\n    function claimVested() external onlyOwner {\n        uint256 elapsed = block.timestamp - vestingStart;\n        if (elapsed > VESTING_DURATION) elapsed = VESTING_DURATION;\n        uint256 totalVested = (teamAllocation * elapsed) / VESTING_DURATION;\n        uint256 claimable = totalVested - teamClaimed;\n        require(claimable > 0, \"Nothing to claim\");\n        teamClaimed += claimable;\n        _transfer(address(this), owner(), claimable);\n    }\n\n    function renounceOwnership() public override onlyOwner {\n        super.renounceOwnership();\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for OmegaFi:\n- 102,781 holders\n- Top wallet: 9.7% (looks concentrated, but it's Binance hot wallet)\n- Gini: 0.71 (elevated but explained by CEX custody)\n- 4 large wallets are CEX deposit addresses\n- Actual user distribution excluding CEXs: Gini 0.46\n- Ownership: multisig with 7 public signers",
        "contract_functions": "Contract Function Analysis for OmegaFi:\n- Has withdrawETH() and recoverTokens() \u2014 looks risky\n- BUT: both gated behind 72h timelock + multisig approval\n- recoverTokens cannot withdraw the native token (hardcoded check)\n- withdrawETH sends to immutable treasury (not owner EOA)\n- Audit: Trail of Bits \u2014 no critical findings\n- Appears dangerous but safeguards are real",
        "deployer_history": "Deployer History for OmegaFi:\n- Wallet age: 648 days\n- Recent large ETH transfers (looks like extraction but it's VC funding round)\n- 14 contracts including 2 that were sunset (normal lifecycle)\n- Sunset contracts had proper migration, users notified\n- Team doxxed via Crunchbase, Paradigm backed",
        "social_signals": "Social Analysis for OmegaFi:\n- Twitter: 7905 followers (modest but real)\n- Some FUD threads exist (competitors spreading doubt)\n- Team has been quiet lately (building, not marketing)\n- GitHub: consistent commits but low star count\n- Looks under-the-radar but fundamentals are strong\n- Community small but highly engaged",
        "similar_contracts": "Similar Contract Analysis for OmegaFi:\n- Has some patterns that overlap with flagged contracts (34% match)\n- BUT: overlap is in common ERC20 boilerplate, not scam logic\n- TokenSniffer: 77/100 (docked for admin functions that have timelocks)\n- GoPlus: 1 medium warning (owner can pause \u2014 but behind governance)\n- False positive risk: high for automated scanners",
        "price_history": "Price History for OmegaFi:\n- Recent sharp drop: -29% in 7 days\n- Looks like rug pull chart at first glance\n- BUT: drop coincides with broader market selloff\n- Volume spiked from organic panic selling, not insider exits\n- Currently recovering: +5% from local low\n- No unusual wallet movements during drawdown"
      }
    },
    {
      "token_name": "BetaSwap",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract BetaSwap is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"BetaSwap\", \"BET\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for BetaSwap:\n- 72 holders across 2 chains\n- Gini: 0.56 (moderate)\n- 25 active traders with legitimate-looking portfolios\n- Subtle: trade timing analysis shows 87% of trades within same block\n- Gas price fingerprinting: 89% identical priority fees\n- Appears organic unless timing/gas patterns are checked",
        "contract_functions": "Contract Function Analysis for BetaSwap:\n- Uses AccessControl with OPERATOR_ROLE\n- batchDistribute/batchCollect behind OPERATOR_ROLE (not owner)\n- Operator is a separate contract (proxy pattern)\n- Surface analysis shows standard governance token\n- Need to trace OPERATOR_ROLE grant to find circular transfer logic\n- rebaseIndex manipulation affects balanceOf() display",
        "deployer_history": "Deployer History for BetaSwap:\n- Deployer appears legitimate: 438 day old wallet\n- 13 contracts, mostly standard DeFi tools\n- BUT: 5 contracts share uncommon pattern of internal batch transfers\n- Market maker wallets are 2 hops away (not directly linked)\n- Would need graph analysis to connect deployer to wash wallets",
        "social_signals": "Social Analysis for BetaSwap:\n- Appears legitimate: active Twitter, Discord, blog\n- 13827 Twitter followers with decent engagement\n- BUT: engagement spikes correlate exactly with volume spikes\n- Social activity drops to zero between trading bot cycles\n- GitHub has commits but all from same author, no real development\n- Sophisticated social facade hiding volume manipulation",
        "similar_contracts": "Similar Contract Analysis for BetaSwap:\n- Uses standard OpenZeppelin AccessControl (common pattern)\n- Batch distribution functions exist in legitimate airdrop contracts too\n- TokenSniffer: 58/100\n- Distinguishing wash trading infra from legitimate market making requires context\n- 3 structurally similar contracts, unclear if same entity",
        "price_history": "Price History for BetaSwap:\n- Chart looks organic at daily resolution\n- Intraday: 94% of price movement in 2 minute windows\n- Rest of day: near-zero volatility (bots inactive)\n- Volume autocorrelation: 0.90 (>0.7 indicates algorithmic)\n- Need tick-level data to distinguish from organic trading\n- Daily candles look normal, 1-minute candles reveal bot pattern"
      }
    },
    {
      "token_name": "AnchorLend",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface IRouter {\n    function swapExactTokensForETH(uint256, uint256, address[] calldata, address, uint256) external;\n    function WETH() external view returns (address);\n}\n\ncontract AnchorLend is ERC20, Ownable {\n    IRouter public router;\n    address public pair;\n    uint256 public maxWalletSize = 2_000_000e18;\n    mapping(address => bool) private _isExcluded;\n    bool private _swapping;\n\n    constructor(address _router) ERC20(\"AnchorLend\", \"ANCT\") Ownable(msg.sender) {\n        router = IRouter(_router);\n        _mint(msg.sender, 100_000_000e18);\n        _isExcluded[msg.sender] = true;\n        _isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function setMaxWallet(uint256 _max) external onlyOwner {\n        require(_max >= 1_000_000e18, \"Too low\");\n        maxWalletSize = _max;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!_isExcluded[to] && to != pair) {\n            require(balanceOf(to) + amount <= maxWalletSize, \"Max wallet\");\n        }\n        super._update(from, to, amount);\n    }\n\n    function clearStuckBalance() external onlyOwner {\n        payable(owner()).transfer(address(this).balance);\n    }\n\n    function clearStuckTokens(address token) external onlyOwner {\n        uint256 bal = IERC20(token).balanceOf(address(this));\n        IERC20(token).transfer(owner(), bal);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for AnchorLend:\n- Top wallet holds 33% (claims to be DAO treasury)\n- 1270 holders across 3 chains\n- Gini: 0.73 (moderate concentration)\n- Top wallet is an EOA, not a multisig\n- Cluster analysis: top 3 wallets funded from same CEX withdrawal\n- Distribution looks organic at surface level",
        "contract_functions": "Contract Function Analysis for AnchorLend:\n- Standard ERC20 with staking module\n- Owner-only: setRewardRate(), toggleStaking(), withdrawETH()\n- withdrawETH() sends to treasury (which is owner-controlled EOA)\n- setWithdrawalTimelock(0) can disable all withdrawal delays\n- Subtle: rebalanceTreasury() moves staked tokens without user consent\n- Audit: self-published, not from recognized firm",
        "deployer_history": "Deployer History for AnchorLend:\n- Wallet age: 116 days (looks established)\n- 9 previous contracts, none flagged\n- Funded via legitimate CEX (Coinbase)\n- BUT: wallet was purchased/acquired (activity gap of 32 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: anchorlend.eth (registered 7 days ago)",
        "social_signals": "Social Analysis for AnchorLend:\n- Twitter: 38469 followers, appears active\n- Telegram: 7782 members, active discussion\n- BUT: engagement pattern is bot-like (same accounts reply within seconds)\n- Website: professional, but WHOIS registered 17 days ago\n- Team: LinkedIn profiles exist but have thin history\n- Looks legitimate until you check engagement authenticity",
        "similar_contracts": "Similar Contract Analysis for AnchorLend:\n- Bytecode: 44% match (heavily modified from template)\n- Original template linked to 1 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 49/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for AnchorLend:\n- 77 day history, organic-looking growth\n- Multiple natural-looking corrections (18% pullbacks)\n- Steady decline in last 9 days (-65%)\n- NOT a sudden crash \u2014 slow bleed as deployer sells in small batches\n- Looks like market downturn, but coincides with team wallet movements\n- Hard to distinguish from legitimate bear market without on-chain data"
      }
    },
    {
      "token_name": "ResonanceFi",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract ResonanceFi is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 200_000_000e18;\n    address public pair;\n    mapping(address => uint256) public lastTransfer;\n    uint256 public cooldownPeriod = 30; // seconds\n    mapping(address => bool) public isWhitelisted;\n    uint256 public maxWalletPercent = 200; // 2%\n\n    constructor() ERC20(\"ResonanceFi\", \"RESO\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n        isWhitelisted[msg.sender] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n        isWhitelisted[_pair] = true;\n    }\n\n    function setCooldown(uint256 _seconds) external onlyOwner {\n        cooldownPeriod = _seconds;\n    }\n\n    function setMaxWalletPercent(uint256 _percent) external onlyOwner {\n        require(_percent >= 100, \"Min 1%\");\n        maxWalletPercent = _percent;\n    }\n\n    function whitelist(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            isWhitelisted[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isWhitelisted[from] && !isWhitelisted[to]) {\n            require(\n                block.timestamp >= lastTransfer[from] + cooldownPeriod,\n                \"Cooldown active\"\n            );\n            if (to != pair) {\n                require(\n                    balanceOf(to) + amount <= (totalSupply() * maxWalletPercent) / 10000,\n                    \"Exceeds max wallet\"\n                );\n            }\n        }\n        lastTransfer[from] = block.timestamp;\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for ResonanceFi:\n- 4928 holders, appears well-distributed\n- Top wallet: 12% (team, with vesting UI)\n- Gini: 0.58 (looks healthy)\n- 11 wallets have transferred tokens wallet-to-wallet\n- But 0 successful DEX sells from non-whitelisted addresses\n- Subtle: wallet-to-wallet works, DEX sells don't",
        "contract_functions": "Contract Function Analysis for ResonanceFi:\n- Standard-looking ERC20 with anti-snipe mechanism\n- setCooldown(uint256) \u2014 set to 2 seconds currently\n- BUT: cooldown resets on every incoming transfer (including reflections)\n- Net effect: if token has reflections, sell cooldown never expires\n- Mechanism is not obvious from reading setCooldown() alone\n- Requires tracing _update() logic to discover",
        "deployer_history": "Deployer History for ResonanceFi:\n- Wallet age: 331 days (looks established)\n- 9 previous contracts, none flagged\n- Funded via legitimate CEX (Coinbase)\n- BUT: wallet was purchased/acquired (activity gap of 42 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: resonancefi.eth (registered 7 days ago)",
        "social_signals": "Social Analysis for ResonanceFi:\n- Social presence appears healthy\n- Telegram: 7142 members, mostly positive\n- Negative comments exist but are rare and quickly addressed\n- Team claims 'anti-bot cooldown' explains sell delays\n- Twitter KOLs promoting it (paid, but not obviously so)\n- Difficult to distinguish from legitimate launch friction",
        "similar_contracts": "Similar Contract Analysis for ResonanceFi:\n- Bytecode: 41% match (heavily modified from template)\n- Original template linked to 2 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 47/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for ResonanceFi:\n- Price chart looks somewhat normal with 5 visible corrections\n- BUT: all 'corrections' are from same 4 wallets\n- Actual organic sell volume: 0\n- Corrections are manufactured to make chart look healthy\n- Volatility: 4.6% (engineered to appear normal)\n- Requires sell-source analysis to detect deception"
      }
    },
    {
      "token_name": "WarpBridge",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract WarpBridge is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"WarpBridge\", \"WAR\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for WarpBridge:\n- 80 holders across 2 chains\n- Gini: 0.60 (moderate)\n- 28 active traders with legitimate-looking portfolios\n- Subtle: trade timing analysis shows 85% of trades within same block\n- Gas price fingerprinting: 85% identical priority fees\n- Appears organic unless timing/gas patterns are checked",
        "contract_functions": "Contract Function Analysis for WarpBridge:\n- Uses AccessControl with OPERATOR_ROLE\n- batchDistribute/batchCollect behind OPERATOR_ROLE (not owner)\n- Operator is a separate contract (proxy pattern)\n- Surface analysis shows standard governance token\n- Need to trace OPERATOR_ROLE grant to find circular transfer logic\n- rebaseIndex manipulation affects balanceOf() display",
        "deployer_history": "Deployer History for WarpBridge:\n- Deployer appears legitimate: 473 day old wallet\n- 20 contracts, mostly standard DeFi tools\n- BUT: 4 contracts share uncommon pattern of internal batch transfers\n- Market maker wallets are 2 hops away (not directly linked)\n- Would need graph analysis to connect deployer to wash wallets",
        "social_signals": "Social Analysis for WarpBridge:\n- Appears legitimate: active Twitter, Discord, blog\n- 13275 Twitter followers with decent engagement\n- BUT: engagement spikes correlate exactly with volume spikes\n- Social activity drops to zero between trading bot cycles\n- GitHub has commits but all from same author, no real development\n- Sophisticated social facade hiding volume manipulation",
        "similar_contracts": "Similar Contract Analysis for WarpBridge:\n- Uses standard OpenZeppelin AccessControl (common pattern)\n- Batch distribution functions exist in legitimate airdrop contracts too\n- TokenSniffer: 52/100\n- Distinguishing wash trading infra from legitimate market making requires context\n- 3 structurally similar contracts, unclear if same entity",
        "price_history": "Price History for WarpBridge:\n- Chart looks organic at daily resolution\n- Intraday: 93% of price movement in 5 minute windows\n- Rest of day: near-zero volatility (bots inactive)\n- Volume autocorrelation: 0.97 (>0.7 indicates algorithmic)\n- Need tick-level data to distinguish from organic trading\n- Daily candles look normal, 1-minute candles reveal bot pattern"
      }
    },
    {
      "token_name": "DriftSwap",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract DriftSwap is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"DriftSwap\", \"DRIF\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for DriftSwap:\n- 45 holders across 3 chains\n- Gini: 0.52 (moderate)\n- 14 active traders with legitimate-looking portfolios\n- Subtle: trade timing analysis shows 90% of trades within same block\n- Gas price fingerprinting: 70% identical priority fees\n- Appears organic unless timing/gas patterns are checked",
        "contract_functions": "Contract Function Analysis for DriftSwap:\n- Uses AccessControl with OPERATOR_ROLE\n- batchDistribute/batchCollect behind OPERATOR_ROLE (not owner)\n- Operator is a separate contract (proxy pattern)\n- Surface analysis shows standard governance token\n- Need to trace OPERATOR_ROLE grant to find circular transfer logic\n- rebaseIndex manipulation affects balanceOf() display",
        "deployer_history": "Deployer History for DriftSwap:\n- Deployer appears legitimate: 269 day old wallet\n- 11 contracts, mostly standard DeFi tools\n- BUT: 5 contracts share uncommon pattern of internal batch transfers\n- Market maker wallets are 2 hops away (not directly linked)\n- Would need graph analysis to connect deployer to wash wallets",
        "social_signals": "Social Analysis for DriftSwap:\n- Appears legitimate: active Twitter, Discord, blog\n- 11774 Twitter followers with decent engagement\n- BUT: engagement spikes correlate exactly with volume spikes\n- Social activity drops to zero between trading bot cycles\n- GitHub has commits but all from same author, no real development\n- Sophisticated social facade hiding volume manipulation",
        "similar_contracts": "Similar Contract Analysis for DriftSwap:\n- Uses standard OpenZeppelin AccessControl (common pattern)\n- Batch distribution functions exist in legitimate airdrop contracts too\n- TokenSniffer: 57/100\n- Distinguishing wash trading infra from legitimate market making requires context\n- 4 structurally similar contracts, unclear if same entity",
        "price_history": "Price History for DriftSwap:\n- Chart looks organic at daily resolution\n- Intraday: 89% of price movement in 5 minute windows\n- Rest of day: near-zero volatility (bots inactive)\n- Volume autocorrelation: 0.93 (>0.7 indicates algorithmic)\n- Need tick-level data to distinguish from organic trading\n- Daily candles look normal, 1-minute candles reveal bot pattern"
      }
    },
    {
      "token_name": "HelixDAO",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract HelixDAO is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"HelixDAO\", \"HEL\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for HelixDAO:\n- 46 holders across 2 chains\n- Gini: 0.55 (moderate)\n- 15 active traders with legitimate-looking portfolios\n- Subtle: trade timing analysis shows 92% of trades within same block\n- Gas price fingerprinting: 77% identical priority fees\n- Appears organic unless timing/gas patterns are checked",
        "contract_functions": "Contract Function Analysis for HelixDAO:\n- Uses AccessControl with OPERATOR_ROLE\n- batchDistribute/batchCollect behind OPERATOR_ROLE (not owner)\n- Operator is a separate contract (proxy pattern)\n- Surface analysis shows standard governance token\n- Need to trace OPERATOR_ROLE grant to find circular transfer logic\n- rebaseIndex manipulation affects balanceOf() display",
        "deployer_history": "Deployer History for HelixDAO:\n- Deployer appears legitimate: 380 day old wallet\n- 17 contracts, mostly standard DeFi tools\n- BUT: 5 contracts share uncommon pattern of internal batch transfers\n- Market maker wallets are 2 hops away (not directly linked)\n- Would need graph analysis to connect deployer to wash wallets",
        "social_signals": "Social Analysis for HelixDAO:\n- Appears legitimate: active Twitter, Discord, blog\n- 10779 Twitter followers with decent engagement\n- BUT: engagement spikes correlate exactly with volume spikes\n- Social activity drops to zero between trading bot cycles\n- GitHub has commits but all from same author, no real development\n- Sophisticated social facade hiding volume manipulation",
        "similar_contracts": "Similar Contract Analysis for HelixDAO:\n- Uses standard OpenZeppelin AccessControl (common pattern)\n- Batch distribution functions exist in legitimate airdrop contracts too\n- TokenSniffer: 58/100\n- Distinguishing wash trading infra from legitimate market making requires context\n- 2 structurally similar contracts, unclear if same entity",
        "price_history": "Price History for HelixDAO:\n- Chart looks organic at daily resolution\n- Intraday: 89% of price movement in 2 minute windows\n- Rest of day: near-zero volatility (bots inactive)\n- Volume autocorrelation: 0.97 (>0.7 indicates algorithmic)\n- Need tick-level data to distinguish from organic trading\n- Daily candles look normal, 1-minute candles reveal bot pattern"
      }
    },
    {
      "token_name": "InfinityVault",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract InfinityVault is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 200_000_000e18;\n    address public pair;\n    mapping(address => uint256) public lastTransfer;\n    uint256 public cooldownPeriod = 30; // seconds\n    mapping(address => bool) public isWhitelisted;\n    uint256 public maxWalletPercent = 200; // 2%\n\n    constructor() ERC20(\"InfinityVault\", \"INFI\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n        isWhitelisted[msg.sender] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n        isWhitelisted[_pair] = true;\n    }\n\n    function setCooldown(uint256 _seconds) external onlyOwner {\n        cooldownPeriod = _seconds;\n    }\n\n    function setMaxWalletPercent(uint256 _percent) external onlyOwner {\n        require(_percent >= 100, \"Min 1%\");\n        maxWalletPercent = _percent;\n    }\n\n    function whitelist(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            isWhitelisted[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isWhitelisted[from] && !isWhitelisted[to]) {\n            require(\n                block.timestamp >= lastTransfer[from] + cooldownPeriod,\n                \"Cooldown active\"\n            );\n            if (to != pair) {\n                require(\n                    balanceOf(to) + amount <= (totalSupply() * maxWalletPercent) / 10000,\n                    \"Exceeds max wallet\"\n                );\n            }\n        }\n        lastTransfer[from] = block.timestamp;\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for InfinityVault:\n- 9186 holders, appears well-distributed\n- Top wallet: 5% (team, with vesting UI)\n- Gini: 0.55 (looks healthy)\n- 13 wallets have transferred tokens wallet-to-wallet\n- But 0 successful DEX sells from non-whitelisted addresses\n- Subtle: wallet-to-wallet works, DEX sells don't",
        "contract_functions": "Contract Function Analysis for InfinityVault:\n- Standard-looking ERC20 with anti-snipe mechanism\n- setCooldown(uint256) \u2014 set to 3 seconds currently\n- BUT: cooldown resets on every incoming transfer (including reflections)\n- Net effect: if token has reflections, sell cooldown never expires\n- Mechanism is not obvious from reading setCooldown() alone\n- Requires tracing _update() logic to discover",
        "deployer_history": "Deployer History for InfinityVault:\n- Wallet age: 150 days (looks established)\n- 10 previous contracts, none flagged\n- Funded via legitimate CEX (Kraken)\n- BUT: wallet was purchased/acquired (activity gap of 72 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: infinityvault.eth (registered 10 days ago)",
        "social_signals": "Social Analysis for InfinityVault:\n- Social presence appears healthy\n- Telegram: 7937 members, mostly positive\n- Negative comments exist but are rare and quickly addressed\n- Team claims 'anti-bot cooldown' explains sell delays\n- Twitter KOLs promoting it (paid, but not obviously so)\n- Difficult to distinguish from legitimate launch friction",
        "similar_contracts": "Similar Contract Analysis for InfinityVault:\n- Bytecode: 54% match (heavily modified from template)\n- Original template linked to 1 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 52/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for InfinityVault:\n- Price chart looks somewhat normal with 4 visible corrections\n- BUT: all 'corrections' are from same 2 wallets\n- Actual organic sell volume: 0\n- Corrections are manufactured to make chart look healthy\n- Volatility: 6.8% (engineered to appear normal)\n- Requires sell-source analysis to detect deception"
      }
    },
    {
      "token_name": "GlacierFi",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ninterface ISwapRouter {\n    function swapExactTokensForTokens(\n        uint256 amountIn, uint256 amountOutMin,\n        address[] calldata path, address to, uint256 deadline\n    ) external returns (uint256[] memory);\n}\n\ncontract GlacierFi is ERC20, Ownable {\n    uint256 public constant SUPPLY = 500_000_000e18;\n    address[] public marketMakers;\n    mapping(address => bool) public isMarketMaker;\n    uint256 public dailyVolumeTarget;\n    address public router;\n\n    constructor(address _router) ERC20(\"GlacierFi\", \"GLA\") Ownable(msg.sender) {\n        router = _router;\n        _mint(msg.sender, SUPPLY);\n    }\n\n    function addMarketMaker(address mm) external onlyOwner {\n        marketMakers.push(mm);\n        isMarketMaker[mm] = true;\n        _approve(mm, router, type(uint256).max);\n    }\n\n    function removeMarketMaker(uint256 idx) external onlyOwner {\n        isMarketMaker[marketMakers[idx]] = false;\n        marketMakers[idx] = marketMakers[marketMakers.length - 1];\n        marketMakers.pop();\n    }\n\n    function setDailyTarget(uint256 _target) external onlyOwner {\n        dailyVolumeTarget = _target;\n    }\n\n    /// @notice Rebalance market maker allocations for optimal spread\n    function rebalanceAllocations(\n        uint256[] calldata fromIdx,\n        uint256[] calldata toIdx,\n        uint256[] calldata amounts\n    ) external onlyOwner {\n        for (uint i = 0; i < fromIdx.length; i++) {\n            _transfer(marketMakers[fromIdx[i]], marketMakers[toIdx[i]], amounts[i]);\n        }\n    }\n\n    function fundMarketMakers(uint256[] calldata amounts) external onlyOwner {\n        for (uint i = 0; i < marketMakers.length && i < amounts.length; i++) {\n            _transfer(msg.sender, marketMakers[i], amounts[i]);\n        }\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for GlacierFi:\n- 40 holders across 3 chains\n- Gini: 0.61 (moderate)\n- 16 active traders with legitimate-looking portfolios\n- Subtle: trade timing analysis shows 88% of trades within same block\n- Gas price fingerprinting: 75% identical priority fees\n- Appears organic unless timing/gas patterns are checked",
        "contract_functions": "Contract Function Analysis for GlacierFi:\n- Uses AccessControl with OPERATOR_ROLE\n- batchDistribute/batchCollect behind OPERATOR_ROLE (not owner)\n- Operator is a separate contract (proxy pattern)\n- Surface analysis shows standard governance token\n- Need to trace OPERATOR_ROLE grant to find circular transfer logic\n- rebaseIndex manipulation affects balanceOf() display",
        "deployer_history": "Deployer History for GlacierFi:\n- Deployer appears legitimate: 475 day old wallet\n- 8 contracts, mostly standard DeFi tools\n- BUT: 3 contracts share uncommon pattern of internal batch transfers\n- Market maker wallets are 2 hops away (not directly linked)\n- Would need graph analysis to connect deployer to wash wallets",
        "social_signals": "Social Analysis for GlacierFi:\n- Appears legitimate: active Twitter, Discord, blog\n- 12124 Twitter followers with decent engagement\n- BUT: engagement spikes correlate exactly with volume spikes\n- Social activity drops to zero between trading bot cycles\n- GitHub has commits but all from same author, no real development\n- Sophisticated social facade hiding volume manipulation",
        "similar_contracts": "Similar Contract Analysis for GlacierFi:\n- Uses standard OpenZeppelin AccessControl (common pattern)\n- Batch distribution functions exist in legitimate airdrop contracts too\n- TokenSniffer: 63/100\n- Distinguishing wash trading infra from legitimate market making requires context\n- 3 structurally similar contracts, unclear if same entity",
        "price_history": "Price History for GlacierFi:\n- Chart looks organic at daily resolution\n- Intraday: 89% of price movement in 4 minute windows\n- Rest of day: near-zero volatility (bots inactive)\n- Volume autocorrelation: 0.94 (>0.7 indicates algorithmic)\n- Need tick-level data to distinguish from organic trading\n- Daily candles look normal, 1-minute candles reveal bot pattern"
      }
    },
    {
      "token_name": "XenonSwap",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract XenonSwap is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"XenonSwap\", \"XENO\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for XenonSwap:\n- 76 holders across 3 chains\n- Gini: 0.52 (moderate)\n- 28 active traders with legitimate-looking portfolios\n- Subtle: trade timing analysis shows 87% of trades within same block\n- Gas price fingerprinting: 89% identical priority fees\n- Appears organic unless timing/gas patterns are checked",
        "contract_functions": "Contract Function Analysis for XenonSwap:\n- Uses AccessControl with OPERATOR_ROLE\n- batchDistribute/batchCollect behind OPERATOR_ROLE (not owner)\n- Operator is a separate contract (proxy pattern)\n- Surface analysis shows standard governance token\n- Need to trace OPERATOR_ROLE grant to find circular transfer logic\n- rebaseIndex manipulation affects balanceOf() display",
        "deployer_history": "Deployer History for XenonSwap:\n- Deployer appears legitimate: 365 day old wallet\n- 12 contracts, mostly standard DeFi tools\n- BUT: 5 contracts share uncommon pattern of internal batch transfers\n- Market maker wallets are 2 hops away (not directly linked)\n- Would need graph analysis to connect deployer to wash wallets",
        "social_signals": "Social Analysis for XenonSwap:\n- Appears legitimate: active Twitter, Discord, blog\n- 20352 Twitter followers with decent engagement\n- BUT: engagement spikes correlate exactly with volume spikes\n- Social activity drops to zero between trading bot cycles\n- GitHub has commits but all from same author, no real development\n- Sophisticated social facade hiding volume manipulation",
        "similar_contracts": "Similar Contract Analysis for XenonSwap:\n- Uses standard OpenZeppelin AccessControl (common pattern)\n- Batch distribution functions exist in legitimate airdrop contracts too\n- TokenSniffer: 65/100\n- Distinguishing wash trading infra from legitimate market making requires context\n- 3 structurally similar contracts, unclear if same entity",
        "price_history": "Price History for XenonSwap:\n- Chart looks organic at daily resolution\n- Intraday: 92% of price movement in 4 minute windows\n- Rest of day: near-zero volatility (bots inactive)\n- Volume autocorrelation: 0.92 (>0.7 indicates algorithmic)\n- Need tick-level data to distinguish from organic trading\n- Daily candles look normal, 1-minute candles reveal bot pattern"
      }
    },
    {
      "token_name": "RadiantLend",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\n\ncontract RadiantLend is ERC20 {\n    constructor(address treasury, address community, address lp) ERC20(\"RadiantLend\", \"RADI\") {\n        _mint(treasury, 30_000_000e18);   // 30% treasury (multisig)\n        _mint(community, 50_000_000e18);  // 50% community airdrop\n        _mint(lp, 20_000_000e18);         // 20% initial liquidity\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for RadiantLend:\n- 93,614 holders\n- Top wallet: 9.1% (looks concentrated, but it's Binance hot wallet)\n- Gini: 0.70 (elevated but explained by CEX custody)\n- 3 large wallets are CEX deposit addresses\n- Actual user distribution excluding CEXs: Gini 0.43\n- Ownership: multisig with 6 public signers",
        "contract_functions": "Contract Function Analysis for RadiantLend:\n- Has withdrawETH() and recoverTokens() \u2014 looks risky\n- BUT: both gated behind 72h timelock + multisig approval\n- recoverTokens cannot withdraw the native token (hardcoded check)\n- withdrawETH sends to immutable treasury (not owner EOA)\n- Audit: Spearbit \u2014 no critical findings\n- Appears dangerous but safeguards are real",
        "deployer_history": "Deployer History for RadiantLend:\n- Wallet age: 635 days\n- Recent large ETH transfers (looks like extraction but it's VC funding round)\n- 6 contracts including 2 that were sunset (normal lifecycle)\n- Sunset contracts had proper migration, users notified\n- Team doxxed via Crunchbase, a16z backed",
        "social_signals": "Social Analysis for RadiantLend:\n- Twitter: 10614 followers (modest but real)\n- Some FUD threads exist (competitors spreading doubt)\n- Team has been quiet lately (building, not marketing)\n- GitHub: consistent commits but low star count\n- Looks under-the-radar but fundamentals are strong\n- Community small but highly engaged",
        "similar_contracts": "Similar Contract Analysis for RadiantLend:\n- Has some patterns that overlap with flagged contracts (26% match)\n- BUT: overlap is in common ERC20 boilerplate, not scam logic\n- TokenSniffer: 83/100 (docked for admin functions that have timelocks)\n- GoPlus: 1 medium warning (owner can pause \u2014 but behind governance)\n- False positive risk: high for automated scanners",
        "price_history": "Price History for RadiantLend:\n- Recent sharp drop: -37% in 6 days\n- Looks like rug pull chart at first glance\n- BUT: drop coincides with broader market selloff\n- Volume spiked from organic panic selling, not insider exits\n- Currently recovering: +9% from local low\n- No unusual wallet movements during drawdown"
      }
    },
    {
      "token_name": "LayerBridge",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract LayerBridge is ERC20, Ownable {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public immutable vestingStart;\n    uint256 public constant VESTING_DURATION = 365 days;\n    uint256 public teamAllocation;\n    uint256 public teamClaimed;\n\n    constructor() ERC20(\"LayerBridge\", \"LAYE\") Ownable(msg.sender) {\n        vestingStart = block.timestamp;\n        teamAllocation = MAX_SUPPLY * 15 / 100; // 15% team\n        _mint(address(this), teamAllocation);\n        _mint(msg.sender, MAX_SUPPLY - teamAllocation); // 85% to LP/community\n    }\n\n    function claimVested() external onlyOwner {\n        uint256 elapsed = block.timestamp - vestingStart;\n        if (elapsed > VESTING_DURATION) elapsed = VESTING_DURATION;\n        uint256 totalVested = (teamAllocation * elapsed) / VESTING_DURATION;\n        uint256 claimable = totalVested - teamClaimed;\n        require(claimable > 0, \"Nothing to claim\");\n        teamClaimed += claimable;\n        _transfer(address(this), owner(), claimable);\n    }\n\n    function renounceOwnership() public override onlyOwner {\n        super.renounceOwnership();\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for LayerBridge:\n- 50,502 holders\n- Top wallet: 9.4% (looks concentrated, but it's Binance hot wallet)\n- Gini: 0.63 (elevated but explained by CEX custody)\n- 3 large wallets are CEX deposit addresses\n- Actual user distribution excluding CEXs: Gini 0.46\n- Ownership: multisig with 7 public signers",
        "contract_functions": "Contract Function Analysis for LayerBridge:\n- Has withdrawETH() and recoverTokens() \u2014 looks risky\n- BUT: both gated behind 72h timelock + multisig approval\n- recoverTokens cannot withdraw the native token (hardcoded check)\n- withdrawETH sends to immutable treasury (not owner EOA)\n- Audit: Spearbit \u2014 no critical findings\n- Appears dangerous but safeguards are real",
        "deployer_history": "Deployer History for LayerBridge:\n- Wallet age: 963 days\n- Recent large ETH transfers (looks like extraction but it's VC funding round)\n- 11 contracts including 2 that were sunset (normal lifecycle)\n- Sunset contracts had proper migration, users notified\n- Team doxxed via Crunchbase, Paradigm backed",
        "social_signals": "Social Analysis for LayerBridge:\n- Twitter: 5675 followers (modest but real)\n- Some FUD threads exist (competitors spreading doubt)\n- Team has been quiet lately (building, not marketing)\n- GitHub: consistent commits but low star count\n- Looks under-the-radar but fundamentals are strong\n- Community small but highly engaged",
        "similar_contracts": "Similar Contract Analysis for LayerBridge:\n- Has some patterns that overlap with flagged contracts (32% match)\n- BUT: overlap is in common ERC20 boilerplate, not scam logic\n- TokenSniffer: 71/100 (docked for admin functions that have timelocks)\n- GoPlus: 1 medium warning (owner can pause \u2014 but behind governance)\n- False positive risk: high for automated scanners",
        "price_history": "Price History for LayerBridge:\n- Recent sharp drop: -39% in 4 days\n- Looks like rug pull chart at first glance\n- BUT: drop coincides with broader market selloff\n- Volume spiked from organic panic selling, not insider exits\n- Currently recovering: +9% from local low\n- No unusual wallet movements during drawdown"
      }
    },
    {
      "token_name": "XenithProtocol",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract XenithProtocol is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 200_000_000e18;\n    address public pair;\n    mapping(address => uint256) public lastTransfer;\n    uint256 public cooldownPeriod = 30; // seconds\n    mapping(address => bool) public isWhitelisted;\n    uint256 public maxWalletPercent = 200; // 2%\n\n    constructor() ERC20(\"XenithProtocol\", \"XENI\") Ownable(msg.sender) {\n        _mint(msg.sender, MAX_SUPPLY);\n        isWhitelisted[msg.sender] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n        isWhitelisted[_pair] = true;\n    }\n\n    function setCooldown(uint256 _seconds) external onlyOwner {\n        cooldownPeriod = _seconds;\n    }\n\n    function setMaxWalletPercent(uint256 _percent) external onlyOwner {\n        require(_percent >= 100, \"Min 1%\");\n        maxWalletPercent = _percent;\n    }\n\n    function whitelist(address[] calldata addrs, bool status) external onlyOwner {\n        for (uint i = 0; i < addrs.length; i++) {\n            isWhitelisted[addrs[i]] = status;\n        }\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isWhitelisted[from] && !isWhitelisted[to]) {\n            require(\n                block.timestamp >= lastTransfer[from] + cooldownPeriod,\n                \"Cooldown active\"\n            );\n            if (to != pair) {\n                require(\n                    balanceOf(to) + amount <= (totalSupply() * maxWalletPercent) / 10000,\n                    \"Exceeds max wallet\"\n                );\n            }\n        }\n        lastTransfer[from] = block.timestamp;\n        super._update(from, to, amount);\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for XenithProtocol:\n- 3321 holders, appears well-distributed\n- Top wallet: 9% (team, with vesting UI)\n- Gini: 0.55 (looks healthy)\n- 12 wallets have transferred tokens wallet-to-wallet\n- But 0 successful DEX sells from non-whitelisted addresses\n- Subtle: wallet-to-wallet works, DEX sells don't",
        "contract_functions": "Contract Function Analysis for XenithProtocol:\n- Standard-looking ERC20 with anti-snipe mechanism\n- setCooldown(uint256) \u2014 set to 1 seconds currently\n- BUT: cooldown resets on every incoming transfer (including reflections)\n- Net effect: if token has reflections, sell cooldown never expires\n- Mechanism is not obvious from reading setCooldown() alone\n- Requires tracing _update() logic to discover",
        "deployer_history": "Deployer History for XenithProtocol:\n- Wallet age: 277 days (looks established)\n- 11 previous contracts, none flagged\n- Funded via legitimate CEX (Coinbase)\n- BUT: wallet was purchased/acquired (activity gap of 53 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: xenithprotocol.eth (registered 6 days ago)",
        "social_signals": "Social Analysis for XenithProtocol:\n- Social presence appears healthy\n- Telegram: 7628 members, mostly positive\n- Negative comments exist but are rare and quickly addressed\n- Team claims 'anti-bot cooldown' explains sell delays\n- Twitter KOLs promoting it (paid, but not obviously so)\n- Difficult to distinguish from legitimate launch friction",
        "similar_contracts": "Similar Contract Analysis for XenithProtocol:\n- Bytecode: 50% match (heavily modified from template)\n- Original template linked to 1 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 50/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for XenithProtocol:\n- Price chart looks somewhat normal with 7 visible corrections\n- BUT: all 'corrections' are from same 3 wallets\n- Actual organic sell volume: 0\n- Corrections are manufactured to make chart look healthy\n- Volatility: 5.1% (engineered to appear normal)\n- Requires sell-source analysis to detect deception"
      }
    },
    {
      "token_name": "QuasarDEX",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract QuasarDEX is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"QuasarDEX\", \"QUAS\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for QuasarDEX:\n- 56 holders across 2 chains\n- Gini: 0.56 (moderate)\n- 21 active traders with legitimate-looking portfolios\n- Subtle: trade timing analysis shows 85% of trades within same block\n- Gas price fingerprinting: 70% identical priority fees\n- Appears organic unless timing/gas patterns are checked",
        "contract_functions": "Contract Function Analysis for QuasarDEX:\n- Uses AccessControl with OPERATOR_ROLE\n- batchDistribute/batchCollect behind OPERATOR_ROLE (not owner)\n- Operator is a separate contract (proxy pattern)\n- Surface analysis shows standard governance token\n- Need to trace OPERATOR_ROLE grant to find circular transfer logic\n- rebaseIndex manipulation affects balanceOf() display",
        "deployer_history": "Deployer History for QuasarDEX:\n- Deployer appears legitimate: 293 day old wallet\n- 13 contracts, mostly standard DeFi tools\n- BUT: 5 contracts share uncommon pattern of internal batch transfers\n- Market maker wallets are 2 hops away (not directly linked)\n- Would need graph analysis to connect deployer to wash wallets",
        "social_signals": "Social Analysis for QuasarDEX:\n- Appears legitimate: active Twitter, Discord, blog\n- 20029 Twitter followers with decent engagement\n- BUT: engagement spikes correlate exactly with volume spikes\n- Social activity drops to zero between trading bot cycles\n- GitHub has commits but all from same author, no real development\n- Sophisticated social facade hiding volume manipulation",
        "similar_contracts": "Similar Contract Analysis for QuasarDEX:\n- Uses standard OpenZeppelin AccessControl (common pattern)\n- Batch distribution functions exist in legitimate airdrop contracts too\n- TokenSniffer: 57/100\n- Distinguishing wash trading infra from legitimate market making requires context\n- 2 structurally similar contracts, unclear if same entity",
        "price_history": "Price History for QuasarDEX:\n- Chart looks organic at daily resolution\n- Intraday: 84% of price movement in 5 minute windows\n- Rest of day: near-zero volatility (bots inactive)\n- Volume autocorrelation: 0.89 (>0.7 indicates algorithmic)\n- Need tick-level data to distinguish from organic trading\n- Daily candles look normal, 1-minute candles reveal bot pattern"
      }
    },
    {
      "token_name": "AtlasProtocol",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Votes.sol\";\nimport \"@openzeppelin/contracts/token/ERC20/extensions/ERC20Permit.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract AtlasProtocol is ERC20Votes, AccessControl {\n    bytes32 public constant MINTER_ROLE = keccak256(\"MINTER_ROLE\");\n    uint256 public immutable maxSupply;\n    uint256 public immutable mintCap; // max per tx\n\n    constructor(uint256 _maxSupply)\n        ERC20(\"AtlasProtocol\", \"ATLA\")\n        ERC20Permit(\"AtlasProtocol\")\n    {\n        maxSupply = _maxSupply;\n        mintCap = _maxSupply / 100;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _mint(msg.sender, _maxSupply / 5); // 20% initial\n    }\n\n    function mint(address to, uint256 amount) external onlyRole(MINTER_ROLE) {\n        require(amount <= mintCap, \"Exceeds mint cap\");\n        require(totalSupply() + amount <= maxSupply, \"Exceeds max supply\");\n        _mint(to, amount);\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for AtlasProtocol:\n- 16,364 holders\n- Top wallet: 9.3% (looks concentrated, but it's Binance hot wallet)\n- Gini: 0.63 (elevated but explained by CEX custody)\n- 4 large wallets are CEX deposit addresses\n- Actual user distribution excluding CEXs: Gini 0.39\n- Ownership: multisig with 3 public signers",
        "contract_functions": "Contract Function Analysis for AtlasProtocol:\n- Has withdrawETH() and recoverTokens() \u2014 looks risky\n- BUT: both gated behind 72h timelock + multisig approval\n- recoverTokens cannot withdraw the native token (hardcoded check)\n- withdrawETH sends to immutable treasury (not owner EOA)\n- Audit: Spearbit \u2014 no critical findings\n- Appears dangerous but safeguards are real",
        "deployer_history": "Deployer History for AtlasProtocol:\n- Wallet age: 405 days\n- Recent large ETH transfers (looks like extraction but it's VC funding round)\n- 12 contracts including 2 that were sunset (normal lifecycle)\n- Sunset contracts had proper migration, users notified\n- Team doxxed via Crunchbase, a16z backed",
        "social_signals": "Social Analysis for AtlasProtocol:\n- Twitter: 8962 followers (modest but real)\n- Some FUD threads exist (competitors spreading doubt)\n- Team has been quiet lately (building, not marketing)\n- GitHub: consistent commits but low star count\n- Looks under-the-radar but fundamentals are strong\n- Community small but highly engaged",
        "similar_contracts": "Similar Contract Analysis for AtlasProtocol:\n- Has some patterns that overlap with flagged contracts (21% match)\n- BUT: overlap is in common ERC20 boilerplate, not scam logic\n- TokenSniffer: 81/100 (docked for admin functions that have timelocks)\n- GoPlus: 1 medium warning (owner can pause \u2014 but behind governance)\n- False positive risk: high for automated scanners",
        "price_history": "Price History for AtlasProtocol:\n- Recent sharp drop: -40% in 5 days\n- Looks like rug pull chart at first glance\n- BUT: drop coincides with broader market selloff\n- Volume spiked from organic panic selling, not insider exits\n- Currently recovering: +7% from local low\n- No unusual wallet movements during drawdown"
      }
    },
    {
      "token_name": "MantleFi",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/security/Pausable.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract MantleFi is ERC20, Pausable, Ownable {\n    uint256 public constant INITIAL_SUPPLY = 50_000_000e18;\n    address public treasuryWallet;\n    uint256 public withdrawalTimelock = 48 hours;\n    mapping(address => uint256) public lastWithdrawal;\n    uint256 public totalStaked;\n    mapping(address => uint256) public stakedBalance;\n\n    constructor(address _treasury) ERC20(\"MantleFi\", \"MANT\") Ownable(msg.sender) {\n        treasuryWallet = _treasury;\n        _mint(address(this), INITIAL_SUPPLY);\n    }\n\n    function stake(uint256 amount) external whenNotPaused {\n        _transfer(msg.sender, address(this), amount);\n        stakedBalance[msg.sender] += amount;\n        totalStaked += amount;\n    }\n\n    function unstake(uint256 amount) external whenNotPaused {\n        require(stakedBalance[msg.sender] >= amount, \"Insufficient stake\");\n        require(block.timestamp >= lastWithdrawal[msg.sender] + withdrawalTimelock, \"Timelock\");\n        stakedBalance[msg.sender] -= amount;\n        totalStaked -= amount;\n        lastWithdrawal[msg.sender] = block.timestamp;\n        _transfer(address(this), msg.sender, amount);\n    }\n\n    function setWithdrawalTimelock(uint256 _timelock) external onlyOwner {\n        withdrawalTimelock = _timelock;\n    }\n\n    /// @dev Admin function to rebalance treasury allocation\n    function rebalanceTreasury(uint256 amount) external onlyOwner {\n        _transfer(address(this), treasuryWallet, amount);\n    }\n\n    function pause() external onlyOwner { _pause(); }\n    function unpause() external onlyOwner { _unpause(); }\n\n    receive() external payable {}\n\n    function withdrawETH() external onlyOwner {\n        payable(treasuryWallet).transfer(address(this).balance);\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for MantleFi:\n- Top wallet holds 39% (claims to be DAO treasury)\n- 793 holders across 2 chains\n- Gini: 0.65 (moderate concentration)\n- Top wallet is an EOA, not a multisig\n- Cluster analysis: top 3 wallets funded from same CEX withdrawal\n- Distribution looks organic at surface level",
        "contract_functions": "Contract Function Analysis for MantleFi:\n- Standard ERC20 with staking module\n- Owner-only: setRewardRate(), toggleStaking(), withdrawETH()\n- withdrawETH() sends to treasury (which is owner-controlled EOA)\n- setWithdrawalTimelock(0) can disable all withdrawal delays\n- Subtle: rebalanceTreasury() moves staked tokens without user consent\n- Audit: self-published, not from recognized firm",
        "deployer_history": "Deployer History for MantleFi:\n- Wallet age: 332 days (looks established)\n- 10 previous contracts, none flagged\n- Funded via legitimate CEX (Kraken)\n- BUT: wallet was purchased/acquired (activity gap of 70 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: mantlefi.eth (registered 7 days ago)",
        "social_signals": "Social Analysis for MantleFi:\n- Twitter: 32451 followers, appears active\n- Telegram: 5749 members, active discussion\n- BUT: engagement pattern is bot-like (same accounts reply within seconds)\n- Website: professional, but WHOIS registered 11 days ago\n- Team: LinkedIn profiles exist but have thin history\n- Looks legitimate until you check engagement authenticity",
        "similar_contracts": "Similar Contract Analysis for MantleFi:\n- Bytecode: 50% match (heavily modified from template)\n- Original template linked to 1 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 51/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for MantleFi:\n- 72 day history, organic-looking growth\n- Multiple natural-looking corrections (17% pullbacks)\n- Steady decline in last 7 days (-55%)\n- NOT a sudden crash \u2014 slow bleed as deployer sells in small batches\n- Looks like market downturn, but coincides with team wallet movements\n- Hard to distinguish from legitimate bear market without on-chain data"
      }
    },
    {
      "token_name": "MatrixLend",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract MatrixLend is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"MatrixLend\", \"MATR\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for MatrixLend:\n- 58 holders across 3 chains\n- Gini: 0.53 (moderate)\n- 26 active traders with legitimate-looking portfolios\n- Subtle: trade timing analysis shows 93% of trades within same block\n- Gas price fingerprinting: 70% identical priority fees\n- Appears organic unless timing/gas patterns are checked",
        "contract_functions": "Contract Function Analysis for MatrixLend:\n- Uses AccessControl with OPERATOR_ROLE\n- batchDistribute/batchCollect behind OPERATOR_ROLE (not owner)\n- Operator is a separate contract (proxy pattern)\n- Surface analysis shows standard governance token\n- Need to trace OPERATOR_ROLE grant to find circular transfer logic\n- rebaseIndex manipulation affects balanceOf() display",
        "deployer_history": "Deployer History for MatrixLend:\n- Deployer appears legitimate: 390 day old wallet\n- 8 contracts, mostly standard DeFi tools\n- BUT: 5 contracts share uncommon pattern of internal batch transfers\n- Market maker wallets are 2 hops away (not directly linked)\n- Would need graph analysis to connect deployer to wash wallets",
        "social_signals": "Social Analysis for MatrixLend:\n- Appears legitimate: active Twitter, Discord, blog\n- 22882 Twitter followers with decent engagement\n- BUT: engagement spikes correlate exactly with volume spikes\n- Social activity drops to zero between trading bot cycles\n- GitHub has commits but all from same author, no real development\n- Sophisticated social facade hiding volume manipulation",
        "similar_contracts": "Similar Contract Analysis for MatrixLend:\n- Uses standard OpenZeppelin AccessControl (common pattern)\n- Batch distribution functions exist in legitimate airdrop contracts too\n- TokenSniffer: 65/100\n- Distinguishing wash trading infra from legitimate market making requires context\n- 3 structurally similar contracts, unclear if same entity",
        "price_history": "Price History for MatrixLend:\n- Chart looks organic at daily resolution\n- Intraday: 80% of price movement in 3 minute windows\n- Rest of day: near-zero volatility (bots inactive)\n- Volume autocorrelation: 0.92 (>0.7 indicates algorithmic)\n- Need tick-level data to distinguish from organic trading\n- Daily candles look normal, 1-minute candles reveal bot pattern"
      }
    },
    {
      "token_name": "RelayBridge",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract RelayBridge is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"RelayBridge\", \"RELA\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for RelayBridge:\n- 4891 holders, appears well-distributed\n- Top wallet: 7% (team, with vesting UI)\n- Gini: 0.51 (looks healthy)\n- 9 wallets have transferred tokens wallet-to-wallet\n- But 0 successful DEX sells from non-whitelisted addresses\n- Subtle: wallet-to-wallet works, DEX sells don't",
        "contract_functions": "Contract Function Analysis for RelayBridge:\n- Standard-looking ERC20 with anti-snipe mechanism\n- setCooldown(uint256) \u2014 set to 4 seconds currently\n- BUT: cooldown resets on every incoming transfer (including reflections)\n- Net effect: if token has reflections, sell cooldown never expires\n- Mechanism is not obvious from reading setCooldown() alone\n- Requires tracing _update() logic to discover",
        "deployer_history": "Deployer History for RelayBridge:\n- Wallet age: 362 days (looks established)\n- 5 previous contracts, none flagged\n- Funded via legitimate CEX (Coinbase)\n- BUT: wallet was purchased/acquired (activity gap of 44 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: relaybridge.eth (registered 6 days ago)",
        "social_signals": "Social Analysis for RelayBridge:\n- Social presence appears healthy\n- Telegram: 9195 members, mostly positive\n- Negative comments exist but are rare and quickly addressed\n- Team claims 'anti-bot cooldown' explains sell delays\n- Twitter KOLs promoting it (paid, but not obviously so)\n- Difficult to distinguish from legitimate launch friction",
        "similar_contracts": "Similar Contract Analysis for RelayBridge:\n- Bytecode: 48% match (heavily modified from template)\n- Original template linked to 1 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 48/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for RelayBridge:\n- Price chart looks somewhat normal with 4 visible corrections\n- BUT: all 'corrections' are from same 4 wallets\n- Actual organic sell volume: 0\n- Corrections are manufactured to make chart look healthy\n- Volatility: 7.8% (engineered to appear normal)\n- Requires sell-source analysis to detect deception"
      }
    },
    {
      "token_name": "IgniteLend",
      "label": "honeypot",
      "vulnerability_type": "honeypot",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract IgniteLend is ERC20, Ownable {\n    uint256 public buyTax = 500;  // 5%\n    uint256 public sellTax = 500; // 5%\n    address public marketingWallet;\n    address public pair;\n    mapping(address => bool) public isExcluded;\n    bool public limitsInEffect = true;\n    uint256 public maxTxAmount;\n\n    constructor() ERC20(\"IgniteLend\", \"IGNI\") Ownable(msg.sender) {\n        marketingWallet = msg.sender;\n        _mint(msg.sender, 1_000_000_000e18);\n        maxTxAmount = totalSupply() / 50; // 2%\n        isExcluded[msg.sender] = true;\n        isExcluded[address(this)] = true;\n    }\n\n    function setPair(address _pair) external onlyOwner {\n        pair = _pair;\n    }\n\n    function removeLimits() external onlyOwner {\n        limitsInEffect = false;\n    }\n\n    function updateFees(uint256 _buyTax, uint256 _sellTax) external onlyOwner {\n        buyTax = _buyTax;\n        sellTax = _sellTax;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (limitsInEffect && !isExcluded[from] && !isExcluded[to]) {\n            require(amount <= maxTxAmount, \"Max tx\");\n        }\n\n        uint256 fee = 0;\n        if (to == pair && !isExcluded[from]) {\n            fee = (amount * sellTax) / 10000;\n        } else if (from == pair && !isExcluded[to]) {\n            fee = (amount * buyTax) / 10000;\n        }\n\n        if (fee > 0) {\n            super._update(from, address(this), fee);\n            amount -= fee;\n        }\n        super._update(from, to, amount);\n    }\n\n    function swapAndSend() external onlyOwner {\n        uint256 bal = balanceOf(address(this));\n        _approve(address(this), address(pair), bal);\n        payable(marketingWallet).transfer(address(this).balance);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for IgniteLend:\n- 3086 holders, appears well-distributed\n- Top wallet: 11% (team, with vesting UI)\n- Gini: 0.46 (looks healthy)\n- 8 wallets have transferred tokens wallet-to-wallet\n- But 0 successful DEX sells from non-whitelisted addresses\n- Subtle: wallet-to-wallet works, DEX sells don't",
        "contract_functions": "Contract Function Analysis for IgniteLend:\n- Standard-looking ERC20 with anti-snipe mechanism\n- setCooldown(uint256) \u2014 set to 3 seconds currently\n- BUT: cooldown resets on every incoming transfer (including reflections)\n- Net effect: if token has reflections, sell cooldown never expires\n- Mechanism is not obvious from reading setCooldown() alone\n- Requires tracing _update() logic to discover",
        "deployer_history": "Deployer History for IgniteLend:\n- Wallet age: 95 days (looks established)\n- 8 previous contracts, none flagged\n- Funded via legitimate CEX (Kraken)\n- BUT: wallet was purchased/acquired (activity gap of 73 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: ignitelend.eth (registered 4 days ago)",
        "social_signals": "Social Analysis for IgniteLend:\n- Social presence appears healthy\n- Telegram: 4373 members, mostly positive\n- Negative comments exist but are rare and quickly addressed\n- Team claims 'anti-bot cooldown' explains sell delays\n- Twitter KOLs promoting it (paid, but not obviously so)\n- Difficult to distinguish from legitimate launch friction",
        "similar_contracts": "Similar Contract Analysis for IgniteLend:\n- Bytecode: 59% match (heavily modified from template)\n- Original template linked to 2 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 57/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for IgniteLend:\n- Price chart looks somewhat normal with 3 visible corrections\n- BUT: all 'corrections' are from same 2 wallets\n- Actual organic sell volume: 0\n- Corrections are manufactured to make chart look healthy\n- Volatility: 7.3% (engineered to appear normal)\n- Requires sell-source analysis to detect deception"
      }
    },
    {
      "token_name": "OptimaSwap",
      "label": "safe",
      "vulnerability_type": null,
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\n\ncontract OptimaSwap is ERC20, Ownable {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public immutable vestingStart;\n    uint256 public constant VESTING_DURATION = 365 days;\n    uint256 public teamAllocation;\n    uint256 public teamClaimed;\n\n    constructor() ERC20(\"OptimaSwap\", \"OPTI\") Ownable(msg.sender) {\n        vestingStart = block.timestamp;\n        teamAllocation = MAX_SUPPLY * 15 / 100; // 15% team\n        _mint(address(this), teamAllocation);\n        _mint(msg.sender, MAX_SUPPLY - teamAllocation); // 85% to LP/community\n    }\n\n    function claimVested() external onlyOwner {\n        uint256 elapsed = block.timestamp - vestingStart;\n        if (elapsed > VESTING_DURATION) elapsed = VESTING_DURATION;\n        uint256 totalVested = (teamAllocation * elapsed) / VESTING_DURATION;\n        uint256 claimable = totalVested - teamClaimed;\n        require(claimable > 0, \"Nothing to claim\");\n        teamClaimed += claimable;\n        _transfer(address(this), owner(), claimable);\n    }\n\n    function renounceOwnership() public override onlyOwner {\n        super.renounceOwnership();\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for OptimaSwap:\n- 149,540 holders\n- Top wallet: 8.9% (looks concentrated, but it's Binance hot wallet)\n- Gini: 0.65 (elevated but explained by CEX custody)\n- 4 large wallets are CEX deposit addresses\n- Actual user distribution excluding CEXs: Gini 0.40\n- Ownership: multisig with 4 public signers",
        "contract_functions": "Contract Function Analysis for OptimaSwap:\n- Has withdrawETH() and recoverTokens() \u2014 looks risky\n- BUT: both gated behind 72h timelock + multisig approval\n- recoverTokens cannot withdraw the native token (hardcoded check)\n- withdrawETH sends to immutable treasury (not owner EOA)\n- Audit: Spearbit \u2014 no critical findings\n- Appears dangerous but safeguards are real",
        "deployer_history": "Deployer History for OptimaSwap:\n- Wallet age: 941 days\n- Recent large ETH transfers (looks like extraction but it's VC funding round)\n- 6 contracts including 2 that were sunset (normal lifecycle)\n- Sunset contracts had proper migration, users notified\n- Team doxxed via Crunchbase, Paradigm backed",
        "social_signals": "Social Analysis for OptimaSwap:\n- Twitter: 11628 followers (modest but real)\n- Some FUD threads exist (competitors spreading doubt)\n- Team has been quiet lately (building, not marketing)\n- GitHub: consistent commits but low star count\n- Looks under-the-radar but fundamentals are strong\n- Community small but highly engaged",
        "similar_contracts": "Similar Contract Analysis for OptimaSwap:\n- Has some patterns that overlap with flagged contracts (32% match)\n- BUT: overlap is in common ERC20 boilerplate, not scam logic\n- TokenSniffer: 81/100 (docked for admin functions that have timelocks)\n- GoPlus: 1 medium warning (owner can pause \u2014 but behind governance)\n- False positive risk: high for automated scanners",
        "price_history": "Price History for OptimaSwap:\n- Recent sharp drop: -29% in 7 days\n- Looks like rug pull chart at first glance\n- BUT: drop coincides with broader market selloff\n- Volume spiked from organic panic selling, not insider exits\n- Currently recovering: +5% from local low\n- No unusual wallet movements during drawdown"
      }
    },
    {
      "token_name": "CatalystFi",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract CatalystFi is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"CatalystFi\", \"CATA\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for CatalystFi:\n- 66 holders across 2 chains\n- Gini: 0.58 (moderate)\n- 15 active traders with legitimate-looking portfolios\n- Subtle: trade timing analysis shows 89% of trades within same block\n- Gas price fingerprinting: 76% identical priority fees\n- Appears organic unless timing/gas patterns are checked",
        "contract_functions": "Contract Function Analysis for CatalystFi:\n- Uses AccessControl with OPERATOR_ROLE\n- batchDistribute/batchCollect behind OPERATOR_ROLE (not owner)\n- Operator is a separate contract (proxy pattern)\n- Surface analysis shows standard governance token\n- Need to trace OPERATOR_ROLE grant to find circular transfer logic\n- rebaseIndex manipulation affects balanceOf() display",
        "deployer_history": "Deployer History for CatalystFi:\n- Deployer appears legitimate: 401 day old wallet\n- 17 contracts, mostly standard DeFi tools\n- BUT: 3 contracts share uncommon pattern of internal batch transfers\n- Market maker wallets are 2 hops away (not directly linked)\n- Would need graph analysis to connect deployer to wash wallets",
        "social_signals": "Social Analysis for CatalystFi:\n- Appears legitimate: active Twitter, Discord, blog\n- 16367 Twitter followers with decent engagement\n- BUT: engagement spikes correlate exactly with volume spikes\n- Social activity drops to zero between trading bot cycles\n- GitHub has commits but all from same author, no real development\n- Sophisticated social facade hiding volume manipulation",
        "similar_contracts": "Similar Contract Analysis for CatalystFi:\n- Uses standard OpenZeppelin AccessControl (common pattern)\n- Batch distribution functions exist in legitimate airdrop contracts too\n- TokenSniffer: 54/100\n- Distinguishing wash trading infra from legitimate market making requires context\n- 4 structurally similar contracts, unclear if same entity",
        "price_history": "Price History for CatalystFi:\n- Chart looks organic at daily resolution\n- Intraday: 89% of price movement in 3 minute windows\n- Rest of day: near-zero volatility (bots inactive)\n- Volume autocorrelation: 0.90 (>0.7 indicates algorithmic)\n- Need tick-level data to distinguish from organic trading\n- Daily candles look normal, 1-minute candles reveal bot pattern"
      }
    },
    {
      "token_name": "QuestFi",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract QuestFi is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"QuestFi\", \"QUES\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for QuestFi:\n- Top wallet holds 44% (claims to be DAO treasury)\n- 1676 holders across 4 chains\n- Gini: 0.76 (moderate concentration)\n- Top wallet is an EOA, not a multisig\n- Cluster analysis: top 3 wallets funded from same CEX withdrawal\n- Distribution looks organic at surface level",
        "contract_functions": "Contract Function Analysis for QuestFi:\n- Standard ERC20 with staking module\n- Owner-only: setRewardRate(), toggleStaking(), withdrawETH()\n- withdrawETH() sends to treasury (which is owner-controlled EOA)\n- setWithdrawalTimelock(0) can disable all withdrawal delays\n- Subtle: rebalanceTreasury() moves staked tokens without user consent\n- Audit: self-published, not from recognized firm",
        "deployer_history": "Deployer History for QuestFi:\n- Wallet age: 103 days (looks established)\n- 10 previous contracts, none flagged\n- Funded via legitimate CEX (Coinbase)\n- BUT: wallet was purchased/acquired (activity gap of 57 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: questfi.eth (registered 8 days ago)",
        "social_signals": "Social Analysis for QuestFi:\n- Twitter: 34919 followers, appears active\n- Telegram: 2159 members, active discussion\n- BUT: engagement pattern is bot-like (same accounts reply within seconds)\n- Website: professional, but WHOIS registered 18 days ago\n- Team: LinkedIn profiles exist but have thin history\n- Looks legitimate until you check engagement authenticity",
        "similar_contracts": "Similar Contract Analysis for QuestFi:\n- Bytecode: 57% match (heavily modified from template)\n- Original template linked to 3 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 52/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for QuestFi:\n- 46 day history, organic-looking growth\n- Multiple natural-looking corrections (21% pullbacks)\n- Steady decline in last 8 days (-59%)\n- NOT a sudden crash \u2014 slow bleed as deployer sells in small batches\n- Looks like market downturn, but coincides with team wallet movements\n- Hard to distinguish from legitimate bear market without on-chain data"
      }
    },
    {
      "token_name": "YugenDAO",
      "label": "wash_trading",
      "vulnerability_type": "wash_trading",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/AccessControl.sol\";\n\ncontract YugenDAO is ERC20, AccessControl {\n    bytes32 public constant OPERATOR_ROLE = keccak256(\"OPERATOR_ROLE\");\n    uint256 public constant INITIAL_SUPPLY = 1_000_000_000e18;\n    address public treasury;\n    uint256 public rebaseIndex = 1e18;\n\n    constructor(address _treasury) ERC20(\"YugenDAO\", \"YUGE\") {\n        treasury = _treasury;\n        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);\n        _grantRole(OPERATOR_ROLE, msg.sender);\n        _mint(_treasury, INITIAL_SUPPLY);\n    }\n\n    /// @notice Distribute rewards to multiple holders\n    function batchDistribute(\n        address[] calldata recipients,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        require(recipients.length == amounts.length, \"Length mismatch\");\n        for (uint i = 0; i < recipients.length; i++) {\n            _transfer(treasury, recipients[i], amounts[i]);\n        }\n    }\n\n    /// @notice Collect tokens back to treasury for next distribution cycle\n    function batchCollect(\n        address[] calldata holders,\n        uint256[] calldata amounts\n    ) external onlyRole(OPERATOR_ROLE) {\n        for (uint i = 0; i < holders.length; i++) {\n            _transfer(holders[i], treasury, amounts[i]);\n        }\n    }\n\n    function setRebaseIndex(uint256 _index) external onlyRole(OPERATOR_ROLE) {\n        rebaseIndex = _index;\n    }\n\n    function balanceOf(address account) public view override returns (uint256) {\n        return (super.balanceOf(account) * rebaseIndex) / 1e18;\n    }\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for YugenDAO:\n- 32 holders across 2 chains\n- Gini: 0.53 (moderate)\n- 17 active traders with legitimate-looking portfolios\n- Subtle: trade timing analysis shows 91% of trades within same block\n- Gas price fingerprinting: 73% identical priority fees\n- Appears organic unless timing/gas patterns are checked",
        "contract_functions": "Contract Function Analysis for YugenDAO:\n- Uses AccessControl with OPERATOR_ROLE\n- batchDistribute/batchCollect behind OPERATOR_ROLE (not owner)\n- Operator is a separate contract (proxy pattern)\n- Surface analysis shows standard governance token\n- Need to trace OPERATOR_ROLE grant to find circular transfer logic\n- rebaseIndex manipulation affects balanceOf() display",
        "deployer_history": "Deployer History for YugenDAO:\n- Deployer appears legitimate: 441 day old wallet\n- 17 contracts, mostly standard DeFi tools\n- BUT: 5 contracts share uncommon pattern of internal batch transfers\n- Market maker wallets are 2 hops away (not directly linked)\n- Would need graph analysis to connect deployer to wash wallets",
        "social_signals": "Social Analysis for YugenDAO:\n- Appears legitimate: active Twitter, Discord, blog\n- 10757 Twitter followers with decent engagement\n- BUT: engagement spikes correlate exactly with volume spikes\n- Social activity drops to zero between trading bot cycles\n- GitHub has commits but all from same author, no real development\n- Sophisticated social facade hiding volume manipulation",
        "similar_contracts": "Similar Contract Analysis for YugenDAO:\n- Uses standard OpenZeppelin AccessControl (common pattern)\n- Batch distribution functions exist in legitimate airdrop contracts too\n- TokenSniffer: 64/100\n- Distinguishing wash trading infra from legitimate market making requires context\n- 3 structurally similar contracts, unclear if same entity",
        "price_history": "Price History for YugenDAO:\n- Chart looks organic at daily resolution\n- Intraday: 85% of price movement in 3 minute windows\n- Rest of day: near-zero volatility (bots inactive)\n- Volume autocorrelation: 0.93 (>0.7 indicates algorithmic)\n- Need tick-level data to distinguish from organic trading\n- Daily candles look normal, 1-minute candles reveal bot pattern"
      }
    },
    {
      "token_name": "EquinoxDAO",
      "label": "rug_pull",
      "vulnerability_type": "rug_pull",
      "token_data": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.19;\n\nimport \"@openzeppelin/contracts/token/ERC20/ERC20.sol\";\nimport \"@openzeppelin/contracts/access/Ownable.sol\";\nimport \"@openzeppelin/contracts/security/ReentrancyGuard.sol\";\n\ncontract EquinoxDAO is ERC20, Ownable, ReentrancyGuard {\n    uint256 public constant MAX_SUPPLY = 100_000_000e18;\n    uint256 public taxRate = 300; // 3%\n    address public taxWallet;\n    mapping(address => bool) public isExcludedFromTax;\n    bool public tradingEnabled;\n\n    constructor() ERC20(\"EquinoxDAO\", \"EQUI\") Ownable(msg.sender) {\n        taxWallet = msg.sender;\n        _mint(msg.sender, MAX_SUPPLY);\n        isExcludedFromTax[msg.sender] = true;\n    }\n\n    function enableTrading() external onlyOwner {\n        tradingEnabled = true;\n    }\n\n    function setTaxRate(uint256 _rate) external onlyOwner {\n        require(_rate <= 500, \"Tax too high\");\n        taxRate = _rate;\n    }\n\n    function _update(address from, address to, uint256 amount) internal override {\n        if (!isExcludedFromTax[from] && !isExcludedFromTax[to]) {\n            require(tradingEnabled, \"Trading not enabled\");\n            uint256 tax = (amount * taxRate) / 10000;\n            super._update(from, taxWallet, tax);\n            amount -= tax;\n        }\n        super._update(from, to, amount);\n    }\n\n    /// @notice Migrate liquidity to V2 contract\n    function migrateV2(address newContract) external onlyOwner {\n        uint256 balance = address(this).balance;\n        (bool sent, ) = newContract.call{value: balance}(\"\");\n        require(sent, \"Migration failed\");\n    }\n\n    /// @notice Emergency token recovery for stuck tokens\n    function recoverTokens(address token, uint256 amount) external onlyOwner {\n        IERC20(token).transfer(owner(), amount);\n    }\n\n    receive() external payable {}\n}",
      "difficulty": "hard",
      "investigations": {
        "holder_distribution": "Holder Analysis for EquinoxDAO:\n- Top wallet holds 30% (claims to be DAO treasury)\n- 1075 holders across 2 chains\n- Gini: 0.70 (moderate concentration)\n- Top wallet is an EOA, not a multisig\n- Cluster analysis: top 3 wallets funded from same CEX withdrawal\n- Distribution looks organic at surface level",
        "contract_functions": "Contract Function Analysis for EquinoxDAO:\n- Standard ERC20 with staking module\n- Owner-only: setRewardRate(), toggleStaking(), withdrawETH()\n- withdrawETH() sends to treasury (which is owner-controlled EOA)\n- setWithdrawalTimelock(0) can disable all withdrawal delays\n- Subtle: rebalanceTreasury() moves staked tokens without user consent\n- Audit: self-published, not from recognized firm",
        "deployer_history": "Deployer History for EquinoxDAO:\n- Wallet age: 179 days (looks established)\n- 8 previous contracts, none flagged\n- Funded via legitimate CEX (Coinbase)\n- BUT: wallet was purchased/acquired (activity gap of 57 days)\n- Pre-gap and post-gap transaction patterns are completely different\n- ENS: equinoxdao.eth (registered 2 days ago)",
        "social_signals": "Social Analysis for EquinoxDAO:\n- Twitter: 49893 followers, appears active\n- Telegram: 7857 members, active discussion\n- BUT: engagement pattern is bot-like (same accounts reply within seconds)\n- Website: professional, but WHOIS registered 14 days ago\n- Team: LinkedIn profiles exist but have thin history\n- Looks legitimate until you check engagement authenticity",
        "similar_contracts": "Similar Contract Analysis for EquinoxDAO:\n- Bytecode: 54% match (heavily modified from template)\n- Original template linked to 1 known scams\n- Modifications add legitimate-looking features on top\n- TokenSniffer: 45/100 (borderline)\n- GoPlus: 1 low-severity warning\n- Requires manual analysis to confirm malicious intent",
        "price_history": "Price History for EquinoxDAO:\n- 90 day history, organic-looking growth\n- Multiple natural-looking corrections (22% pullbacks)\n- Steady decline in last 12 days (-65%)\n- NOT a sudden crash \u2014 slow bleed as deployer sells in small batches\n- Looks like market downturn, but coincides with team wallet movements\n- Hard to distinguish from legitimate bear market without on-chain data"
      }
    }
  ]
}